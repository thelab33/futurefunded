# =============================================================================
# FutureFunded — .gitignore (Production / SaaS / Playwright Hardened • Enterprise)
# =============================================================================

# -----------------------------------------------------------------------------
# Python
# -----------------------------------------------------------------------------
__pycache__/
*.py[cod]
*.pyd
*.pyo

# Virtualenvs
.venv/
venv/
ENV/
env/

# -----------------------------------------------------------------------------
# Flask / App runtime
# -----------------------------------------------------------------------------
instance/
app/data/*.db
*.sqlite
*.sqlite3

# -----------------------------------------------------------------------------
# Node / Frontend build outputs
# -----------------------------------------------------------------------------
node_modules/
dist/
build/

# -----------------------------------------------------------------------------
# Playwright (ALL artifacts)
# -----------------------------------------------------------------------------
test-results/
playwright-report/
.playwright/
.trace/
*.zip
*.webm
*.mp4
*.har

# -----------------------------------------------------------------------------
# Test / CI artifacts (local)
# -----------------------------------------------------------------------------
artifacts/
.reports/
out/
tools/.artifacts/

# Generated media ONLY inside artifact/output dirs (do NOT globally ignore images)
artifacts/**/*.{png,jpg,jpeg,webp,gif,avif}
.reports/**/*.{png,jpg,jpeg,webp,gif,avif}
tools/.artifacts/**/*.{png,jpg,jpeg,webp,gif,avif}
out/**/*.{png,jpg,jpeg,webp,gif,avif}

# -----------------------------------------------------------------------------
# Local backups / patch artifacts
# -----------------------------------------------------------------------------
*.bak
*.bak.*
*.BAK
*.BAK.*
*.orig
*.rej
*.fix*
*.patch*

# Common editor backup patterns
*~
*.save

# -----------------------------------------------------------------------------
# Logs & temp files
# -----------------------------------------------------------------------------
*.log
*.tmp
*.temp

# -----------------------------------------------------------------------------
# Editor / OS noise
# -----------------------------------------------------------------------------
.DS_Store
Thumbs.db
*.swp
*.swo
*:Zone.Identifier

.vscode/
.idea/
.husky/

# -----------------------------------------------------------------------------
# Coverage / reports
# -----------------------------------------------------------------------------
coverage/
.nyc_output/
htmlcov/

# -----------------------------------------------------------------------------
# Environment files (never commit secrets)
# -----------------------------------------------------------------------------
.env
.env.*
.cache/

# -----------------------------------------------------------------------------
# Local audit dumps & one-off outputs (root)
# -----------------------------------------------------------------------------
classes.txt
ids.txt
data_attrs.txt
css_vs_html_presence.txt
inline_events_hits.txt
inline_styles_hits.txt
style-coverage.json
DOM_CONTRACT_full_snapshot_*.txt
stripe_fill_fail_*.png

# Catch common audit dump patterns (safe)
audit-*
*coverage*.json
*coverage*.css
*coverage*.txt

# -----------------------------------------------------------------------------
# Local archives / scratch (keep out of repo)
# -----------------------------------------------------------------------------
app/templates_backup/
app/static/css_backup/
app/static/css/archive/
app/static/_unused/

# -----------------------------------------------------------------------------
# Template scratch snapshots (explicit)
# -----------------------------------------------------------------------------
app/templates/index.pre-overlay-dedupe.html
app/templates/index.vnext.html

# -----------------------------------------------------------------------------
# Don’t track backup JS/CSS snapshots (keep real assets tracked)
# -----------------------------------------------------------------------------
app/static/js/*.bak*
app/static/css/*.bak*
app/templates/*.bak*
app/__init__.py.bak*
run.py.bak*

# -----------------------------------------------------------------------------
# Tooling scratch (keep repo focused; avoid tool sprawl)
# -----------------------------------------------------------------------------
missing-selectors.css
style-audit.css
ff_css_classes_count.txt
index_selectors.json

ff_*_autopatch*.py
ff_*_audit*.py
ff_*_coverage*.py
ff_*_doctor*.py
ff_*_sanitize*.py
ff_*_superpatch*.py

launch_audit.py
safe_add_detect_env.py
safe_fix_detect_env_load.py

# If these are generated locally (common in your workflow)
tools/_backups/
tools/.playwright/

# -----------------------------------------------------------------------------
# Optional: local tooling outputs (uncomment if you generate these)
# -----------------------------------------------------------------------------
# .mypy_cache/
# .pytest_cache/
# .ruff_cache/
# .tox/
# pip-wheel-metadata/