@layer ff.tokens, ff.base, ff.type, ff.layout, ff.surfaces, ff.controls, ff.pages, ff.utilities, ff.overrides;

@layer ff.tokens {
  .ff-root,
  :root {
    --ff-font-sans: Inter, ui-sans-serif, system-ui, -apple-system, BlinkMacSystemFont, "Segoe UI", sans-serif;
    --ff-font-mono: ui-monospace, SFMono-Regular, Menlo, Monaco, Consolas, "Liberation Mono", monospace;

    --ff-0: 0;
    --ff-1: 0.25rem;
    --ff-2: 0.5rem;
    --ff-3: 0.75rem;
    --ff-4: 1rem;
    --ff-5: 1.25rem;
    --ff-6: 1.5rem;
    --ff-7: 2rem;
    --ff-8: 2.5rem;
    --ff-9: 3rem;
    --ff-10: 4rem;
    --ff-11: 5rem;

    --ff-r-1: 0.5rem;
    --ff-r-2: 0.75rem;
    --ff-r-3: 1rem;
    --ff-r-4: 1.25rem;
    --ff-r-5: 1.75rem;
    --ff-pill: 999px;

    --ff-shadow-1: 0 1px 2px rgba(15, 23, 42, 0.06), 0 8px 24px rgba(15, 23, 42, 0.05);
    --ff-shadow-2: 0 6px 18px rgba(15, 23, 42, 0.08), 0 20px 50px rgba(15, 23, 42, 0.10);
    --ff-shadow-3: 0 12px 32px rgba(15, 23, 42, 0.12), 0 28px 88px rgba(15, 23, 42, 0.14);

    --ff-fs-100: 0.75rem;
    --ff-fs-200: 0.8125rem;
    --ff-fs-300: 0.875rem;
    --ff-fs-400: 0.9375rem;
    --ff-fs-500: 1rem;
    --ff-fs-600: 1.125rem;
    --ff-fs-700: 1.25rem;
    --ff-fs-800: 1.5rem;
    --ff-fs-900: clamp(1.875rem, 4vw, 3rem);
    --ff-fs-display: clamp(2.25rem, 5vw, 4.75rem);

    --ff-lh-tight: 1.05;
    --ff-lh-title: 1.12;
    --ff-lh-body: 1.6;
    --ff-lh-loose: 1.75;

    --ff-bg: #f6f8fb;
    --ff-bg-subtle: #fbfcfe;
    --ff-bg-muted: #edf2f8;
    --ff-bg-strong: #e2eaf4;
    --ff-surface: rgba(255, 255, 255, 0.86);
    --ff-surface-2: rgba(255, 255, 255, 0.96);
    --ff-surface-3: #ffffff;
    --ff-surface-inverse: #0f172a;

    --ff-text: #0f172a;
    --ff-text-soft: #334155;
    --ff-text-muted: #64748b;
    --ff-text-inverse: #f8fafc;

    --ff-border-subtle: rgba(15, 23, 42, 0.08);
    --ff-border-strong: rgba(15, 23, 42, 0.14);

    --ff-brand: #0ea5e9;
    --ff-brand-strong: #0369a1;
    --ff-brand-soft: rgba(14, 165, 233, 0.12);

    --ff-accent: #f97316;
    --ff-accent-strong: #c2410c;
    --ff-accent-soft: rgba(249, 115, 22, 0.12);

    --ff-success: #0f9f6e;
    --ff-success-soft: rgba(15, 159, 110, 0.12);

    --ff-warn: #b45309;
    --ff-warn-soft: rgba(180, 83, 9, 0.12);

    --ff-danger: #b91c1c;
    --ff-danger-soft: rgba(185, 28, 28, 0.12);

    --ff-focus: #0369a1;
    --ff-ring: 0 0 0 3px rgba(3, 105, 161, 0.18);
    --ff-backdrop: rgba(2, 6, 23, 0.58);

    --ff-container: 75rem;
    --ff-container-wide: 80rem;
    --ff-reading: 42rem;
    --ff-section-pad: clamp(3rem, 6vw, 5rem);

    --ff-bp-sm: 40rem;
    --ff-bp-md: 48rem;
    --ff-bp-lg: 64rem;
    --ff-bp-xl: 80rem;

    --ff-z-base: 1;
    --ff-z-chrome: 30;
    --ff-z-fab: 40;
    --ff-z-tabs: 50;
    --ff-z-overlay: 70;
    --ff-z-toast: 90;
    --ff-z-skip: 100;

    --ff-nav-h: 5rem;
    --ff-tabs-h: 4.25rem;

    --ff-blur: 14px;
    --ff-t-fast: 140ms ease;
    --ff-t-med: 220ms ease;
    --ff-t-slow: 360ms cubic-bezier(.2, .8, .2, 1);
  }

  .ff-root[data-theme="light"] {
    color-scheme: light;
  }

  .ff-root[data-theme="dark"] {
    --ff-bg: #06090f;
    --ff-bg-subtle: #0a1019;
    --ff-bg-muted: #0f172a;
    --ff-bg-strong: #162234;
    --ff-surface: rgba(11, 18, 32, 0.76);
    --ff-surface-2: rgba(11, 18, 32, 0.9);
    --ff-surface-3: #0b1220;
    --ff-surface-inverse: #f8fafc;

    --ff-text: #f8fafc;
    --ff-text-soft: #dbe5f0;
    --ff-text-muted: #94a3b8;
    --ff-text-inverse: #020617;

    --ff-border-subtle: rgba(148, 163, 184, 0.16);
    --ff-border-strong: rgba(148, 163, 184, 0.26);

    --ff-brand: #38bdf8;
    --ff-brand-strong: #7dd3fc;
    --ff-brand-soft: rgba(56, 189, 248, 0.16);

    --ff-accent: #fb923c;
    --ff-accent-strong: #fdba74;
    --ff-accent-soft: rgba(251, 146, 60, 0.16);

    --ff-success: #34d399;
    --ff-success-soft: rgba(52, 211, 153, 0.16);

    --ff-warn: #f59e0b;
    --ff-warn-soft: rgba(245, 158, 11, 0.16);

    --ff-danger: #f87171;
    --ff-danger-soft: rgba(248, 113, 113, 0.16);

    --ff-focus: #7dd3fc;
    --ff-ring: 0 0 0 3px rgba(125, 211, 252, 0.2);
    --ff-backdrop: rgba(2, 6, 23, 0.72);

    --ff-shadow-1: 0 1px 2px rgba(0, 0, 0, 0.34), 0 12px 28px rgba(0, 0, 0, 0.22);
    --ff-shadow-2: 0 8px 24px rgba(0, 0, 0, 0.34), 0 28px 70px rgba(0, 0, 0, 0.28);
    --ff-shadow-3: 0 16px 36px rgba(0, 0, 0, 0.4), 0 42px 110px rgba(0, 0, 0, 0.34);
    color-scheme: dark;
  }

  @media (prefers-color-scheme: dark) {
    .ff-root:not([data-theme="light"]) {
      --ff-bg: #06090f;
      --ff-bg-subtle: #0a1019;
      --ff-bg-muted: #0f172a;
      --ff-bg-strong: #162234;
      --ff-surface: rgba(11, 18, 32, 0.76);
      --ff-surface-2: rgba(11, 18, 32, 0.9);
      --ff-surface-3: #0b1220;
      --ff-surface-inverse: #f8fafc;

      --ff-text: #f8fafc;
      --ff-text-soft: #dbe5f0;
      --ff-text-muted: #94a3b8;
      --ff-text-inverse: #020617;

      --ff-border-subtle: rgba(148, 163, 184, 0.16);
      --ff-border-strong: rgba(148, 163, 184, 0.26);

      --ff-brand: #38bdf8;
      --ff-brand-strong: #7dd3fc;
      --ff-brand-soft: rgba(56, 189, 248, 0.16);

      --ff-accent: #fb923c;
      --ff-accent-strong: #fdba74;
      --ff-accent-soft: rgba(251, 146, 60, 0.16);

      --ff-success: #34d399;
      --ff-success-soft: rgba(52, 211, 153, 0.16);

      --ff-warn: #f59e0b;
      --ff-warn-soft: rgba(245, 158, 11, 0.16);

      --ff-danger: #f87171;
      --ff-danger-soft: rgba(248, 113, 113, 0.16);

      --ff-focus: #7dd3fc;
      --ff-ring: 0 0 0 3px rgba(125, 211, 252, 0.2);
      --ff-backdrop: rgba(2, 6, 23, 0.72);
      --ff-shadow-1: 0 1px 2px rgba(0, 0, 0, 0.34), 0 12px 28px rgba(0, 0, 0, 0.22);
      --ff-shadow-2: 0 8px 24px rgba(0, 0, 0, 0.34), 0 28px 70px rgba(0, 0, 0, 0.28);
      --ff-shadow-3: 0 16px 36px rgba(0, 0, 0, 0.4), 0 42px 110px rgba(0, 0, 0, 0.34);
      color-scheme: dark;
    }
  }

  @media (prefers-color-scheme: light) {
    .ff-root:not([data-theme="dark"]) {
      color-scheme: light;
    }
  }
}

@layer ff.base {
  *,
  *::before,
  *::after {
    box-sizing: border-box;
  }

  html {
    color-scheme: light dark;
    scroll-behavior: smooth;
    overflow-x: hidden;
  }

  body,
  h1,
  h2,
  h3,
  h4,
  h5,
  h6,
  p,
  ul,
  ol,
  dl,
  dd,
  figure,
  blockquote {
    margin: 0;
  }

  html,
  body {
    min-width: 0;
    overflow-x: hidden;
  }

  body,
  body.ff-body {
    min-height: 100vh;
    background:
      radial-gradient(circle at top left, rgba(14, 165, 233, 0.06), transparent 28rem),
      radial-gradient(circle at top right, rgba(249, 115, 22, 0.04), transparent 24rem),
      linear-gradient(180deg, var(--ff-bg) 0%, var(--ff-bg) 100%);
    color: var(--ff-text);
    font-family: var(--ff-font-sans);
    font-size: var(--ff-fs-500);
    line-height: var(--ff-lh-body);
    text-rendering: optimizeLegibility;
    -webkit-font-smoothing: antialiased;
  }

  img,
  svg,
  video,
  canvas,
  picture {
    display: block;
    max-width: 100%;
    height: auto;
  }

  iframe {
    max-width: 100%;
    border: 0;
  }

  button,
  input,
  textarea,
  select {
    font: inherit;
    color: inherit;
  }

  button {
    border: 0;
    background: none;
    padding: 0;
    cursor: pointer;
  }

  a {
    color: inherit;
    text-decoration-thickness: 1.2px;
    text-underline-offset: 0.16em;
  }

  p,
  li,
  dd,
  dt,
  span,
  a,
  label,
  small {
    overflow-wrap: anywhere;
    word-break: break-word;
  }

  ul[role="list"],
  ol[role="list"] {
    list-style: none;
    padding: 0;
  }

  table {
    width: 100%;
    border-collapse: collapse;
  }

  progress {
    vertical-align: baseline;
  }

  [hidden] {
    display: none !important;
  }

  ::selection {
    background: var(--ff-brand-soft);
    color: var(--ff-text);
  }

  :focus-visible {
    outline: 2px solid var(--ff-focus);
    outline-offset: 2px;
    box-shadow: var(--ff-ring);
  }

  .ff-root[data-ff-webdriver="true"] :focus-visible {
    outline-width: 3px;
  }

  .ff-shell {
    position: relative;
    min-height: 100vh;
    isolation: isolate;
  }

  .ff-shellBg {
    position: fixed;
    inset: 0;
    z-index: -1;
    pointer-events: none;
    background:
      linear-gradient(180deg, rgba(255, 255, 255, 0) 0%, rgba(14, 165, 233, 0.025) 100%),
      radial-gradient(circle at 18% 0%, rgba(14, 165, 233, 0.08), transparent 34rem),
      radial-gradient(circle at 92% 10%, rgba(249, 115, 22, 0.06), transparent 24rem);
  }

  .ff-container {
    width: 100%;
    max-width: 1200px;
    margin-inline: auto;
    padding-inline: var(--ff-4);
  }

  .ff-main {
    display: block;
    min-width: 0;
    padding-block-end: calc(var(--ff-tabs-h) + env(safe-area-inset-bottom, 0px) + 1rem);
  }

  .ff-section {
    position: relative;
    min-width: 0;
    max-width: 100%;
    padding-block: var(--ff-section-pad);
  }

  .ff-section--hero {
    padding-block-start: clamp(1.5rem, 4vw, 2.5rem);
    padding-block-end: clamp(3rem, 6vw, 5rem);
  }

  .ff-link {
    color: var(--ff-brand-strong);
    text-decoration: underline;
  }

  .ff-link:hover {
    color: var(--ff-brand);
  }

  .ff-shell,
  .ff-section,
  .ff-container,
  .ff-card {
    min-width: 0;
    max-width: 100%;
  }

  .ff-sr {
    position: absolute !important;
    width: 1px !important;
    height: 1px !important;
    margin: -1px !important;
    padding: 0 !important;
    overflow: hidden !important;
    clip: rect(0 0 0 0) !important;
    white-space: nowrap !important;
    border: 0 !important;
  }

  .ff-focus-probe,
  .ff-skip,
  .ff-skiplink {
    position: absolute;
    inset-inline-start: -9999px;
    inset-block-start: auto;
  }

  .ff-focus-probe:focus,
  .ff-focus-probe:focus-visible,
  .ff-skip:focus,
  .ff-skip:focus-visible,
  .ff-skiplink:focus,
  .ff-skiplink:focus-visible {
    inset-inline-start: 1rem;
    inset-block-start: 1rem;
    z-index: var(--ff-z-skip);
    display: inline-flex;
    align-items: center;
    gap: 0.5rem;
    padding: 0.625rem 0.875rem;
    background: var(--ff-surface-2);
    color: var(--ff-text);
    border: 1px solid var(--ff-border-strong);
    border-radius: var(--ff-r-2);
    box-shadow: var(--ff-shadow-2);
  }

  .ff-burst {
    position: fixed;
    inset: 0;
    z-index: 80;
    pointer-events: none;
  }

  @media (prefers-reduced-motion: reduce) {
    html {
      scroll-behavior: auto;
    }

    *,
    *::before,
    *::after {
      animation-duration: 0.01ms !important;
      animation-iteration-count: 1 !important;
      transition-duration: 0.01ms !important;
      scroll-behavior: auto !important;
    }
  }

  @media (forced-colors: active) {
    * {
      forced-color-adjust: auto;
    }

    :focus-visible {
      outline: 2px solid Highlight;
      box-shadow: none;
    }
  }
}

@layer ff.type {
  .ff-display,
  .ff-heroTitle {
    font-size: var(--ff-fs-display);
    line-height: 0.98;
    letter-spacing: -0.04em;
    font-weight: 800;
    color: var(--ff-text);
  }

  .ff-heroTitle {
    text-wrap: balance;
    max-width: 22ch;
    letter-spacing: -0.02em;
  }

  .ff-h2,
  .ff-modal__title {
    font-size: clamp(1.5rem, 2.4vw, 2rem);
    line-height: var(--ff-lh-title);
    letter-spacing: -0.03em;
    font-weight: 760;
    color: var(--ff-text);
  }

  .ff-h3,
  .ff-tierName,
  .ff-railcard__name {
    font-size: clamp(1.0625rem, 1.6vw, 1.25rem);
    line-height: 1.22;
    letter-spacing: -0.02em;
    font-weight: 720;
    color: var(--ff-text);
  }

  .ff-kicker,
  .ff-snapshotLabel,
  .ff-onboardSummary__label,
  .ff-label {
    font-size: var(--ff-fs-200);
    line-height: 1.2;
    font-weight: 760;
    text-transform: uppercase;
    letter-spacing: 0.09em;
    color: var(--ff-brand-strong);
  }

  .ff-lead,
  .ff-heroTagline {
    font-size: clamp(1rem, 1.4vw, 1.125rem);
    line-height: var(--ff-lh-loose);
    color: var(--ff-text-soft);
    max-width: 62ch;
  }

  .ff-help,
  .ff-brand__sub,
  .ff-tierList,
  .ff-storyList,
  .ff-footer__link,
  .ff-announce__text {
    font-size: var(--ff-fs-400);
    line-height: 1.55;
  }

  .ff-big,
  .ff-tierPrice,
  .ff-snapshotValue {
    font-size: clamp(1.5rem, 2.5vw, 2.35rem);
    line-height: 1.05;
    letter-spacing: -0.035em;
    font-weight: 800;
    color: var(--ff-text);
  }

  .ff-tierSuffix,
  .ff-muted,
  .ff-text-muted,
  .ff-heroMomentum,
  .ff-poweredby {
    color: var(--ff-text-muted);
  }

  .ff-brand__title,
  .ff-topbarBrand__text {
    font-size: 1rem;
    line-height: 1.15;
    font-weight: 780;
    letter-spacing: -0.02em;
    color: var(--ff-text);
  }

  .ff-brand__sub {
    color: var(--ff-text-muted);
  }

  .ff-heroName {
    color: var(--ff-brand-strong);
  }

  .ff-heroAccent {
    color: var(--ff-text-soft);
  }

  .ff-num,
  .ff-tierAmt,
  .ff-snapshotValue,
  .ff-tierPrice {
    font-variant-numeric: tabular-nums lining-nums;
    font-feature-settings: "tnum" 1, "lnum" 1;
  }

  .ff-quote {
    padding: 1rem 1.125rem;
    background: var(--ff-bg-muted);
    border-inline-start: 4px solid var(--ff-brand);
    border-radius: 0 var(--ff-r-2) var(--ff-r-2) 0;
  }

  .ff-heroMomentum {
    display: inline-flex;
    align-items: center;
    gap: 0.4rem;
    font-size: var(--ff-fs-300);
  }

  .ff-heroMomentum strong {
    color: var(--ff-text);
    font-weight: 700;
  }
}

@layer ff.layout {
  .ff-stack {
    display: flex;
    flex-direction: column;
    gap: var(--ff-4);
  }

  .ff-row {
    display: flex;
    gap: var(--ff-4);
  }

  .ff-wrap {
    flex-wrap: wrap;
  }

  .ff-ais {
    align-items: center;
  }

  .ff-jcb,
  .ff-row--between {
    justify-content: space-between;
  }

  .ff-grid {
    display: grid;
    gap: var(--ff-4);
  }

  .ff-grid--2 {
    grid-template-columns: repeat(2, minmax(0, 1fr));
  }

  .ff-minw-0 {
    min-width: 0;
  }

  .ff-w-100 {
    width: 100%;
  }

  .ff-nowrap {
    white-space: nowrap;
  }

  .ff-ta-right {
    text-align: right;
  }

  .ff-brand,
  .ff-topbarBrand,
  .ff-platformBrand {
    display: inline-flex;
    align-items: center;
    gap: 0.75rem;
    min-width: 0;
  }

  .ff-platformBrand__disc {
    display: inline-grid;
    place-items: center;
    width: 2rem;
    height: 2rem;
    border-radius: 999px;
    background: var(--ff-surface-2);
    border: 1px solid var(--ff-border-subtle);
  }

  .ff-brand img,
  .ff-topbarBrand img {
    border-radius: 999px;
    object-fit: cover;
    flex: 0 0 auto;
  }

  .ff-sectionhead {
    display: grid;
    gap: 1rem;
    align-items: end;
  }

  .ff-sectionhead__text,
  .ff-sectionhead__actions,
  .ff-heroMedia,
  .ff-heroPanel,
  .ff-progressCompact__summary,
  .ff-progressCompact__details,
  .ff-trustFaqCompact__signals,
  .ff-trustFaqCompact__faq,
  .ff-footerBrand--compact,
  .ff-footerNav--compact {
    min-width: 0;
  }

  .ff-hero__shell {
    position: relative;
  }

  .ff-hero__bg {
    position: absolute;
    inset: 0;
    overflow: clip;
    pointer-events: none;
  }

  .ff-hero__orb {
    position: absolute;
    width: 26rem;
    aspect-ratio: 1;
    border-radius: 50%;
    opacity: 0.18;
    filter: blur(48px);
  }

  .ff-hero__orb--a {
    inset-block-start: -8rem;
    inset-inline-start: -7rem;
    background: rgba(14, 165, 233, 0.24);
  }

  .ff-hero__orb--b {
    inset-block-start: 3rem;
    inset-inline-end: -8rem;
    background: rgba(249, 115, 22, 0.18);
  }

  .ff-hero__gridlines {
    position: absolute;
    inset: 0;
    background-image:
      linear-gradient(rgba(15, 23, 42, 0.04) 1px, transparent 1px),
      linear-gradient(90deg, rgba(15, 23, 42, 0.04) 1px, transparent 1px);
    background-size: 2.75rem 2.75rem;
    mask-image: linear-gradient(180deg, rgba(0, 0, 0, 0.7), transparent 86%);
  }

  .ff-root[data-theme="dark"] .ff-hero__gridlines {
    background-image:
      linear-gradient(rgba(148, 163, 184, 0.06) 1px, transparent 1px),
      linear-gradient(90deg, rgba(148, 163, 184, 0.06) 1px, transparent 1px);
  }

  @media (prefers-color-scheme: dark) {
    .ff-root:not([data-theme="light"]) .ff-hero__gridlines {
      background-image:
        linear-gradient(rgba(148, 163, 184, 0.06) 1px, transparent 1px),
        linear-gradient(90deg, rgba(148, 163, 184, 0.06) 1px, transparent 1px);
    }
  }

  .ff-hero__grid,
  .ff-storyGrid,
  .ff-progressCompact__hero,
  .ff-trustFaqCompact__grid,
  .ff-footer__grid,
  .ff-impactPick__chips,
  .ff-hero__kpis,
  .ff-rail__track,
  .ff-impactTierGrid,
  .ff-sponsorGrid,
  .ff-teamGrid,
  .ff-sponsorWall,
  .ff-footer__linkgrid,
  .ff-onboardGrid,
  .ff-onboardSummary__grid,
  .ff-faqList,
  .ff-drawer__grid,
  .ff-checkoutHead,
  .ff-checkoutBody,
  .ff-checkoutMethod {
    display: grid;
    gap: 1rem;
  }

  .ff-heroContext,
  .ff-heroCtas,
  .ff-trustSignals,
  .ff-tabs__list {
    display: flex;
    flex-wrap: wrap;
    gap: 0.625rem;
    padding: 0;
  }

  .ff-hero__grid,
  .ff-storyGrid,
  .ff-progressCompact__hero,
  .ff-trustFaqCompact__grid,
  .ff-footer__grid {
    gap: clamp(1rem, 3vw, 2rem);
  }

  .ff-impactTierGrid,
  .ff-sponsorGrid,
  .ff-teamGrid,
  .ff-sponsorWall,
  .ff-hero__kpis,
  .ff-impactPick__chips {
    grid-template-columns: 1fr;
  }

  .ff-footer__linkgrid {
    grid-template-columns: repeat(2, minmax(0, 1fr));
  }

  .ff-rail__track {
    grid-template-columns: repeat(2, minmax(0, 1fr));
  }

  .ff-storyList,
  .ff-tierList {
    padding-inline-start: 1.1rem;
  }

  .ff-storyList li + li,
  .ff-tierList li + li {
    margin-block-start: 0.35rem;
  }

  .ff-onboardStepper {
    display: grid;
    grid-template-columns: repeat(2, minmax(0, 1fr));
    gap: 0.5rem;
  }

  @media (min-width: 40rem) {
    .ff-impactTierGrid,
    .ff-sponsorGrid,
    .ff-teamGrid,
    .ff-impactPick__chips {
      grid-template-columns: repeat(2, minmax(0, 1fr));
    }

    .ff-hero__kpis {
      grid-template-columns: repeat(3, minmax(0, 1fr));
    }

    .ff-storyGrid,
    .ff-progressCompact__hero,
    .ff-trustFaqCompact__grid {
      grid-template-columns: repeat(2, minmax(0, 1fr));
    }

    .ff-footer__grid {
      grid-template-columns: 1.2fr 1fr;
    }

    .ff-sponsorWall {
      grid-template-columns: repeat(3, minmax(0, 1fr));
    }

    .ff-onboardStepper {
      grid-template-columns: repeat(4, minmax(0, 1fr));
    }
  }

  @media (min-width: 48rem) {
    .ff-sectionhead {
      grid-template-columns: minmax(0, 1fr) auto;
    }

    .ff-onboardGrid,
    .ff-onboardSummary__grid {
      grid-template-columns: repeat(2, minmax(0, 1fr));
    }
  }

  @media (min-width: 64rem) {
    .ff-hero__grid {
      grid-template-columns: minmax(0, 1.45fr) minmax(19rem, 0.85fr);
    }

    .ff-rail__track {
      grid-template-columns: repeat(3, minmax(0, 1fr));
    }

    .ff-impactTierGrid,
    .ff-sponsorGrid {
      grid-template-columns: repeat(4, minmax(0, 1fr));
    }

    .ff-teamGrid {
      grid-template-columns: repeat(3, minmax(0, 1fr));
    }

    .ff-progressCompact__hero {
      grid-template-columns: minmax(19rem, 0.9fr) minmax(0, 1.1fr);
    }
  }

  @media (min-width: 80rem) {
    .ff-container--wide {
      max-width: var(--ff-container-wide);
    }
  }
}

@layer ff.surfaces {
  .ff-glass,
  .ff-surface,
  .ff-card,
  .ff-panel,
  .ff-modal__panel,
  .ff-sheet__panel,
  .ff-drawer__panel {
    background: var(--ff-surface);
    border: 1px solid var(--ff-border-subtle);
    box-shadow: var(--ff-shadow-1);
    backdrop-filter: blur(var(--ff-blur));
    -webkit-backdrop-filter: blur(var(--ff-blur));
  }

  .ff-card,
  .ff-surface,
  .ff-glass,
  .ff-panel,
  .ff-modal__panel,
  .ff-sheet__panel,
  .ff-drawer__panel {
    border-radius: var(--ff-r-4);
  }

  .ff-card,
  .ff-kpiCard,
  .ff-impactTier,
  .ff-tierCard,
  .ff-teamCard,
  .ff-legalCard,
  .ff-videoNote,
  .ff-checkoutSupport,
  .ff-trustMiniPolicy,
  .ff-sponsorCard {
    transition:
      transform .15s ease,
      box-shadow .15s ease,
      border-color var(--ff-t-fast),
      background-color var(--ff-t-fast);
  }

  .ff-card:hover,
  .ff-kpiCard:hover,
  .ff-impactTier:hover,
  .ff-tierCard:hover,
  .ff-teamCard:hover,
  .ff-sponsorCard:hover {
    transform: translateY(-3px);
    box-shadow: var(--ff-shadow-2);
  }

  .ff-pad {
    padding: clamp(1rem, 2vw, 1.5rem);
  }

  .ff-card--premium,
  .ff-tierCard--recommended,
  .ff-topbar__capsule,
  .ff-footerTray,
  .ff-progressCompact,
  .ff-trustFaqCompact {
    background: var(--ff-surface-2);
    box-shadow: var(--ff-shadow-2);
  }

  .ff-divider {
    border: 0;
    height: 1px;
    background: var(--ff-border-subtle);
  }

  .ff-pill,
  .ff-chip,
  .ff-tierPill,
  .ff-railcard__chip {
    display: inline-flex;
    align-items: center;
    gap: 0.45rem;
    min-height: 2rem;
    padding: 0.35rem 0.7rem;
    border-radius: var(--ff-pill);
    border: 1px solid var(--ff-border-subtle);
    background: var(--ff-surface-3);
    color: var(--ff-text-soft);
    font-size: var(--ff-fs-200);
    font-weight: 700;
    line-height: 1;
  }

  .ff-pill--soft {
    background: var(--ff-bg-muted);
  }

  .ff-pill--ghost {
    background: transparent;
  }

  .ff-pill--accent,
  .ff-pill--verified,
  .is-selected {
    background: var(--ff-brand-soft);
    color: var(--ff-brand-strong);
    border-color: rgba(14, 165, 233, 0.24);
  }

  .ff-pill--live,
  .is-ready {
    background: var(--ff-success-soft);
    color: var(--ff-success);
    border-color: rgba(15, 159, 110, 0.24);
  }

  .ff-dot {
    width: 0.5rem;
    height: 0.5rem;
    border-radius: 999px;
    background: currentColor;
    box-shadow: 0 0 0 0.2rem currentColor;
    opacity: 0.28;
  }

  .ff-alert {
    padding: 0.875rem 1rem;
    border-radius: var(--ff-r-2);
    border: 1px solid var(--ff-border-subtle);
    font-size: var(--ff-fs-400);
  }

  .ff-alert--info {
    background: var(--ff-brand-soft);
    color: var(--ff-text-soft);
  }

  .ff-alert--success {
    background: var(--ff-success-soft);
    color: var(--ff-success);
  }

  .ff-alert--error,
  .is-error {
    background: var(--ff-danger-soft);
    color: var(--ff-danger);
    border-color: rgba(185, 28, 28, 0.24);
  }

  .ff-kpiCard,
  .ff-impactTier,
  .ff-tierCard,
  .ff-teamCard,
  .ff-legalCard,
  .ff-videoNote,
  .ff-checkoutSupport,
  .ff-trustMiniPolicy,
  .ff-sponsorCard,
  .ff-trustSignal,
  .ff-progressCompact__summary,
  .ff-progressCompact__use,
  .ff-snapshotItem,
  .ff-onboardSummary__item,
  .ff-sponsorWall__item,
  .ff-activityFeed__item {
    background: var(--ff-surface-3);
    border: 1px solid var(--ff-border-subtle);
    border-radius: var(--ff-r-3);
    box-shadow: var(--ff-shadow-1);
  }

  .ff-kpiCard,
  .ff-impactTier,
  .ff-tierCard,
  .ff-teamCard,
  .ff-sponsorCard {
    padding: 1rem;
  }

  .ff-tierCard--recommended,
  .ff-teamCard.is-featured,
  .ff-sponsorCard--founder,
  .is-vip {
    border-color: rgba(14, 165, 233, 0.28);
    box-shadow: var(--ff-shadow-2);
  }

  .ff-sponsorCard--founder,
  .ff-progressCompact__summary,
  .ff-onboardSummary {
    background:
      radial-gradient(circle at top right, rgba(14, 165, 233, 0.08), transparent 8rem),
      linear-gradient(180deg, var(--ff-surface-3), var(--ff-bg-muted));
  }

  .ff-poweredby {
    font-size: var(--ff-fs-200);
  }

  .ff-meter {
    position: relative;
  }

  .ff-meter__progress {
    width: 100%;
    height: 0.9rem;
    appearance: none;
    border: 0;
    border-radius: 999px;
    overflow: hidden;
    background: var(--ff-bg-strong);
  }

  .ff-meter__progress::-webkit-progress-bar {
    background: var(--ff-bg-strong);
  }

  .ff-meter__progress::-webkit-progress-value {
    background: linear-gradient(90deg, var(--ff-brand) 0%, var(--ff-accent) 100%);
    border-radius: 999px;
  }

  .ff-meter__progress::-moz-progress-bar {
    background: linear-gradient(90deg, var(--ff-brand) 0%, var(--ff-accent) 100%);
    border-radius: 999px;
  }

  .ff-payment-trust {
    display: inline-flex;
    align-items: center;
    gap: 0.55rem;
    color: var(--ff-text-soft);
    font-size: var(--ff-fs-400);
  }

  .ff-vipSpotlight {
    position: relative;
    overflow: hidden;
    padding: 1rem;
    background:
      radial-gradient(circle at top right, rgba(14, 165, 233, 0.12), transparent 8rem),
      linear-gradient(180deg, var(--ff-surface-3), var(--ff-bg-muted));
    border: 1px solid var(--ff-border-subtle);
    border-radius: var(--ff-r-3);
    box-shadow: var(--ff-shadow-1);
  }

  .ff-vipSpotlight::after {
    content: "";
    position: absolute;
    inset: auto 1rem 1rem auto;
    width: 3rem;
    height: 3rem;
    border-radius: 999px;
    background: radial-gradient(circle, rgba(14, 165, 233, 0.18), transparent 70%);
    pointer-events: none;
  }

  .ff-skeleton {
    position: relative;
    overflow: hidden;
    border-radius: inherit;
    background: linear-gradient(90deg, var(--ff-bg-muted) 0%, var(--ff-bg-strong) 50%, var(--ff-bg-muted) 100%);
    background-size: 200% 100%;
    animation: ff-shimmer 1.25s linear infinite;
  }

  .ff-skeleton--payment {
    min-height: 3.75rem;
  }

  .ff-skeleton--video {
    min-height: 16rem;
  }

  .ff-sponsorWall__item.ff-skeleton {
    min-height: 4.75rem;
  }

  .is-loading {
    cursor: progress;
  }

  .is-loading,
  .is-loading * {
    pointer-events: none;
  }

  .is-media-missing {
    position: relative;
    overflow: hidden;
    background:
      linear-gradient(180deg, rgba(14, 165, 233, 0.06), rgba(15, 23, 42, 0.03)),
      linear-gradient(135deg, #eef4f9, #dde8f3);
    border: 1px solid var(--ff-border-subtle);
  }

  .ff-root[data-theme="dark"] .is-media-missing {
    background:
      linear-gradient(180deg, rgba(56, 189, 248, 0.08), rgba(2, 6, 23, 0.1)),
      linear-gradient(135deg, #0f172a, #162235);
  }

  .is-media-missing > img {
    opacity: 0;
  }

  .is-media-missing::before {
    content: attr(data-ff-fallback-label);
    position: absolute;
    inset-inline: 0.85rem;
    inset-block-end: 0.95rem;
    z-index: 1;
    color: var(--ff-text-soft);
    font-size: var(--ff-fs-200);
    font-weight: 700;
    line-height: 1.2;
  }

  .is-media-missing::after {
    content: "Preview media";
    position: absolute;
    inset-inline-start: 0.7rem;
    inset-block-start: 0.7rem;
    z-index: 1;
    display: inline-flex;
    align-items: center;
    min-height: 1.5rem;
    padding: 0.2rem 0.5rem;
    border-radius: var(--ff-pill);
    border: 1px solid var(--ff-border-strong);
    background: rgba(255, 255, 255, 0.82);
    color: var(--ff-text-muted);
    font-size: 0.6875rem;
    font-weight: 700;
    letter-spacing: 0.01em;
  }

  .ff-root[data-theme="dark"] .is-media-missing::after {
    background: rgba(15, 23, 42, 0.78);
  }

  @keyframes ff-shimmer {
    0% { background-position: 200% 0; }
    100% { background-position: -200% 0; }
  }
}

@layer ff.controls {
  .ff-btn,
  .ff-iconbtn,
  .ff-chip,
  .ff-tab,
  .ff-nav__link,
  .ff-drawer__link,
  .ff-footer__link,
  .ff-footer__linkgrid a,
  .ff-onboardStep,
  .ff-onboardColor {
    transition:
      transform .12s ease,
      box-shadow .12s ease,
      background-color .2s ease,
      border-color .2s ease,
      color .2s ease,
      opacity .2s ease;
  }

  .ff-btn {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    gap: 0.5rem;
    min-height: 2.75rem;
    padding: 0.72rem 1rem;
    border-radius: var(--ff-r-2);
    border: 1px solid transparent;
    background: transparent;
    color: var(--ff-text);
    font-weight: 720;
    text-align: center;
    box-shadow: 0 1px 0 rgba(255, 255, 255, 0.06) inset;
  }

  .ff-btn:hover,
  .ff-iconbtn:hover,
  .ff-chip:hover,
  .ff-tab:hover {
    transform: translateY(-1px);
  }

  .ff-btn:active,
  .ff-iconbtn:active,
  .ff-chip:active,
  .ff-tab:active {
    transform: translateY(0);
  }

  .ff-btn--primary,
  .ff-tab--cta {
    background: linear-gradient(180deg, var(--ff-brand) 0%, var(--ff-brand-strong) 100%);
    color: #ffffff;
    box-shadow:
      0 1px 0 rgba(255, 255, 255, 0.16) inset,
      0 10px 24px rgba(2, 132, 199, 0.16);
  }

  .ff-btn--primary:hover,
  .ff-tab--cta:hover {
    filter: brightness(1.03);
  }

  .ff-btn--secondary {
    background: var(--ff-surface-3);
    border-color: var(--ff-border-strong);
    color: var(--ff-text);
  }

  .ff-btn--ghost,
  .ff-btn:not(.ff-btn--primary):not(.ff-btn--secondary) {
    background: transparent;
    border-color: var(--ff-border-subtle);
    color: var(--ff-text);
  }

  .ff-btn--pill,
  .ff-chip--pill,
  .ff-tab--pill {
    border-radius: var(--ff-pill);
  }

  .ff-btn--sm {
    min-height: 2.375rem;
    padding: 0.6rem 0.875rem;
    font-size: var(--ff-fs-400);
  }

  .ff-btn--lg {
    min-height: 3.25rem;
    padding: 0.9rem 1.2rem;
    font-size: 1rem;
  }

  .ff-btn[disabled],
  .ff-btn[aria-disabled="true"] {
    opacity: 0.55;
    pointer-events: none;
  }

  .ff-btn[aria-busy="true"]::after,
  .ff-btn.is-loading::after {
    content: "";
    width: 0.9rem;
    height: 0.9rem;
    border-radius: 999px;
    border: 2px solid currentColor;
    border-inline-end-color: transparent;
    animation: ff-spin .8s linear infinite;
  }

  .ff-btn__label {
    font-weight: 760;
  }

  .ff-btn__sub {
    display: none;
    font-size: var(--ff-fs-200);
    font-weight: 600;
    opacity: 0.9;
  }

  .ff-iconbtn {
    display: inline-grid;
    place-items: center;
    width: 2.6rem;
    height: 2.6rem;
    border-radius: 999px;
    border: 1px solid var(--ff-border-subtle);
    background: var(--ff-surface-3);
    color: var(--ff-text-soft);
    box-shadow: var(--ff-shadow-1);
  }

  .ff-chip {
    justify-content: center;
    min-height: 2.6rem;
    padding: 0.68rem 0.9rem;
    background: var(--ff-surface-3);
    color: var(--ff-text-soft);
    border: 1px solid var(--ff-border-subtle);
    border-radius: var(--ff-r-2);
    font-weight: 700;
  }

  .ff-chip--impact {
    flex-direction: column;
    align-items: flex-start;
    gap: 0.15rem;
    min-height: 3rem;
  }

  .ff-chip__amt {
    font-weight: 760;
    color: var(--ff-text);
  }

  .ff-chip__meta {
    font-size: var(--ff-fs-200);
    color: var(--ff-text-muted);
  }

  .ff-nav {
    display: inline-flex;
    align-items: center;
    flex-wrap: wrap;
    gap: 0.25rem;
    padding: 0.25rem;
    border-radius: var(--ff-pill);
  }

  .ff-nav__link,
  .ff-tab,
  .ff-drawer__link,
  .ff-footer__link {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    min-height: 2.25rem;
    padding: 0.48rem 0.72rem;
    border-radius: var(--ff-pill);
    color: var(--ff-text-soft);
    font-size: var(--ff-fs-300);
    font-weight: 680;
    text-decoration: none;
  }

  .ff-nav__link:hover,
  .ff-tab:hover,
  .ff-drawer__link:hover,
  .ff-footer__link:hover {
    background: var(--ff-bg-muted);
    color: var(--ff-text);
  }

  .ff-tab[aria-current="page"],
  .ff-tab[aria-selected="true"],
  .ff-nav__link[aria-current="page"] {
    background: var(--ff-brand-soft);
    color: var(--ff-brand-strong);
    border-color: rgba(14, 165, 233, 0.22);
  }

  .ff-input,
  textarea.ff-input,
  select.ff-input {
    width: 100%;
    min-height: 2.95rem;
    padding: 0.78rem 0.9rem;
    border-radius: var(--ff-r-2);
    border: 1px solid var(--ff-border-strong);
    background: var(--ff-surface-3);
    color: var(--ff-text);
    box-shadow: 0 1px 0 rgba(255, 255, 255, 0.06) inset;
  }

  textarea.ff-input {
    min-height: 7rem;
    resize: vertical;
  }

  .ff-input::placeholder {
    color: var(--ff-text-muted);
  }

  .ff-input[aria-invalid="true"] {
    border-color: var(--ff-danger);
    box-shadow: 0 0 0 3px rgba(185, 28, 28, 0.12);
  }

  .ff-disclosure {
    overflow: clip;
    border: 1px solid var(--ff-border-subtle);
    border-radius: var(--ff-r-3);
    background: var(--ff-surface-3);
  }

  .ff-disclosure__sum {
    display: flex;
    align-items: center;
    justify-content: space-between;
    gap: 1rem;
    list-style: none;
    cursor: pointer;
    padding: 0.95rem 1rem;
    user-select: none;
  }

  .ff-disclosure__sum::-webkit-details-marker {
    display: none;
  }

  .ff-disclosure__sum::after {
    content: "+";
    font-size: 1.125rem;
    font-weight: 800;
    color: var(--ff-text-muted);
  }

  .ff-disclosure[open] .ff-disclosure__sum::after {
    content: "–";
  }

  .ff-disclosure__panel {
    padding: 0 1rem 1rem;
  }

  .ff-tabs {
    position: fixed;
    inset-inline: 0;
    inset-block-end: 0;
    z-index: var(--ff-z-tabs);
    padding: 0.5rem 0.75rem calc(0.5rem + env(safe-area-inset-bottom, 0px));
    background: linear-gradient(180deg, rgba(246, 248, 251, 0) 0%, rgba(246, 248, 251, 0.9) 24%, rgba(246, 248, 251, 0.98) 100%);
    backdrop-filter: blur(12px);
    -webkit-backdrop-filter: blur(12px);
  }

  .ff-root[data-theme="dark"] .ff-tabs {
    background: linear-gradient(180deg, rgba(6, 9, 15, 0) 0%, rgba(6, 9, 15, 0.9) 24%, rgba(6, 9, 15, 0.98) 100%);
  }

  @media (prefers-color-scheme: dark) {
    .ff-root:not([data-theme="light"]) .ff-tabs {
      background: linear-gradient(180deg, rgba(6, 9, 15, 0) 0%, rgba(6, 9, 15, 0.9) 24%, rgba(6, 9, 15, 0.98) 100%);
    }
  }

  .ff-tabs__scroller {
    width: min(100%, var(--ff-container));
    margin-inline: auto;
    overflow-x: auto;
    scrollbar-width: none;
  }

  .ff-tabs__scroller::-webkit-scrollbar {
    display: none;
  }

  .ff-tabs__list {
    min-width: max-content;
    padding: 0.35rem;
    background: var(--ff-surface-2);
    border: 1px solid var(--ff-border-subtle);
    border-radius: var(--ff-pill);
    box-shadow: var(--ff-shadow-2);
  }

  .ff-backtotop {
    position: fixed;
    inset-inline-end: 1rem;
    inset-block-end: calc(var(--ff-tabs-h) + env(safe-area-inset-bottom, 0px) + 1rem);
    z-index: var(--ff-z-fab);
    display: inline-flex;
    align-items: center;
    gap: 0.45rem;
    padding: 0.7rem 0.9rem;
    border-radius: var(--ff-pill);
    background: var(--ff-surface-2);
    border: 1px solid var(--ff-border-subtle);
    box-shadow: var(--ff-shadow-2);
    color: var(--ff-text);
  }

  .ff-themeToggle[aria-pressed="true"] {
    background: var(--ff-brand-soft);
    color: var(--ff-brand-strong);
    border-color: rgba(14, 165, 233, 0.24);
  }

  .ff-onboardStep {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    min-height: 2.4rem;
    padding: 0.55rem 0.75rem;
    border-radius: var(--ff-pill);
    border: 1px solid var(--ff-border-subtle);
    background: var(--ff-surface-3);
    color: var(--ff-text-soft);
    font-size: var(--ff-fs-300);
    font-weight: 760;
    text-align: center;
  }

  .ff-onboardStep[aria-current="step"] {
    background: var(--ff-brand-soft);
    color: var(--ff-brand-strong);
    border-color: rgba(14, 165, 233, 0.24);
    box-shadow: var(--ff-shadow-1);
  }

  .ff-onboardColor {
    min-height: 3rem;
    padding: 0.35rem;
    border: 1px solid var(--ff-border-subtle);
    border-radius: var(--ff-r-2);
    background: var(--ff-surface-3);
    cursor: pointer;
  }

  .ff-onboardSwatches {
    display: inline-flex;
    align-items: center;
    gap: 0.75rem;
  }

  .ff-onboardSwatch {
    width: 3rem;
    height: 3rem;
    border-radius: 999px;
    border: 1px solid var(--ff-border-strong);
    box-shadow: var(--ff-shadow-1);
    background: var(--ff-bg-muted);
  }

  @keyframes ff-spin {
    to { transform: rotate(360deg); }
  }

  @media (min-width: 64rem) {
    .ff-btn__sub {
      display: inline;
    }

    .ff-tabs {
      display: none;
    }

    .ff-backtotop {
      inset-block-end: 1rem;
    }
  }
}

@layer ff.pages {
  .ff-chrome {
    position: sticky;
    inset-block-start: 0;
    z-index: var(--ff-z-chrome);
    padding-block: 0.5rem 0;
  }

  .ff-topbar__capsule,
  .ff-footerTray,
  .ff-progressCompact,
  .ff-trustFaqCompact {
    padding: 1rem;
    border-radius: var(--ff-r-4);
  }

  .ff-topbar__capsuleInner {
    display: grid;
    gap: 0.625rem;
  }

  .ff-topbar__mainRow {
    display: flex;
    align-items: center;
    justify-content: space-between;
    gap: 0.75rem;
    min-height: 2.9rem;
    flex-wrap: wrap;
  }

  .ff-topbar__desktop-only {
    display: none;
  }

  .ff-topbar__mobile-only {
    display: inline-flex;
  }

  .ff-topbarGoal {
    padding: 0.7rem 0.8rem;
    border-radius: var(--ff-r-3);
    border: 1px solid var(--ff-border-subtle);
    background: var(--ff-surface-3);
  }

  .ff-topbarGoal__stack {
    display: grid;
    gap: 0.55rem;
  }

  .ff-topbarGoal__numbers,
  .ff-topbarGoal__progress {
    display: flex;
    align-items: center;
    justify-content: space-between;
    gap: 0.875rem;
  }

  .ff-topbarGoal__raised,
  .ff-topbarGoal__goal,
  .ff-topbarGoal__percent {
    font-weight: 760;
    color: var(--ff-text);
  }

  .ff-scroll {
    position: relative;
    height: 3px;
    border-radius: 999px;
    background: var(--ff-bg-muted);
    overflow: hidden;
  }

  .ff-scroll > span {
    display: block;
    width: 24%;
    height: 100%;
    background: linear-gradient(90deg, var(--ff-brand), var(--ff-accent));
    border-radius: inherit;
  }

  .ff-announce__inner {
    padding: 0.7rem 0.875rem;
    border-radius: var(--ff-r-3);
    border: 1px solid var(--ff-border-subtle);
    background: var(--ff-surface-3);
  }

  .ff-announce__row {
    display: flex;
    align-items: center;
    justify-content: space-between;
    gap: 0.75rem;
    flex-wrap: wrap;
  }

  .ff-announce__badge {
    display: inline-flex;
    align-items: center;
    min-height: 1.8rem;
    padding: 0.3rem 0.65rem;
    border-radius: var(--ff-pill);
    background: var(--ff-accent-soft);
    color: var(--ff-accent-strong);
    font-size: var(--ff-fs-100);
    font-weight: 760;
    letter-spacing: 0.08em;
    text-transform: uppercase;
  }

  .ff-hero__capsule,
  .ff-heroPanel .ff-card,
  .ff-heroMedia {
    border-radius: var(--ff-r-4);
  }

  .ff-hero__capsuleInner {
    display: grid;
    gap: 0.95rem;
    padding: clamp(1rem, 2vw, 1.35rem);
  }

  .ff-heroLead {
    max-width: 58ch;
  }

  .ff-heroContext {
    gap: 0.5rem;
  }

  .ff-heroCtas {
    align-items: center;
  }

  .ff-hero__kpis {
    gap: 0.75rem;
  }

  .ff-kpiCard {
    min-height: 5.5rem;
  }

  .ff-heroPanel .ff-card {
    position: sticky;
    inset-block-start: calc(var(--ff-nav-h) + 0.75rem);
    padding: 1rem;
  }

  .ff-heroPanelHead {
    display: grid;
    gap: 0.35rem;
  }

  .ff-railcard {
    position: relative;
    overflow: hidden;
    min-height: 10.5rem;
    border-radius: var(--ff-r-3);
    border: 1px solid var(--ff-border-subtle);
    background: var(--ff-surface-3);
    box-shadow: var(--ff-shadow-1);
  }

  .ff-railcard__img {
    width: 100%;
    height: 100%;
    min-height: 10.5rem;
    object-fit: cover;
  }

  .ff-railcard__meta {
    position: absolute;
    inset-inline: 0;
    inset-block-end: 0;
    display: grid;
    gap: 0.25rem;
    padding: 0.9rem;
    background: linear-gradient(180deg, rgba(2, 6, 23, 0.04) 0%, rgba(2, 6, 23, 0.8) 100%);
    color: #ffffff;
  }

  .ff-railcard__chip {
    width: max-content;
    background: rgba(255, 255, 255, 0.16);
    border-color: rgba(255, 255, 255, 0.22);
    color: #ffffff;
  }

  .ff-teamCard,
  .ff-storyCard {
    overflow: hidden;
  }

  .ff-teamCard__inner {
    display: grid;
    gap: 0.875rem;
  }

  .ff-teamCard__media,
  .ff-storyPoster {
    position: relative;
    overflow: hidden;
    border-radius: var(--ff-r-3);
    background:
      linear-gradient(180deg, rgba(14, 165, 233, 0.05), rgba(15, 23, 42, 0.03)),
      linear-gradient(135deg, #eef4f9, #dde8f3);
    border: 1px solid var(--ff-border-subtle);
  }

  .ff-root[data-theme="dark"] .ff-teamCard__media,
  .ff-root[data-theme="dark"] .ff-storyPoster {
    background:
      linear-gradient(180deg, rgba(56, 189, 248, 0.08), rgba(2, 6, 23, 0.1)),
      linear-gradient(135deg, #0f172a, #162235);
  }

  @media (prefers-color-scheme: dark) {
    .ff-root:not([data-theme="light"]) .ff-teamCard__media,
    .ff-root:not([data-theme="light"]) .ff-storyPoster {
      background:
        linear-gradient(180deg, rgba(56, 189, 248, 0.08), rgba(2, 6, 23, 0.1)),
        linear-gradient(135deg, #0f172a, #162235);
    }
  }

  .ff-teamCard__img {
    width: 100%;
    aspect-ratio: 16 / 10;
    object-fit: cover;
    object-position: center;
  }

  .ff-storyPoster__img {
    width: 100%;
    aspect-ratio: 16 / 9;
    object-fit: cover;
  }

  .ff-storyPoster__overlay {
    position: absolute;
    inset: auto 0.75rem 0.75rem 0.75rem;
    display: inline-flex;
    align-items: center;
    gap: 0.65rem;
    width: fit-content;
    padding: 0.62rem 0.84rem;
    border-radius: var(--ff-pill);
    background: rgba(2, 6, 23, 0.74);
    color: #ffffff;
    box-shadow: var(--ff-shadow-2);
  }

  .ff-impactTier__head,
  .ff-tierHead,
  .ff-tierFoot,
  .ff-impactTier__footer,
  .ff-sponsorWallBlock,
  .ff-sponsorTiers__inner {
    display: grid;
    gap: 0.7rem;
  }

  .ff-tierTitleRow {
    display: flex;
    align-items: center;
    justify-content: space-between;
    gap: 0.75rem;
  }

  .ff-tierPrice {
    display: inline-flex;
    align-items: baseline;
    gap: 0.2rem;
  }

  .ff-tierSuffix {
    font-size: 1rem;
  }

  .ff-impactPick__chips {
    gap: 0.625rem;
  }

  .ff-sponsorWall {
    gap: 0.75rem;
  }

  .ff-sponsorWall__item {
    min-height: 4.75rem;
    display: flex;
    align-items: center;
    justify-content: center;
    padding: 0.875rem;
    text-align: center;
  }

  .ff-sponsorWall__item.ff-skeleton::before,
  .ff-sponsorWall__item.ff-skeleton::after {
    content: "";
    position: absolute;
    inset-inline-start: 0.8rem;
    border-radius: 999px;
    background: rgba(100, 116, 139, 0.16);
  }

  .ff-sponsorWall__item.ff-skeleton::before {
    inset-block-start: 1rem;
    width: 42%;
    height: 0.78rem;
  }

  .ff-sponsorWall__item.ff-skeleton::after {
    inset-block-start: 2.2rem;
    width: 64%;
    height: 0.58rem;
  }

  .ff-sponsorWallEmpty .ff-help {
    padding: 0.85rem 0.95rem;
    border: 1px dashed var(--ff-border-strong);
    border-radius: var(--ff-r-2);
    background: linear-gradient(180deg, var(--ff-surface-3), var(--ff-bg-muted));
  }

  .ff-trustSignals {
    display: grid;
    gap: 0.7rem;
  }

  .ff-trustSignal {
    padding: 0.9rem;
  }

  .ff-progressCompact__hero {
    align-items: start;
  }

  .ff-progressCompact__summary {
    padding: 1rem;
  }

  .ff-progressCompact__summary .ff-meter {
    margin-top: 0.85rem;
  }

  .ff-progressCompact__details .ff-impactTierGrid--compact {
    display: grid;
    gap: 0.75rem;
    grid-template-columns: 1fr;
    padding: 0;
  }

  .ff-progressCompact__details .ff-impactTierGrid--compact > li {
    list-style: none;
    min-height: 100%;
    display: flex;
    flex-direction: column;
    justify-content: center;
    padding: 0.9rem;
    border-radius: var(--ff-r-3);
    background: var(--ff-surface-3);
    border: 1px solid var(--ff-border-subtle);
    box-shadow: var(--ff-shadow-1);
  }

  .ff-progressCompact__use {
    margin-top: 0.85rem;
    padding: 1rem;
  }

  .ff-progressCompact__use .ff-storyList {
    margin-top: 0.75rem;
  }

  .ff-progressCompact__use .ff-row[aria-label="Suggested support levels"] {
    gap: 0.55rem;
    flex-wrap: wrap;
  }

  .ff-activityFeed {
    display: grid;
    gap: 0.625rem;
    margin: 0.9rem 0 0;
    padding: 0.875rem;
    width: 100%;
    max-width: min(100%, 42rem);
    border: 1px solid var(--ff-border-subtle);
    border-radius: var(--ff-r-3);
    background: linear-gradient(180deg, var(--ff-surface-3), var(--ff-bg-muted));
    box-shadow: var(--ff-shadow-1);
    overflow: clip;
  }

  .ff-heroHeader > .ff-activityFeed {
    margin-top: 1rem;
    margin-bottom: 0.2rem;
  }

  .ff-activityFeed__item {
    position: relative;
    display: flex;
    align-items: center;
    min-height: 2.8rem;
    padding: 0.75rem 0.875rem 0.75rem 2.1rem;
    font-size: var(--ff-fs-400);
    font-weight: 650;
    color: var(--ff-text-soft);
  }

  .ff-activityFeed__item::before {
    content: "";
    position: absolute;
    inset-inline-start: 0.8rem;
    inset-block-start: 50%;
    width: 0.55rem;
    height: 0.55rem;
    border-radius: 999px;
    transform: translateY(-50%);
    background: var(--ff-brand);
    box-shadow: 0 0 0 0.22rem var(--ff-brand-soft);
  }

  .ff-activityFeed__item:nth-child(2)::before {
    background: var(--ff-success);
    box-shadow: 0 0 0 0.22rem var(--ff-success-soft);
  }

  .ff-activityFeed__item:nth-child(3)::before {
    background: var(--ff-accent);
    box-shadow: 0 0 0 0.22rem var(--ff-accent-soft);
  }

  .ff-campaignSnapshot {
    display: grid;
    grid-template-columns: repeat(2, minmax(0, 1fr));
    gap: 0.75rem;
    margin-top: 1rem;
  }

  .ff-snapshotItem {
    padding: 0.9rem;
  }

  .ff-onboardPanel {
    display: grid;
    gap: 1rem;
    min-width: 0;
  }

  .ff-onboardSummary {
    display: grid;
    gap: 0.875rem;
    padding: 1rem;
    border-radius: var(--ff-r-3);
    border: 1px solid var(--ff-border-subtle);
    box-shadow: var(--ff-shadow-1);
  }

  .ff-onboardSummary__value {
    display: block;
    font-size: var(--ff-fs-500);
    line-height: 1.35;
    font-weight: 720;
    color: var(--ff-text);
  }

  .ff-footerTray {
    display: grid;
    gap: 0.9rem;
  }

  .ff-footer__linkgrid {
    gap: 0.5rem;
  }

  .ff-footer__linkgrid a {
    color: var(--ff-text-soft);
    text-decoration: none;
  }

  .ff-checkoutShell,
  .ff-modal__body {
    min-width: 0;
  }

  .ff-paymentMount,
  .ff-paypalMount,
  .ff-videoFrame {
    border-radius: var(--ff-r-2);
    border: 1px solid var(--ff-border-subtle);
    background: var(--ff-surface-3);
    padding: 0.75rem;
  }

  .ff-videoMount {
    aspect-ratio: 16 / 9;
    min-height: 12rem;
  }

  .ff-videoMount iframe {
    width: 100%;
    height: 100%;
  }

  .ff-toasts {
    position: fixed;
    inset-inline-end: 1rem;
    inset-block-start: 1rem;
    z-index: var(--ff-z-toast);
    display: grid;
    gap: 0.75rem;
    max-width: min(92vw, 24rem);
  }

  .ff-toasts > * {
    padding: 0.875rem 1rem;
    border-radius: var(--ff-r-2);
    background: var(--ff-surface-2);
    border: 1px solid var(--ff-border-subtle);
    box-shadow: var(--ff-shadow-2);
  }

  .ff-floatingDonate {
    position: fixed;
    inset-inline: max(0.75rem, env(safe-area-inset-left)) max(0.75rem, env(safe-area-inset-right));
    inset-block-end: calc(var(--ff-tabs-h, 4.25rem) + env(safe-area-inset-bottom, 0px) + 0.75rem);
    z-index: calc(var(--ff-z-fab, 40) - 1);
    width: auto;
    max-width: min(42rem, calc(100vw - 1.5rem));
    margin-inline: auto;
    border-radius: calc(var(--ff-pill, 999px) + 0.25rem);
    border: 1px solid var(--ff-border-subtle, rgba(15, 23, 42, 0.08));
    background: var(--ff-surface-2, rgba(255, 255, 255, 0.96));
    box-shadow: var(--ff-shadow-3, 0 12px 32px rgba(15, 23, 42, 0.12), 0 28px 88px rgba(15, 23, 42, 0.14));
    backdrop-filter: blur(var(--ff-blur, 14px));
    -webkit-backdrop-filter: blur(var(--ff-blur, 14px));
    opacity: 1;
    transform: translateY(0);
    transition:
      opacity var(--ff-t-med, 220ms ease),
      transform var(--ff-t-med, 220ms ease),
      box-shadow var(--ff-t-med, 220ms ease);
  }

  .ff-floatingDonate:hover {
    transform: translateY(-2px);
  }

  .ff-floatingDonate__inner {
    display: flex;
    align-items: center;
    justify-content: space-between;
    gap: 0.875rem;
    min-width: 0;
    padding: 0.75rem 0.8rem 0.75rem 1rem;
  }

  .ff-floatingDonate__inner > .ff-minw-0,
  .ff-floatingDonate__inner > div {
    min-width: 0;
    flex: 1 1 auto;
  }

  .ff-floatingDonate .ff-kicker {
    color: var(--ff-brand-strong, #0369a1);
  }

  .ff-floatingDonate .ff-help {
    white-space: nowrap;
    overflow: hidden;
    text-overflow: ellipsis;
  }

  .ff-floatingDonate .ff-btn {
    flex: 0 0 auto;
    min-height: 2.75rem;
    padding-inline: 1rem;
  }

  .ff-root[data-theme="dark"] .ff-floatingDonate {
    background: var(--ff-surface-2, rgba(11, 18, 32, 0.9));
    border-color: var(--ff-border-subtle, rgba(148, 163, 184, 0.16));
  }

  @keyframes ff-fadeUp {
    from {
      opacity: 0;
      transform: translateY(12px);
    }
    to {
      opacity: 1;
      transform: none;
    }
  }

  .ff-heroTitle:not([data-ff-animate]),
  .ff-heroTagline:not([data-ff-animate]),
  .ff-btn:not([data-ff-animate]) {
    animation: ff-fadeUp .6s ease-out both;
  }

  .ff-heroTagline:not([data-ff-animate]) {
    animation-delay: 50ms;
  }

  @media (min-width: 48rem) {
    .ff-topbarBrand__pill {
      display: inline-flex;
    }

    .ff-impactPick__chips {
      grid-template-columns: repeat(2, minmax(0, 1fr));
    }
  }

  @media (min-width: 64rem) {
    .ff-topbar__desktop-only {
      display: block;
    }

    .ff-topbar__mobile-only {
      display: none;
    }

    .ff-topbarGoal__stack {
      grid-template-columns: auto 1fr;
      align-items: center;
      gap: 1rem;
    }

    .ff-progressCompact__summary {
      position: sticky;
      inset-block-start: calc(var(--ff-nav-h) + 0.75rem);
    }

    .ff-progressCompact__details .ff-impactTierGrid--compact {
      grid-template-columns: repeat(3, minmax(0, 1fr));
    }

    .ff-campaignSnapshot {
      grid-template-columns: repeat(4, minmax(0, 1fr));
    }

    .ff-floatingDonate {
      inset-inline: auto 1rem;
      inset-block-end: 1rem;
      min-width: 22rem;
    }
  }

  @media (max-width: 63.99rem) {
    .ff-floatingDonate {
      display: none;
    }
  }

  @media (max-width: 47.99rem) {
    .ff-container {
      padding-inline: 1rem;
    }

    .ff-display,
    .ff-heroTitle {
      max-width: 10ch;
      font-size: clamp(2rem, 9.6vw, 3rem);
    }

    .ff-hero__capsuleInner,
    .ff-topbar__capsule,
    .ff-progressCompact,
    .ff-trustFaqCompact,
    .ff-footerTray {
      padding: 0.9rem;
    }

    .ff-teamCard__media {
      min-height: 8.5rem;
    }

    .ff-progressCompact__details .ff-impactTierGrid--compact {
      grid-template-columns: 1fr;
    }
  }
}

@layer ff.utilities {
  .ff-gap-2 { gap: var(--ff-2); }
  .ff-gap-3 { gap: var(--ff-3); }
  .ff-gap-4 { gap: var(--ff-4); }

  .ff-m-0 { margin: 0; }
  .ff-mt-0 { margin-top: 0; }
  .ff-mt-1 { margin-top: var(--ff-1); }
  .ff-mt-2 { margin-top: var(--ff-2); }
  .ff-mt-3 { margin-top: var(--ff-3); }
  .ff-mt-4 { margin-top: var(--ff-4); }

  .ff-mb-0 { margin-bottom: 0; }
  .ff-mb-1 { margin-bottom: var(--ff-1); }
  .ff-mb-2 { margin-bottom: var(--ff-2); }
  .ff-mb-3 { margin-bottom: var(--ff-3); }
  .ff-mb-4 { margin-bottom: var(--ff-4); }

  .ff-p-0 { padding: 0; }
  .ff-center { text-align: center; }
  .ff-hide-mobile { display: none !important; }
  .ff-inline { display: inline-flex; }
  .ff-underline { text-decoration: underline; text-underline-offset: 0.16em; }
  .ff-nounderline { text-decoration: none; }

  [data-ff-animate] {
    --ff-anim-distance: 18px;
    --ff-anim-duration: 560ms;
    --ff-anim-delay: 0ms;
  }

  [data-ff-animate="rise"] {
    animation: ff-rise var(--ff-anim-duration) cubic-bezier(.22, .9, .24, 1) both;
    animation-delay: var(--ff-anim-delay);
    will-change: transform, opacity;
  }

  [data-ff-animate="stagger"] > * {
    opacity: 0;
    transform: translateY(14px);
    animation: ff-rise 520ms cubic-bezier(.22, .9, .24, 1) both;
  }

  [data-ff-animate="stagger"] > *:nth-child(1) { animation-delay: 40ms; }
  [data-ff-animate="stagger"] > *:nth-child(2) { animation-delay: 90ms; }
  [data-ff-animate="stagger"] > *:nth-child(3) { animation-delay: 140ms; }
  [data-ff-animate="stagger"] > *:nth-child(4) { animation-delay: 190ms; }
  [data-ff-animate="stagger"] > *:nth-child(5) { animation-delay: 240ms; }
  [data-ff-animate="stagger"] > *:nth-child(6) { animation-delay: 290ms; }
  [data-ff-animate="stagger"] > *:nth-child(7) { animation-delay: 340ms; }
  [data-ff-animate="stagger"] > *:nth-child(8) { animation-delay: 390ms; }

  .ff-root[data-ff-webdriver="true"] .ff-shellBg,
  .ff-root[data-ff-webdriver="true"] .ff-hero__orb,
  .ff-root[data-ff-webdriver="true"] .ff-hero__gridlines,
  .ff-root[data-ff-webdriver="true"] .ff-skeleton,
  .ff-root[data-ff-webdriver="true"] .ff-modal__panel,
  .ff-root[data-ff-webdriver="true"] .ff-sheet__panel,
  .ff-root[data-ff-webdriver="true"] .ff-drawer__panel,
  .ff-root[data-ff-webdriver="true"] [data-ff-animate],
  .ff-root[data-ff-webdriver="true"] [data-ff-animate] > * {
    animation: none !important;
    transition: none !important;
    filter: none !important;
    opacity: 1 !important;
    transform: none !important;
  }

  @keyframes ff-rise {
    from {
      opacity: 0;
      transform: translateY(var(--ff-anim-distance, 18px));
    }
    to {
      opacity: 1;
      transform: translateY(0);
    }
  }

  @media (prefers-reduced-motion: reduce) {
    [data-ff-animate],
    [data-ff-animate] > * {
      animation: none !important;
      opacity: 1 !important;
      transform: none !important;
      transition: none !important;
    }

    .ff-floatingDonate {
      transition: none !important;
      transform: none !important;
    }
  }

  @media (min-width: 64rem) {
    .ff-hide-mobile {
      display: revert !important;
    }

    .ff-hide-desktop {
      display: none !important;
    }
  }
}

@layer ff.overrides {
  .ff-modal,
  .ff-sheet,
  .ff-drawer {
    position: fixed;
    inset: 0;
    z-index: var(--ff-z-overlay);
    display: none;
    opacity: 0;
    pointer-events: none;
    transition: opacity var(--ff-t-med);
  }

  .ff-modal[hidden],
  .ff-sheet[hidden],
  .ff-drawer[hidden],
  .ff-modal[data-open="false"],
  .ff-sheet[data-open="false"],
  .ff-drawer[data-open="false"],
  .ff-modal[aria-hidden="true"],
  .ff-sheet[aria-hidden="true"],
  .ff-drawer[aria-hidden="true"] {
    display: none;
    opacity: 0;
    pointer-events: none;
  }

  .ff-modal:is(:target, .is-open, [data-open="true"], [aria-hidden="false"]),
  .ff-sheet:is(:target, .is-open, [data-open="true"], [aria-hidden="false"]),
  .ff-drawer:is(:target, .is-open, [data-open="true"], [aria-hidden="false"]) {
    display: grid !important;
    opacity: 1;
    pointer-events: auto;
  }

  .ff-modal__backdrop,
  .ff-sheet__backdrop,
  .ff-drawer__backdrop {
    position: absolute;
    inset: 0;
    display: block;
    background: var(--ff-backdrop);
  }

  .ff-modal__panel,
  .ff-sheet__panel,
  .ff-drawer__panel {
    position: relative;
    z-index: 1;
    width: min(100% - 1rem, 46rem);
    margin: auto;
    opacity: 0;
    transform: translateY(1rem) scale(0.985);
    transition: transform var(--ff-t-slow), opacity var(--ff-t-med);
    max-height: min(92vh, 56rem);
    overflow: hidden;
  }

  .ff-modal:is(:target, .is-open, [data-open="true"], [aria-hidden="false"]) .ff-modal__panel,
  .ff-sheet:is(:target, .is-open, [data-open="true"], [aria-hidden="false"]) .ff-sheet__panel,
  .ff-drawer:is(:target, .is-open, [data-open="true"], [aria-hidden="false"]) .ff-drawer__panel {
    opacity: 1;
    transform: translateY(0) scale(1);
  }

  .ff-sheet--checkout {
    align-items: end;
  }

  .ff-sheet__panel--flagship {
    width: min(100% - 1rem, 54rem);
    margin-block-end: 0.5rem;
  }

  .ff-modal--video .ff-modal__panel--video {
    width: min(100% - 1rem, 72rem);
  }

  .ff-sheet__viewport,
  .ff-sheet__content,
  .ff-sheet__scroll,
  .ff-modal__body,
  .ff-drawer__body {
    min-height: 0;
  }

  .ff-sheet__scroll,
  .ff-modal__body,
  .ff-drawer__body {
    overflow: auto;
    padding: clamp(1rem, 2vw, 1.25rem);
  }

  .ff-sheet__header,
  .ff-modal__head,
  .ff-drawer__head {
    padding: 1rem 1rem 0;
  }

  .ff-sheet__footer--sticky,
  .ff-modal__foot--sticky {
    position: sticky;
    inset-block-end: 0;
    padding: 1rem;
    background: linear-gradient(180deg, rgba(255, 255, 255, 0) 0%, var(--ff-surface-2) 22%);
    backdrop-filter: blur(12px);
    -webkit-backdrop-filter: blur(12px);
    border-top: 1px solid var(--ff-border-subtle);
  }

  .ff-root[data-theme="dark"] .ff-sheet__footer--sticky,
  .ff-root[data-theme="dark"] .ff-modal__foot--sticky {
    background: linear-gradient(180deg, rgba(11, 18, 32, 0) 0%, var(--ff-surface-2) 22%);
  }

  @media (prefers-color-scheme: dark) {
    .ff-root:not([data-theme="light"]) .ff-sheet__footer--sticky,
    .ff-root:not([data-theme="light"]) .ff-modal__foot--sticky {
      background: linear-gradient(180deg, rgba(11, 18, 32, 0) 0%, var(--ff-surface-2) 22%);
    }
  }

  .ff-drawer {
    align-items: stretch;
    justify-items: end;
  }

  .ff-drawer__panel {
    width: min(100% - 0.75rem, 24rem);
    height: calc(100% - 0.75rem);
    margin: 0.375rem;
    transform: translateX(1rem);
  }

  .ff-drawer:is(:target, .is-open, [data-open="true"], [aria-hidden="false"]) .ff-drawer__panel {
    transform: translateX(0);
  }

  .ff-drawer__link {
    justify-content: flex-start;
    border-radius: var(--ff-r-2);
  }

  .ff-modal__body > .ff-stack,
  .ff-checkoutShell {
    gap: 1rem;
  }

  .ff-root.is-open,
  .ff-shell.is-open {
    isolation: isolate;
  }

  .ff-root[data-ff-overlay-open="true"] .ff-floatingDonate,
  body[data-ff-overlay-open="true"] .ff-floatingDonate,
  .ff-sheet:is(:target, .is-open, [data-open="true"], [aria-hidden="false"]) ~ .ff-floatingDonate,
  .ff-modal:is(:target, .is-open, [data-open="true"], [aria-hidden="false"]) ~ .ff-floatingDonate,
  .ff-drawer:is(:target, .is-open, [data-open="true"], [aria-hidden="false"]) ~ .ff-floatingDonate {
    opacity: 0;
    transform: translateY(12px);
    pointer-events: none;
  }

  @media (min-width: 64rem) {
    .ff-main {
      padding-block-end: 2rem;
    }
  }
}

/* ==========================================================================
   FF_AUTHORITY_POLISH_V1
   Purpose:
   - stronger section contrast separation
   - more legible microtext
   - clearer button hierarchy
   - desktop-hide floating donate / stronger mobile sticky CTA
   - denser team cards
   - stronger sponsor wall populated states
   - story visual anchor
   - footer density polish
   ========================================================================== */
@layer ff.overrides {
  :root,
  .ff-root {
    --ff-page-bg: #f3f5f9;
    --ff-surface-1: #ffffff;
    --ff-surface-2: #f7f9fc;
    --ff-surface-3: #f1f5f9;
    --ff-border-soft: rgba(15, 23, 42, 0.08);
    --ff-border-strong: rgba(15, 23, 42, 0.14);
    --ff-shadow-card: 0 10px 28px rgba(15, 23, 42, 0.06), 0 2px 8px rgba(15, 23, 42, 0.04);
    --ff-shadow-card-hover: 0 18px 36px rgba(15, 23, 42, 0.09), 0 4px 12px rgba(15, 23, 42, 0.06);
    --ff-text-1: #162033;
    --ff-text-2: #334155;
    --ff-text-3: #4f5f76;
    --ff-text-4: #66758b;
    --ff-muted-strong: #526277;
    --ff-kicker-ink: #1d4ed8;
    --ff-hairline: #d8e1ec;
    --ff-team-media-bg: linear-gradient(180deg, #eef4fa 0%, #dde7f3 100%);
  }

  html,
  body,
  .ff-body {
    background: var(--ff-page-bg);
    color: var(--ff-text-1);
  }

  .ff-section {
    padding-block: clamp(2.75rem, 4vw, 4.75rem);
  }

  .ff-card,
  .ff-glass.ff-surface,
  .ff-tierCard,
  .ff-progressCompact,
  .ff-storyCard,
  .ff-sponsorCard,
  .ff-teamCard,
  .ff-footerTray,
  .ff-trustCard,
  .ff-faqCard,
  .ff-impactCard--flagship,
  .ff-checkoutShell,
  .ff-modal__panel--flagship,
  .ff-sheet__panel--flagship {
    background: var(--ff-surface-1);
    border: 1px solid var(--ff-border-soft);
    box-shadow: var(--ff-shadow-card);
  }

  .ff-progressCompact__summary,
  .ff-progressCompact__details,
  .ff-storyCopy,
  .ff-storyMedia,
  .ff-sponsorWallBlock,
  .ff-sponsorTiers__inner,
  .ff-trustCard,
  .ff-faqCard,
  .ff-checkoutBody,
  .ff-modal__body {
    background: var(--ff-surface-2);
    border: 1px solid var(--ff-border-soft);
    border-radius: 18px;
    padding: clamp(1rem, 2vw, 1.35rem);
  }

  .ff-divider {
    border-color: var(--ff-hairline);
    opacity: 1;
  }

  /* microtext legibility */
  .ff-help,
  .ff-muted,
  .ff-brand__sub,
  .ff-footerTrust,
  .ff-footer .ff-help,
  .ff-tierList,
  .ff-storyMeta .ff-help,
  .ff-sponsorMeta .ff-help,
  .ff-topbarGoal__percent,
  .ff-topbarGoal__goal {
    color: var(--ff-text-3);
  }

  .ff-muted {
    color: var(--ff-muted-strong);
  }

  .ff-kicker,
  .ff-label,
  .ff-snapshotLabel,
  .ff-teamStat__label,
  .ff-tierPill {
    color: var(--ff-kicker-ink);
    font-weight: 800;
    letter-spacing: 0.04em;
  }

  .ff-pill--ghost,
  .ff-pill--soft {
    color: var(--ff-text-2);
  }

  /* hierarchy / rhythm */
  .ff-sectionhead {
    margin-bottom: 1.2rem;
  }

  .ff-sectionhead__text {
    max-width: 58rem;
  }

  .ff-sectionhead__actions {
    align-self: flex-start;
  }

  .ff-progressCompact__hero,
  .ff-storyGrid,
  .ff-trust-faq .ff-grid,
  .ff-impactCard--flagship,
  .ff-sponsorCard,
  .ff-footer__grid {
    gap: 1.2rem;
  }

  .ff-progressCompact__use,
  .ff-sponsorMeta,
  .ff-storyCopy .ff-row[role="group"],
  .ff-checkoutSuccess,
  .ff-modal__foot--sticky,
  .ff-sheet__footer--sticky {
    margin-top: 1rem;
  }

  /* stronger button hierarchy */
  .ff-btn--primary {
    box-shadow: 0 10px 22px rgba(14, 165, 233, 0.2), inset 0 1px 0 rgba(255, 255, 255, 0.18);
  }

  .ff-btn--secondary {
    background: #fff;
    color: var(--ff-text-1);
    border: 1px solid rgba(14, 165, 233, 0.22);
  }

  .ff-btn--ghost,
  .ff-iconbtn,
  .ff-tab:not(.ff-tab--cta) {
    background: rgba(255, 255, 255, 0.76);
    border: 1px solid var(--ff-border-soft);
  }

  /* floating donate chip: desktop hide, mobile keep */
  @media (min-width: 961px) {
    .ff-floatingDonate[data-ff-floating-donate] {
      opacity: 0 !important;
      visibility: hidden !important;
      pointer-events: none !important;
      transform: translate3d(12px, 0, 0) !important;
    }
  }

  @media (max-width: 960px) {
    .ff-floatingDonate[data-ff-floating-donate] {
      position: fixed;
      left: max(12px, env(safe-area-inset-left));
      right: max(12px, env(safe-area-inset-right));
      bottom: calc(12px + env(safe-area-inset-bottom));
      z-index: 48;
      border-radius: 18px;
      box-shadow: 0 16px 36px rgba(15, 23, 42, 0.18);
    }

    .ff-floatingDonate__inner {
      gap: 0.75rem;
    }
  }

  /* team cards: denser + more real */
  .ff-teamGrid {
    gap: 1rem;
  }

  .ff-teamCard {
    overflow: hidden;
    transition: transform 0.18s ease, box-shadow 0.18s ease, border-color 0.18s ease;
  }

  .ff-teamCard:hover {
    transform: translateY(-2px);
    box-shadow: var(--ff-shadow-card-hover);
    border-color: var(--ff-border-strong);
  }

  .ff-teamCard__inner {
    display: grid;
    gap: 0.9rem;
  }

  .ff-teamCard__head {
    align-items: start;
  }

  .ff-teamCard__meta {
    min-height: 2.6em;
  }

  .ff-teamCard__media {
    position: relative;
    overflow: hidden;
    border-radius: 16px;
    aspect-ratio: 16 / 10;
    background: var(--ff-team-media-bg);
    border: 1px solid rgba(148, 163, 184, 0.2);
  }

  .ff-teamCard__media::after {
    content: "";
    position: absolute;
    inset: auto 0 0 0;
    height: 44%;
    background: linear-gradient(180deg, rgba(15, 23, 42, 0) 0%, rgba(15, 23, 42, 0.18) 100%);
    pointer-events: none;
  }

  .ff-teamCard__img {
    width: 100%;
    height: 100%;
    object-fit: cover;
    display: block;
  }

  .ff-teamCard__stats {
    display: grid;
    grid-template-columns: repeat(2, minmax(0, 1fr));
    gap: 0.65rem;
  }

  .ff-teamStat {
    display: grid;
    gap: 0.18rem;
    padding: 0.65rem 0.75rem;
    border-radius: 14px;
    background: var(--ff-surface-3);
    border: 1px solid var(--ff-border-soft);
  }

  .ff-teamStat__label {
    font-size: 0.72rem;
    text-transform: uppercase;
  }

  .ff-teamStat__value {
    color: var(--ff-text-1);
    font-size: 0.98rem;
    line-height: 1.15;
  }

  .ff-teamCard__foot {
    padding-top: 0.25rem;
    border-top: 1px solid var(--ff-hairline);
  }

  .ff-teamCard.is-featured {
    border-color: rgba(14, 165, 233, 0.22);
    box-shadow: 0 12px 28px rgba(14, 165, 233, 0.08), 0 4px 12px rgba(15, 23, 42, 0.05);
  }

  /* sponsor wall authority */
  .ff-sponsorWall {
    gap: 0.875rem;
    align-items: stretch;
  }

  .ff-sponsorWall__item {
    min-height: 112px;
    border-radius: 16px;
    border: 1px solid rgba(15, 23, 42, 0.08);
    background: linear-gradient(180deg, #fff 0%, #f8fbff 100%);
    box-shadow: 0 6px 18px rgba(15, 23, 42, 0.04);
  }

  .ff-sponsorWall__item .ff-card {
    height: 100%;
    padding: 1rem;
    border: 0;
    box-shadow: none;
    background: transparent;
  }

  .ff-sponsorWall__item strong {
    font-size: 1rem;
    line-height: 1.25;
    color: var(--ff-text-1);
  }

  .ff-sponsorWall__item .ff-help,
  .ff-sponsorWall__item .ff-muted {
    color: var(--ff-text-3);
  }

  /* story anchor */
  .ff-storyPoster {
    display: block;
    overflow: hidden;
    border-radius: 18px;
    border: 1px solid rgba(15, 23, 42, 0.1);
    box-shadow: 0 16px 34px rgba(15, 23, 42, 0.1);
    background: #dfe8f3;
  }

  .ff-storyPoster__img {
    width: 100%;
    aspect-ratio: 16 / 10;
    object-fit: cover;
    display: block;
  }

  .ff-proofMini {
    border-radius: 14px;
    border: 1px solid rgba(14, 165, 233, 0.12);
    background: linear-gradient(180deg, rgba(255,255,255,0.88) 0%, rgba(247,250,253,0.98) 100%);
    padding: 0.9rem 1rem;
  }

  .ff-quote {
    border-left: 3px solid rgba(14, 165, 233, 0.45);
    background: linear-gradient(180deg, rgba(255,255,255,0.86) 0%, rgba(247,250,253,0.96) 100%);
    border-radius: 14px;
    padding: 0.9rem 1rem;
  }

  /* footer density polish */
  .ff-footerTray {
    border-radius: 20px;
  }

  .ff-footer__link,
  .ff-footer .ff-help {
    color: var(--ff-text-3);
  }

  .ff-footer__link:hover {
    color: var(--ff-text-1);
  }

  /* checkout polish */
  .ff-checkoutHead,
  .ff-modal__head--flagship {
    padding-bottom: 0.25rem;
    border-bottom: 1px solid var(--ff-hairline);
  }

  .ff-paymentMount,
  .ff-paypalMount {
    border-radius: 16px;
    background: #fff;
    border: 1px solid var(--ff-border-soft);
    padding: 0.85rem;
  }

  /* dark mode parity */
  .ff-root[data-theme="dark"] .ff-card,
  .ff-root[data-theme="dark"] .ff-glass.ff-surface,
  .ff-root[data-theme="dark"] .ff-tierCard,
  .ff-root[data-theme="dark"] .ff-progressCompact,
  .ff-root[data-theme="dark"] .ff-storyCard,
  .ff-root[data-theme="dark"] .ff-sponsorCard,
  .ff-root[data-theme="dark"] .ff-teamCard,
  .ff-root[data-theme="dark"] .ff-footerTray,
  .ff-root[data-theme="dark"] .ff-trustCard,
  .ff-root[data-theme="dark"] .ff-faqCard,
  .ff-root[data-theme="dark"] .ff-impactCard--flagship,
  .ff-root[data-theme="dark"] .ff-checkoutShell,
  .ff-root[data-theme="dark"] .ff-modal__panel--flagship,
  .ff-root[data-theme="dark"] .ff-sheet__panel--flagship {
    background: rgba(10, 16, 28, 0.9);
    border-color: rgba(148, 163, 184, 0.12);
    box-shadow: 0 16px 34px rgba(0, 0, 0, 0.34);
  }

  .ff-root[data-theme="dark"] .ff-progressCompact__summary,
  .ff-root[data-theme="dark"] .ff-progressCompact__details,
  .ff-root[data-theme="dark"] .ff-storyCopy,
  .ff-root[data-theme="dark"] .ff-storyMedia,
  .ff-root[data-theme="dark"] .ff-sponsorWallBlock,
  .ff-root[data-theme="dark"] .ff-sponsorTiers__inner,
  .ff-root[data-theme="dark"] .ff-checkoutBody,
  .ff-root[data-theme="dark"] .ff-modal__body,
  .ff-root[data-theme="dark"] .ff-teamStat,
  .ff-root[data-theme="dark"] .ff-proofMini,
  .ff-root[data-theme="dark"] .ff-paymentMount,
  .ff-root[data-theme="dark"] .ff-paypalMount {
    background: rgba(255, 255, 255, 0.03);
    border-color: rgba(148, 163, 184, 0.1);
  }
}
/* ==========================================================================
   /FF_AUTHORITY_POLISH_V1
   ========================================================================== */

/* ==========================================================================
   FF_AUTHORITY_POLISH_V2
   - hard mobile floating donate visibility
   - true desktop suppression
   ========================================================================== */
@layer ff.overrides {
  @media (min-width: 961px) {
    .ff-floatingDonate[data-ff-floating-donate] {
      display: none !important;
      opacity: 0 !important;
      visibility: hidden !important;
      pointer-events: none !important;
    }
  }

  @media (max-width: 960px) {
    .ff-floatingDonate[data-ff-floating-donate] {
      display: block !important;
      opacity: 1 !important;
      visibility: visible !important;
      pointer-events: auto !important;
      transform: none !important;
      position: fixed !important;
      left: max(12px, env(safe-area-inset-left)) !important;
      right: max(12px, env(safe-area-inset-right)) !important;
      bottom: calc(12px + env(safe-area-inset-bottom)) !important;
      z-index: 60 !important;
      border-radius: 18px !important;
      box-shadow: 0 18px 36px rgba(15, 23, 42, 0.18) !important;
    }

    .ff-floatingDonate[data-ff-floating-donate] .ff-floatingDonate__inner {
      display: flex !important;
      align-items: center !important;
      justify-content: space-between !important;
      gap: 0.75rem !important;
    }
  }
}
/* ==========================================================================
   /FF_AUTHORITY_POLISH_V2
   ========================================================================== */
