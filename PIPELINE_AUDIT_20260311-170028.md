# FutureFunded Pipeline Audit — 20260311-170028

## PATCH NOW
- app/extensions.py
  - align SOCKET_ASYNC_MODE vs SOCKETIO_ASYNC_MODE
  - keep one CORS init path
  - align Socket.IO runtime strategy across app + Docker + gunicorn

- app/blueprints/main.py
  - remove duplicate module content if present
  - remove inline DB team loading from home()
  - reuse _load_db_teams() / _apply_db_teams_override()
  - rebuild ff_config / ff_cfg_hash / ETag after final teams are resolved
  - normalize CampaignGoal money units
  - remove duplicated JSON-LD cleaning blocks
  - remove any pasted shell / JSON / lockfile / README contamination

- app/blueprints/admin.py
  - protect example delete/restore API routes with auth/admin checks
  - tighten prod-safe admin guard behavior
  - verify CSRF coverage on admin POST routes

- run.py
  - decide if this is WSGI-only or CLI launcher
  - align with Docker/dev scripts

- Dockerfile
  - align runtime command with actual server entry
  - align async mode env variable name
  - align runtime strategy with app/extensions.py

- gunicorn.conf.py
  - align bind port with Docker/runtime
  - keep one worker strategy story

## KEEP + PATCH LATER
- app/turnkey.py
  - keep tenant bootstrap
  - integrate with g.team / home context later
  - add cache invalidation path later

- app/static/manifest.webmanifest
  - verify icon files exist
  - verify #checkout / #sponsors / #progress anchors exist

- scripts/start_dev_env.sh
  - keep as local helper
  - make cloudflared opt-in
  - stop using prod-ish tunnel name by default
  - align Flask launcher with real entrypoint

## REMOVE / REPLACE
- README.md
  - replace if still default CRA README

- yarn.lock
  - remove if npm is canonical package manager

## REMOVE FROM PROD PIPELINE
- app/blueprints/main.py :: /_diag/emit
  - keep dev-only, exclude from prod assumptions/tests

## HARD RULE
- no shell output in source files
- no duplicated module bodies
- no lockfile / README / manifest text pasted into Python modules
