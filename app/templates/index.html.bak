<!doctype html>
{# ============================================================
FutureFunded • Flagship Enterprise Index — DROP-IN REPLACEMENT

• Pure HTML5 + semantic structure
• Flask / Jinja compatible
• Uses ff.css + ff-app.js (no frameworks)
• CSP-safe (no inline JS except JSON data scripts w/ nonce)
============================================================ #}

{# ----------------------------
Version + environment knobs
---------------------------- #}
{% set FF_FLAGSHIP_VERSION = (flagship_version|default('17.0.0')|trim) %}
{% set FF_BUILD_ID         = (build_id|default('flagship-enterprise')|trim) %}
{% set FF_ASSET_V          = (asset_version|default(release_id|default(FF_BUILD_ID))|trim) %}
{% set FF_CSS_V            = (ff_css_version|default(FF_ASSET_V)|trim) %}
{% set ff_env              = (ff_env|default(ENV|default('production'))|trim|lower) %}

{# UI knobs #}
{% set ui_theme_final = (ui_theme|default('graphite-aurora')|trim) %}
{% set ui_skin_final  = (ui_skin|default('nike')|trim) %}
{% set ui_stack_final = (ff_ui|default('shopify')|trim) %}
{% set theme_mode     = (theme_mode|default('dark')|trim) %}

{# Canonical #}
{% set canonical = (canonical_url|default('')|trim) %}
{% if not canonical and request is defined and request.base_url is defined %}
  {% set canonical = (request.base_url|trim) %}
{% endif %}

{# SEO defaults #}
{% set page_title_final = (page_title|default('FutureFunded • Fundraiser')|trim) %}
{% set page_desc_final  = (page_description|default('Fuel the season. Fund the future.')|trim) %}
{% set org_name_final   = (org_name|default('FutureFunded')|trim) %}

{# Payments #}
{% set stripe_pk                = (stripe_pk|default(STRIPE_PUBLISHABLE_KEY|default(''))|trim) %}
{% set paypal_id                = (paypal_client_id|default('')|trim) %}
{% set stripe_intent_endpoint   = (stripe_intent_endpoint|default('/payments/stripe/intent')|trim) %}
{% set payments_config_endpoint = (payments_config_endpoint|default('/payments/config')|trim) %}
{% set payments_health_endpoint = (payments_health_endpoint|default('/payments/health')|trim) %}
{% set paypal_currency          = (paypal_currency|default('USD')|trim) %}
{% set paypal_intent            = (paypal_intent|default('capture')|trim) %}
{% set paypal_create_endpoint   = (paypal_create_endpoint|default('/payments/paypal/order')|trim) %}
{% set paypal_capture_endpoint  = (paypal_capture_endpoint|default('/payments/paypal/capture')|trim) %}
{% set qr_ep                    = (qr_endpoint|default('https://api.qrserver.com/v1/create-qr-code/')|trim) %}

{# Org / campaign defaults (IMPORTANT: keep these plain strings, NOT markdown) #}
{% set _whitelabel     = WHITELABEL_MODE|default(false) %}
{% set _ff_home        = (ff_home|default('https://getfuturefunded.com', true)|trim) %}
{% set _ff_name        = (ff_name|default('FutureFunded', true)|trim) %}
{% set _ff_logo        = (ff_logo|default(url_for('static', filename='images/futurefunded-mark.svg'), true)|trim) %}
{% set _support_email  = (support_email|default('support@getfuturefunded.com', true)|trim) %}

{% set _org_id         = (org_id|default('demo_org', true)|trim) %}
{% set _org_name       = (org_name|default('Connect ATX Elite', true)|trim) %}
{% set _org_location   = (org_location|default('Austin, TX', true)|trim) %}
{% set _org_logo       = (org_logo|default(url_for('static', filename='images/logo.webp'), true)|trim) %}

{% set _campaign_id    = (campaign_id|default('season', true)|trim) %}
{% set _campaign_name  = (campaign_name|default('Season Fund', true)|trim) %}
{% set _announce       = (announcement_text|default('', true)|trim) %}

{% set _goal_amount    = goal_amount|default(25000) %}
{% set _raised_amount  = raised_amount|default(0) %}
{% set _deadline_iso   = (deadline_iso|default('')|trim) %}
{% set _match          = match|default({'enabled': false}) %}

{% set _policy_blurb = policy_blurb|default(
  'Payments are processed securely. Receipts are emailed after successful payment. If you need help with a receipt or a correction, contact campaign support.',
  true
)|trim %}

{% set _governance_blurb = governance_blurb|default(
  'This fundraiser is managed by the program leadership. Donations are used for verified program expenses such as tournaments, travel, facilities, uniforms, training, and scholarship support when available.',
  true
)|trim %}

{% set _privacy_blurb = privacy_blurb|default(
  'We only collect what is needed to process your donation and deliver receipts. Donor information is treated with care and is not sold.',
  true
)|trim %}

{# Share #}
{% set _share_url = (canonical if canonical else (request.base_url if request is defined and request.base_url is defined else ''))|trim %}
{% set _share_msg = 'Support ' ~ _org_name ~ ' this season. Donate here: ' ~ _share_url %}
{% set _proof_msg_donor   = _share_msg %}
{% set _proof_msg_sponsor = 'We’re raising funds for ' ~ _org_name ~ '. Sponsorship support makes a measurable difference. Sponsor here: ' ~ _share_url %}

<html
  lang="en"
  data-theme="{{ theme_mode|e }}"
  data-ff-build="{{ FF_BUILD_ID|e }}"
  data-ff-version="{{ FF_FLAGSHIP_VERSION|e }}"
  data-ff-ui="{{ ui_stack_final|e }}"
  data-ff-skin="{{ ui_skin_final|e }}"
  data-ff-theme="{{ ui_theme_final|e }}"
  data-ff-env="{{ ff_env|e }}"
>
<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width,initial-scale=1,viewport-fit=cover,maximum-scale=5" />
  <meta name="format-detection" content="telephone=no,date=no,address=no,email=no" />
  <meta name="referrer" content="strict-origin-when-cross-origin" />
  <meta name="color-scheme" content="dark light" />
  <meta name="permissions-policy" content="camera=(), microphone=(), geolocation=()" />
  <meta name="generator" content="FutureFunded Flagship v{{ FF_FLAGSHIP_VERSION|e }}" />

  {% if ff_env != 'production' %}
    <meta name="robots" content="noindex,nofollow" />
  {% else %}
    <meta name="robots" content="index,follow,max-image-preview:large" />
  {% endif %}

  <title>{{ page_title_final|e }}</title>
  <meta name="description" content="{{ page_desc_final|e }}" />

  {% if canonical %}
    <link rel="canonical" href="{{ canonical|e }}" />
    <link rel="alternate" hreflang="en" href="{{ canonical|e }}" />
    <link rel="alternate" hreflang="x-default" href="{{ canonical|e }}" />
  {% endif %}

  {# Open Graph / Social #}
  {% set og_title_final = (og_title|default(page_title_final)|trim) %}
  {% set og_desc_final  = (og_description|default(page_desc_final)|trim) %}
  {% set og_img_webp    = og_image|default(url_for('static', filename='images/og.webp', _external=True)) %}
  {% set og_img_jpg     = og_image_jpg|default(url_for('static', filename='images/og.jpg', _external=True)) %}
  {% set og_alt         = (og_image_alt|default('Fundraiser preview')|trim) %}
  {% set og_w           = (og_image_width|default(1200)) %}
  {% set og_h           = (og_image_height|default(630)) %}

  <meta property="og:type" content="website" />
  {% if canonical %}<meta property="og:url" content="{{ canonical|e }}" />{% endif %}
  <meta property="og:site_name" content="{{ org_name_final|e }}" />
  <meta property="og:locale" content="en_US" />
  <meta property="og:title" content="{{ og_title_final|e }}" />
  <meta property="og:description" content="{{ og_desc_final|e }}" />

  <meta property="og:image" content="{{ og_img_webp|e }}" />
  <meta property="og:image:secure_url" content="{{ og_img_webp|e }}" />
  <meta property="og:image:type" content="image/webp" />
  <meta property="og:image:width" content="{{ og_w }}" />
  <meta property="og:image:height" content="{{ og_h }}" />
  <meta property="og:image:alt" content="{{ og_alt|e }}" />

  <meta property="og:image" content="{{ og_img_jpg|e }}" />
  <meta property="og:image:secure_url" content="{{ og_img_jpg|e }}" />
  <meta property="og:image:type" content="image/jpeg" />
  <meta property="og:image:width" content="{{ og_w }}" />
  <meta property="og:image:height" content="{{ og_h }}" />
  <meta property="og:image:alt" content="{{ og_alt|e }}" />

  {% if page_modified_iso|default('') %}
    <meta property="og:updated_time" content="{{ page_modified_iso|trim|e }}" />
  {% endif %}

  <meta name="twitter:card" content="summary_large_image" />
  {% if canonical %}<meta name="twitter:url" content="{{ canonical|e }}" />{% endif %}
  <meta name="twitter:title" content="{{ og_title_final|e }}" />
  <meta name="twitter:description" content="{{ og_desc_final|e }}" />
  <meta name="twitter:image" content="{{ og_img_webp|e }}" />
  <meta name="twitter:image:alt" content="{{ og_alt|e }}" />
  {% if twitter_site|default('') %}<meta name="twitter:site" content="{{ twitter_site|trim|e }}" />{% endif %}
  {% if twitter_creator|default('') %}<meta name="twitter:creator" content="{{ twitter_creator|trim|e }}" />{% endif %}

  {# PWA / Brand #}
  <meta name="theme-color" media="(prefers-color-scheme: light)" content="{{ theme_color_light|default(theme_color|default('#f97316'))|trim|e }}" />
  <meta name="theme-color" media="(prefers-color-scheme: dark)" content="{{ theme_color_dark|default('#0b0f17')|trim|e }}" />
  <meta name="application-name" content="{{ org_name_final|e }}" />
  <meta name="apple-mobile-web-app-capable" content="yes" />
  <meta name="mobile-web-app-capable" content="yes" />
  <meta name="apple-mobile-web-app-title" content="{{ org_name_final|e }}" />
  <meta name="apple-mobile-web-app-status-bar-style" content="black-translucent" />
  <meta name="msapplication-TileColor" content="{{ theme_color|default('#f97316')|trim|e }}" />
  <meta name="msapplication-config" content="{{ url_for('static', filename='browserconfig.xml')|e }}" />

  <link rel="icon" href="{{ url_for('static', filename='images/favicon.ico')|e }}" sizes="any" />
  <link rel="icon" type="image/png" href="{{ url_for('static', filename='images/icon-32.png')|e }}" sizes="32x32" />
  <link rel="apple-touch-icon" href="{{ url_for('static', filename='images/apple-touch-icon.png')|e }}" />
  <link rel="manifest" href="{{ url_for('static', filename='manifest.webmanifest')|e }}" />
  <link rel="mask-icon" href="{{ url_for('static', filename='images/safari-pinned-tab.svg')|e }}" color="{{ theme_color|default('#f97316')|trim|e }}" />

  {% if csrf_token is defined %}
    <meta name="csrf-token" content="{{ csrf_token()|e }}" />
  {% endif %}

  {# Runtime meta (consumed by ff-app.js) #}
  <meta name="ff-env" content="{{ ff_env|e }}" />
  <meta name="ff-build" content="{{ FF_BUILD_ID|e }}" />
  <meta name="ff-release" content="{{ FF_ASSET_V|e }}" />

  <meta name="ff-stripe-pk" content="{{ stripe_pk|e }}" />
  <meta name="ff-stripe-intent-endpoint" content="{{ stripe_intent_endpoint|e }}" />
  <meta name="ff-payments-config-endpoint" content="{{ payments_config_endpoint|e }}" />
  <meta name="ff-payments-health-endpoint" content="{{ payments_health_endpoint|e }}" />

  <meta name="ff-paypal-client-id" content="{{ paypal_id|e }}" />
  <meta name="ff-paypal-currency" content="{{ paypal_currency|e }}" />
  <meta name="ff-paypal-intent" content="{{ paypal_intent|e }}" />
  <meta name="ff-paypal-create-endpoint" content="{{ paypal_create_endpoint|e }}" />
  <meta name="ff-paypal-capture-endpoint" content="{{ paypal_capture_endpoint|e }}" />

  <meta name="ff-qr-endpoint" content="{{ qr_ep|e }}" />

  <meta name="ff-payments-payload-shape" content="{{ payments_payload_shape|default('nested_donor_v1')|trim|e }}" />
  <meta name="ff-payments-extra-json" content="{{ payments_extra_json|default('')|trim|e }}" />
  <meta name="ff-cover-fees-exact" content="{{ 'true' if (COVER_FEES_EXACT|default(false)) else 'false' }}" />
  <meta name="ff-require-email" content="{{ 'true' if (REQUIRE_EMAIL|default(true)) else 'false' }}" />

  <meta name="ff-attribution-enabled" content="{{ 'true' if (ATTRIBUTION_ENABLED|default(true)) else 'false' }}" />
  <meta name="ff-attribution-ttl-days" content="{{ ATTRIBUTION_TTL_DAYS|default(30) }}" />
  <meta name="ff-receipts-enabled" content="{{ 'true' if (RECEIPTS_ENABLED|default(true)) else 'false' }}" />
  <meta name="ff-receipts-endpoint" content="{{ RECEIPTS_ENDPOINT|default('/payments/receipt')|trim|e }}" />

  <meta name="ff-botguard-enabled" content="{{ 'true' if (BOTGUARD_ENABLED|default(false)) else 'false' }}" />
  <meta name="ff-turnstile-sitekey" content="{{ TURNSTILE_SITEKEY|default('')|trim|e }}" />

  <meta name="ff-sentry-dsn" content="{{ SENTRY_DSN|default('')|trim|e }}" />
  <meta name="ff-sentry-sample-rate" content="{{ SENTRY_SAMPLE_RATE|default('0.1')|trim|e }}" />
  <meta name="ff-shortlink-endpoint" content="{{ SHORTLINK_ENDPOINT|default('/links/shorten')|trim|e }}" />

  <meta name="ff-activity-endpoint" content="{{ activity_endpoint|default('/activity/recent')|trim|e }}" />
  <meta name="ff-haptics" content="{{ 'true' if (HAPTICS_ENABLED|default(true)) else 'false' }}" />
  <meta name="ff-milestones" content="{{ 'true' if (MILESTONES_ENABLED|default(true)) else 'false' }}" />
  <meta name="ff-milestone-thresholds" content="{{ milestone_thresholds|default('25,50,75,100')|trim|e }}" />
  <meta name="ff-supporter-wall" content="{{ 'true' if (SUPPORTER_WALL_ENABLED|default(true)) else 'false' }}" />
  <meta name="ff-supporter-wall-max" content="{{ supporter_wall_max|default(18) }}" />
  <meta name="ff-supporter-wall-ttl-min" content="{{ supporter_wall_ttl_min|default(180) }}" />

  {# Performance hints #}
  {% if stripe_pk %}
    <link rel="preconnect" href="https://js.stripe.com" crossorigin />
    <link rel="preconnect" href="https://m.stripe.network" crossorigin />
    <link rel="dns-prefetch" href="https://js.stripe.com" />
    <link rel="dns-prefetch" href="https://m.stripe.network" />
  {% endif %}

  {% if paypal_id %}
    <link rel="preconnect" href="https://www.paypal.com" crossorigin />
    <link rel="preconnect" href="https://www.paypalobjects.com" crossorigin />
    <link rel="dns-prefetch" href="https://www.paypal.com" />
    <link rel="dns-prefetch" href="https://www.paypalobjects.com" />
  {% endif %}

  {% if qr_ep and 'qrserver.com' in qr_ep %}
    <link rel="preconnect" href="https://api.qrserver.com" crossorigin />
    <link rel="dns-prefetch" href="https://api.qrserver.com" />
  {% endif %}

  {% if hero_preload_src|default('') %}
    <link rel="preload" as="image" href="{{ hero_preload_src|trim|e }}" fetchpriority="high" />
  {% endif %}

  {# CSS #}
  {% set css_href = (url_for('static', filename='css/ff.css') ~ '?v=' ~ FF_CSS_V) %}
  <link rel="preload" as="style" href="{{ css_href|e }}" />
  <link rel="stylesheet" href="{{ css_href|e }}" />

  {# Structured data (VALID JSON-LD) #}
  {% set jsonld_default = {
    "@context": "https://schema.org",
    "@graph": [
      {
        "@type": "Organization",
        "name": _org_name,
        "url": canonical,
        "logo": url_for("static", filename="images/icon-512.png", _external=True)
      },
      {
        "@type": "WebPage",
        "name": page_title_final,
        "url": canonical,
        "description": page_desc_final,
        "primaryImageOfPage": {"@type":"ImageObject","url": og_img_webp}
      }
    ]
  } %}
  {% set jsonld_obj = jsonld if (jsonld is defined and jsonld) else jsonld_default %}
  <script type="application/ld+json"{% if csp_nonce|default('') %} nonce="{{ csp_nonce|e }}"{% endif %}>{{ jsonld_obj|tojson }}</script>

  {# ----------------------------
  ffConfig (JSON only) — SINGLE SOURCE OF TRUTH
  Read by ff-app.js at boot
  ---------------------------- #}

  {% set ffConfig_default = {
    "meta": {
      "build": FF_BUILD_ID,
      "version": FF_FLAGSHIP_VERSION,
      "release": FF_ASSET_V,
      "env": ff_env
    },
    "org": {
      "id": _org_id,
      "name": _org_name,
      "location": _org_location,
      "meta": "Youth Basketball • " ~ _org_location,
      "logoSrc": _org_logo,
      "supportEmail": _support_email,
      "homeUrl": _ff_home,
      "whitelabel": _whitelabel,

      "seasonPill": _campaign_name,
      "sportPill": "Youth program",
      "heroAccentLine": "Help more kids stay on the floor.",
      "footerTagline": "Fuel the season. Fund the future."
    },
    "fundraiser": {
      "id": _campaign_id,
      "name": _campaign_name,
      "goalAmount": _goal_amount,
      "raisedAmount": _raised_amount,
      "deadlineISO": _deadline_iso,
      "match": _match,

      "currency": "USD",
      "locale": "en-US",
      "timezone": "America/Chicago"
    },
    "ui": {
      "theme": ui_theme_final,
      "skin": ui_skin_final,
      "stack": ui_stack_final,
      "announcement": {
        "enabled": (_announce|length > 0),
        "text": _announce
      }
    },
    "payments": {
      "stripePublishableKey": stripe_pk,
      "stripeIntentEndpoint": stripe_intent_endpoint,
      "paymentsConfigEndpoint": payments_config_endpoint,
      "paymentsHealthEndpoint": payments_health_endpoint,

      "paypalClientId": paypal_id,
      "paypalCurrency": paypal_currency,
      "paypalIntent": paypal_intent,
      "paypalCreateEndpoint": paypal_create_endpoint,
      "paypalCaptureEndpoint": paypal_capture_endpoint,

      "qrEndpoint": qr_ep,
      "payloadShape": payments_payload_shape|default("nested_donor_v1"),
      "extraJson": payments_extra_json|default(""),
      "coverFeesExact": (COVER_FEES_EXACT|default(false)) and true or false,
      "requireEmail": (REQUIRE_EMAIL|default(true)) and true or false
    },
    "features": {
      "attributionEnabled": (ATTRIBUTION_ENABLED|default(true)) and true or false,
      "attributionTtlDays": ATTRIBUTION_TTL_DAYS|default(30),
      "receiptsEnabled": (RECEIPTS_ENABLED|default(true)) and true or false,
      "receiptsEndpoint": RECEIPTS_ENDPOINT|default("/payments/receipt"),

      "botguardEnabled": (BOTGUARD_ENABLED|default(false)) and true or false,
      "turnstileSitekey": TURNSTILE_SITEKEY|default(""),

      "haptics": (HAPTICS_ENABLED|default(true)) and true or false,
      "milestones": (MILESTONES_ENABLED|default(true)) and true or false,
      "milestoneThresholds": milestone_thresholds|default("25,50,75,100"),

      "supporterWall": (SUPPORTER_WALL_ENABLED|default(true)) and true or false,
      "supporterWallMax": supporter_wall_max|default(18),
      "supporterWallTtlMin": supporter_wall_ttl_min|default(180)
    },

    "share": {
      "shareUrl": _share_url,
      "shareMsg": _share_msg,
      "proofMsgDonor": _proof_msg_donor,
      "proofMsgSponsor": _proof_msg_sponsor
    },

    "observability": {
      "sentryDsn": SENTRY_DSN|default(""),
      "sentrySampleRate": SENTRY_SAMPLE_RATE|default("0.1"),
      "shortlinkEndpoint": SHORTLINK_ENDPOINT|default("/links/shorten")
    },

    "premium": {
      "hooks": {
        "sponsorUpgrade": true,
        "matchingStates": true,
        "vipRecognition": true,
        "endOfCampaignUrgency": true,
        "orgThemingVars": true,
        "multiTenantReady": true
      }
    },

    "data": {
      "teams": teams|default([]),
      "gallery": gallery|default({"enabled": false, "items": []}),
      "sponsors": sponsors|default({"enabled": true, "items": []}),
      "donations": donations|default({"enabled": true, "items": []})
    },

    "compat": {
      "teams": teams|default([]),
      "gallery": gallery|default({"enabled": false, "items": []})
    }
  } %}

  {% set ffConfig_obj = ffConfig if (ffConfig is defined and ffConfig) else ffConfig_default %}

  <script
    id="ffConfig"
    type="application/json"
    data-ff-config
    {% if csp_nonce|default('') %}nonce="{{ csp_nonce|e }}"{% endif %}
  >{{ ffConfig_obj|tojson }}</script>

  {# App loader(s) #}
  {% set app_href = (url_for('static', filename='js/ff-app.js') ~ '?v=' ~ FF_ASSET_V) %}
  <link rel="preload" as="script" href="{{ app_href|e }}" />
  <script src="{{ app_href|e }}" defer{% if csp_nonce|default('') %} nonce="{{ csp_nonce|e }}"{% endif %}></script>

  {% set premium_enabled = PREMIUM_MODE|default(true) %}
  {% if premium_enabled %}
    {% set premium_href = (url_for('static', filename='js/ff-premium.js') ~ '?v=' ~ FF_ASSET_V) %}
    <link rel="preload" as="script" href="{{ premium_href|e }}" />
    <script src="{{ premium_href|e }}" defer{% if csp_nonce|default('') %} nonce="{{ csp_nonce|e }}"{% endif %}></script>
  {% endif %}

  {% if analytics_id|default('') %}
    <meta name="ff-analytics-id" content="{{ analytics_id|trim|e }}" />
  {% endif %}

</head>

<body class="ff-body ff-body--flagship" data-ff-body="enterprise">
  <!-- Skip links -->
  <nav class="ff-skiplinks" aria-label="Skip links">
    <a href="#main">Skip to content</a>
    <a href="#donate">Skip to donation</a>
    <a href="#donateForm">Skip to donation form</a>
  </nav>

  <!-- Screen-reader live region -->
  <div
    id="ffLiveStatus"
    class="ff-sr"
    role="status"
    aria-live="polite"
    aria-atomic="true"
    data-ff-live-region="true"
  ></div>

  <!-- Decorative background -->
  <div class="ff-shellBg" aria-hidden="true"></div>

  <!-- Noscript fallback -->
  <noscript>
    <div class="ff-container">
      <div class="ff-notice ff-notice--premium" role="alert" aria-live="assertive" aria-atomic="true">
        <div class="ff-kicker">JavaScript required</div>
        <p class="ff-help ff-mt-1">This fundraiser uses JavaScript for secure checkout and live updates.</p>
        <p class="ff-help ff-mt-2">
          Enable JavaScript or use a modern browser. Contact:
          <a href="mailto:{{ _email|e }}">{{ _email|e }}</a>
        </p>
      </div>
    </div>
  </noscript>

  <!-- App shell -->
  <div class="ff-shell" id="top" data-ff-shell="true" data-ff-page="fundraiser">
    <!-- Toast host (supports both old and new JS hooks) -->
    <div
      id="toastHost"
      class="ff-toasts"
      aria-live="polite"
      aria-relevant="additions text"
      data-ff-toasts="true"
      data-ff-toast-host
    ></div>

    <!-- Announcement bar (hydration + dismiss-ready) -->
    <aside
      class="ff-announce"
      role="region"
      aria-label="Site announcement"
      aria-live="polite"
      data-ff-announce="true"
      {% if not _announce %}hidden{% endif %}
    >
      <div class="ff-container">
        <div class="ff-announce__row">
          <div class="ff-announce__content" aria-atomic="true">
            <span class="ff-announce__badge" aria-hidden="true">Update</span>
            <p class="ff-announce__text" data-ff-el="announceText">{{ _announce|e }}</p>
          </div>

          <div class="ff-row ff-aic" aria-label="Announcement actions">
            <a class="ff-btn ff-btn--sm ff-btn--ghost" href="#donate" data-ff-cta="announce-donate">Donate</a>
            <button
              class="ff-icon ghost"
              type="button"
              aria-label="Dismiss announcement"
              data-ff-announce-dismiss="true"
              id="ffAnnouncementDismiss"
            >
              <span aria-hidden="true">✕</span>
            </button>
          </div>
        </div>
      </div>
    </aside>

    <!-- Boot shimmer (optional) -->
    <div class="ff-boot" aria-hidden="true" data-ff-boot="true">
      <div class="ff-container">
        <div class="ff-boot__shimmer" aria-hidden="true"></div>
      </div>
    </div>

    <!-- Header / Chrome -->
    <header class="ff-chrome" role="banner" data-ff-header="true">
      <div class="ff-container">

        <!-- Topbar -->
        <div class="ff-topbar" role="region" aria-label="Fundraiser status and totals" data-ff-topbar="true">
          <div class="ff-topbar__row">

            <div class="ff-topbar__left" aria-label="Fundraiser status">
              <span class="ff-strip" data-ff-live="true">
                <span class="ff-badge__dot ff-badge__dot--verified" aria-hidden="true"></span>
                <span class="ff-help">Campaign</span>
                <span class="sep" aria-hidden="true">•</span>
                <span class="ff-num" id="campaignName" data-ff-el="campaignName">{{ _campaign_name|default('Season Fund')|e }}</span>
              </span>

              <span class="ff-strip" aria-label="Raised to date">
                <span class="ff-help">Raised</span>
                <span class="sep" aria-hidden="true">•</span>
                <strong class="ff-num" id="overallRaised" data-ff-money="raised" data-ff-el="overallRaised">{{ _init_raised }}</strong>
              </span>

              <span class="ff-strip" aria-label="Goal amount">
                <span class="ff-help">Goal</span>
                <span class="sep" aria-hidden="true">•</span>
                <strong class="ff-num" id="overallGoal" data-ff-money="goal" data-ff-el="overallGoal">{{ _init_goal }}</strong>
              </span>

              <!-- Optional deadline strip (JS can populate; safe if blank) -->
              <span class="ff-strip ff-strip--desktop" aria-label="Deadline and time left">
                <span class="ff-help">Deadline</span>
                <span class="sep" aria-hidden="true">•</span>
                <strong class="ff-num" id="overallDeadline" data-ff-el="deadline">—</strong>
                <span class="sep" aria-hidden="true">•</span>
                <span class="ff-help">Time left</span>
                <strong class="ff-num" id="overallCountdown" data-ff-el="countdown">—</strong>
              </span>
            </div>

            <div class="ff-topbar__right" aria-label="Top actions">
              <button class="ff-btn ff-btn--sm ff-btn--ghost" type="button" data-ff-modal-open="shareModal" aria-haspopup="dialog">
                Share
              </button>
              <a class="ff-btn ff-btn--sm ff-btn--secondary" href="#sponsors" data-ff-cta="sponsor-nav">Sponsor</a>
              <a class="ff-btn ff-btn--sm ff-btn--primary" href="#donate" data-ff-cta="donate-nav">Donate</a>
            </div>

          </div>
        </div>

        <!-- Main header row -->
        <div class="ff-header" data-ff-chrome="primary">
          <div class="ff-header__row">

            <a class="ff-brand" href="#hero" aria-label="{{ _org_name|e }} fundraiser home" data-ff-brand="true">
              <span class="ff-brand__mark" aria-hidden="true">
                <img
                  class="ff-brand__logo ff-logo"
                  src="{{ _org_logo|e }}"
                  alt=""
                  width="46"
                  height="46"
                  loading="eager"
                  decoding="async"
                  fetchpriority="high"
                  data-ff-el="orgLogo"
                />
              </span>
              <span class="ff-brand__meta">
                <span class="ff-brand__title" id="orgName" data-ff-el="orgName">{{ _org_name|e }}</span>
                <span class="ff-brand__sub" id="orgLocation" data-ff-el="orgLocation">{{ _org_location|e }}</span>
              </span>
            </a>

            <nav class="ff-nav" aria-label="Primary navigation" data-ff-nav="primary">
              <a href="#progress" data-ff-nav-item="progress">Progress</a>
              <a href="#impact" data-ff-nav-item="impact">Impact</a>
              <a href="#sponsors" data-ff-nav-item="sponsors">Sponsors</a>
              <a href="#teams" data-ff-nav-item="teams">Teams</a>
              <a href="#donate" data-ff-nav-item="donate">Donate</a>
              <a href="#faq" data-ff-nav-item="faq">FAQ</a>
            </nav>

            <div class="ff-header__actions" aria-label="Header actions">
              <button class="ff-btn ff-btn--ghost" type="button" data-ff-modal-open="proofModal" aria-haspopup="dialog">
                Proof
              </button>

              <button class="ff-btn ff-btn--ghost" type="button" data-ff-copy-link="true" data-ff-el="copyLinkBtn">
                Copy link
              </button>

              <a class="ff-btn ff-btn--primary" href="#donate" data-ff-cta="donate-header">Donate</a>
            </div>

          </div>

          <!-- Scroll progress -->
          <div class="ff-scroll" aria-hidden="true" data-ff-scrolltrack="true">
            <span id="scrollProgressBar" data-ff-el="scrollProgressBar"></span>
          </div>
        </div>

      </div>
    </header>

    <!-- Main (skip link target) -->
    <main id="main" class="ff-main" role="main" tabindex="-1" data-ff-main="true">
      <div id="hero" data-ff-anchor="hero" aria-hidden="true"></div>

      {# Next sections go here: Hero / Progress / Impact / Sponsors / Teams / Donate / FAQ #}
      
      <!-- ===================== HERO ===================== -->
<section
  id="hero"
  class="ff-hero ff-hero--flagship"
  data-ff-section="hero"
  data-ff-hero="true"
  aria-labelledby="heroTitle"
  tabindex="-1"
>
  <!-- Decorative background -->
  <div class="ff-hero__bg" aria-hidden="true">
    <div class="ff-hero__orb ff-hero__orb--a"></div>
    <div class="ff-hero__orb ff-hero__orb--b"></div>
    <div class="ff-hero__gridlines"></div>
  </div>

  <div class="ff-container">
    <div class="ff-hero__shell">
      <div class="ff-hero__grid" data-ff-hero-layout="true">

        <!-- Left: messaging & CTA -->
        <div class="ff-hero__copy" data-ff-hero-left="true">

          <div class="ff-row ff-wrap ff-ais" aria-label="Fundraiser context">
            <span class="ff-pill" data-ff-el="seasonPill">
              {{ _campaign_label|default(_campaign_name)|e }}
            </span>

            <span class="ff-pill ff-pill--ghost" data-ff-el="sportPill">
              {{ _sport_label|default('Youth program')|e }}
            </span>

            <span class="ff-pill" title="Deadline countdown" aria-live="polite" aria-atomic="true">
              Time left
              <span class="ff-num" data-ff-el="heroCountdown">—</span>
            </span>

            <span class="ff-pill ff-pill--accent" hidden data-ff-el="matchPill" aria-live="polite" aria-atomic="true">
              Match active
            </span>
          </div>

          <h1 id="heroTitle" class="ff-display" data-ff-el="heroHeadline">
            {{ _headline|default('Fuel the season.')|e }}<br />
            <span class="ff-accent">Keep {{ _org_name|e }} moving.</span>
          </h1>

          <p class="ff-lead" data-ff-el="heroSubhead">
            {{ _subhead|default('Your support helps cover practice, travel, fees, and essentials.')|e }}
          </p>

          <!-- KPI list -->
          <ul class="ff-hero__kpis" aria-label="Key fundraiser stats" role="list">
            <li class="ff-kpiCard" role="listitem">
              <div class="ff-help ff-muted">Raised</div>
              <div class="ff-num ff-big" data-ff-money="raised" data-ff-el="heroRaised">
                {{ _init_raised }}
              </div>
            </li>

            <li class="ff-kpiCard" role="listitem">
              <div class="ff-help ff-muted">Goal</div>
              <div class="ff-num ff-big" data-ff-money="goal" data-ff-el="heroGoal">
                {{ _init_goal }}
              </div>
            </li>

            <li class="ff-kpiCard" role="listitem">
              <div class="ff-help ff-muted">Deadline</div>
              <div class="ff-num" data-ff-el="heroDeadline">—</div>
            </li>
          </ul>

          <div class="ff-hero__cta" role="group" aria-label="Primary actions">
            <a class="ff-btn ff-btn--primary ff-btn--lg" href="#donate" data-ff-cta="hero-donate">
              Donate now
            </a>

            <button
              class="ff-btn ff-btn--ghost ff-btn--lg"
              type="button"
              data-ff-modal-open="shareModal"
              aria-haspopup="dialog"
              data-ff-cta="hero-share"
            >
              Share
            </button>
          </div>

          <div class="ff-row ff-wrap ff-ais ff-mt-2" aria-label="Trust strip">
            <span class="ff-pill ff-pill--verified"><span aria-hidden="true">✓</span> Secure checkout</span>
            <span class="ff-pill">Instant email receipt</span>
            <span class="ff-pill">No login required</span>

            {% if not _whitelabel %}
              <a
                class="ff-pill ff-pill--ghost"
                href="{{ _ff_home|e }}"
                target="_blank"
                rel="noopener noreferrer"
                data-ff-poweredby="true"
                aria-label="Powered by {{ _ff_name|e }}"
              >
                <img src="{{ _ff_logo|e }}" alt="{{ _ff_name|e }}" width="16" height="16" loading="lazy" decoding="async" />
                <span>{{ _ff_name|e }}</span>
              </a>
            {% endif %}
          </div>

          <!-- Media -->
          <div class="ff-card ff-card--premium ff-glass ff-pad ff-mt-3" aria-label="Fundraiser media" data-ff-hero-media="true">
            {% if _hero_img %}
              <figure class="ff-figure">
                <picture>
                  <source srcset="{{ _hero_img|e }} 1x" />
                  <img
                    class="ff-hero__mediaImg"
                    src="{{ _hero_img|e }}"
                    alt="{{ _org_name|e }} fundraiser image"
                    loading="eager"
                    decoding="async"
                    fetchpriority="high"
                    width="720"
                    height="420"
                    data-ff-el="heroImg"
                  />
                </picture>
                <figcaption class="ff-sr">Impact snapshot</figcaption>
              </figure>
            {% else %}
              <div class="ff-stack">
                <div class="ff-kicker">Impact snapshot</div>
                <div class="ff-label">Your gift powers practice, travel, gear, and scholarships.</div>
                <p class="ff-help ff-muted">Add <span class="ff-num">hero_image</span> to show a photo here.</p>
              </div>
            {% endif %}
          </div>

          <details class="ff-disc ff-disc--premium ff-mt-3" aria-label="Sponsor recognition policy" data-ff-premium="sponsor-upgrade">
  <summary class="ff-disc__sum">
    <span class="ff-disc__meta">
      <strong>Recognition policy</strong>
      <span class="ff-help">Respectful placement. Approval-led assets.</span>
    </span>
    <span aria-hidden="true">▾</span>
  </summary>
  <div class="ff-disc__body">
    <p class="ff-help">
      Sponsor recognition is displayed in a tiered format. Logos and names are published only when provided and approved.
      Visibility and placements can be adjusted to meet school and community guidelines.
    </p>
    <p class="ff-help">
      For sponsorship packets or invoicing support, email <a href="mailto:{{ _support_email|e }}">{{ _support_email|e }}</a>.
    </p>
  </div>
</details>

        </div>
        <!-- /Left -->

        <!-- Right: Progress rail -->
        <aside class="ff-hero__panel" aria-label="Fundraising console" data-ff-progress-rail="true">
          <section id="progress" data-ff-section="progress" aria-label="Progress" tabindex="-1">
            <article
              class="ff-card ff-card--lift ff-card--premium ff-pad ff-glass"
              aria-labelledby="progressTitle"
              data-ff-progress-card="true"
            >
              <header class="ff-row ff-row--between ff-wrap ff-ais">
                <div class="ff-minw-0">
                  <div class="ff-row ff-wrap ff-ais">
                    <span class="ff-pill ff-pill--live"><span class="ff-dot" aria-hidden="true"></span>Live</span>
                    <span class="ff-pill">
                      Goal <span class="ff-num" data-ff-el="railGoal" data-ff-money="goal">{{ _init_goal }}</span>
                    </span>
                  </div>

                  <h2 id="progressTitle" class="ff-h2 ff-mt-2">Live fundraising</h2>

                  <div class="ff-big ff-num" aria-live="polite" aria-atomic="true" data-ff-el="railRaised" data-ff-money="raised">
                    {{ _init_raised }}
                  </div>

                  <p class="ff-help">
                    <span class="ff-num" data-ff-el="railRemaining">$0</span> to go •
                    <span class="ff-num" data-ff-el="railDeadline">—</span>
                  </p>

                  <div class="ff-row ff-wrap" hidden data-ff-el="matchDetails" aria-live="polite" aria-atomic="true">
                    <span class="ff-pill ff-pill--accent">Match</span>
                    <span class="ff-pill">Remaining <span class="ff-num" data-ff-el="matchRemaining">—</span></span>
                  </div>
                </div>
              </header>

              <div class="ff-mt-2">
                <!-- Progress meter -->
                <div
                  class="ff-meter"
                  role="progressbar"
                  aria-label="Overall fundraising progress"
                  aria-valuemin="0"
                  aria-valuemax="100"
                  aria-valuenow="0"
                  aria-valuetext="0% funded"
                  data-ff-meter="overall"
                  data-ff-el="overallMeter"
                >
                  <div class="ff-meter__ticks" aria-hidden="true">
                    <i class="ff-meter__tick ff-meter__tick--25"></i>
                    <i class="ff-meter__tick ff-meter__tick--50"></i>
                    <i class="ff-meter__tick ff-meter__tick--75"></i>
                    <i class="ff-meter__tick ff-meter__tick--100"></i>
                  </div>
                  <span id="overallBar" aria-hidden="true" data-ff-el="overallBar"></span>
                </div>

                <div class="ff-row ff-row--between ff-wrap ff-mt-2" aria-label="Raised and goal numbers">
                  <span class="ff-help">Raised: <span class="ff-num" data-ff-el="railRaisedInline" data-ff-money="raised">{{ _init_raised }}</span></span>
                  <span class="ff-help">Goal: <span class="ff-num" data-ff-el="railGoalInline" data-ff-money="goal">{{ _init_goal }}</span></span>
                  <span class="ff-help"><strong class="ff-num" data-ff-el="overallPercent">0</strong>%</span>
                </div>
              </div>

              <div class="ff-divider ff-my-2" aria-hidden="true"></div>

              <!-- Quick gifts -->
              <section aria-label="Quick gifts" data-ff-quickgifts="true">
                <div class="ff-kicker">Quick gifts</div>
                <p class="ff-help">One tap prefills secure checkout.</p>

                <ul class="ff-chipgrid" role="list" aria-label="Quick gift buttons">
                  <li><button class="ff-chip" type="button" data-ff-quick-amount="25" data-ff-prefill-amount="25">$25</button></li>
                  <li><button class="ff-chip" type="button" data-ff-quick-amount="75" data-ff-prefill-amount="75">$75</button></li>
                  <li><button class="ff-chip" type="button" data-ff-quick-amount="150" data-ff-prefill-amount="150">$150</button></li>
                  <li><button class="ff-chip" type="button" data-ff-quick-amount="500" data-ff-prefill-amount="500">$500</button></li>
                  <li class="ff-chipgrid__span2">
                    <button class="ff-chip" type="button" data-ff-quick-amount="1000" data-ff-prefill-amount="1000">Sponsor $1,000</button>
                  </li>
                </ul>
              </section>

              <div class="ff-divider ff-my-2" aria-hidden="true"></div>

              <!-- QR block -->
              <section class="ff-row ff-wrap ff-ais" aria-label="Scan to donate" data-ff-qr-block="true">
                <img
                  class="ff-qr"
                  data-ff-qr="img"
                  alt="{{ _org_name|e }} fundraiser QR code"
                  width="112"
                  height="112"
                  loading="eager"
                  decoding="async"
                />
                <div class="ff-minw-0 ff-grow">
                  <div class="ff-kicker">Scan to donate</div>
                  <div class="ff-label">Fast, secure checkout</div>

                  <div class="ff-row ff-wrap ff-mt-2" role="group" aria-label="Share actions">
                    <button class="ff-btn ff-btn--secondary ff-btn--sm" type="button" data-ff-modal-open="shareModal" aria-haspopup="dialog">
                      Share tools
                    </button>
                    <button class="ff-btn ff-btn--ghost ff-btn--sm" type="button" data-ff-qr-download="true">
                      Download QR
                    </button>
                  </div>
                </div>
              </section>

              <div class="ff-row ff-wrap ff-mt-3" role="group" aria-label="Console actions">
                <a class="ff-btn ff-btn--primary ff-btn--lg" href="#donate" data-ff-cta="rail-donate">Donate securely</a>
              </div>

              <p class="ff-help ff-muted ff-mt-2">Secure checkout • Instant email receipt</p>

              <details class="ff-disc ff-disc--premium ff-mt-3" aria-label="Progress details" data-ff-progress-details="true">
                <summary class="ff-disc__sum">
                  <span>
                    <span class="ff-kicker">Recent gifts</span><br />
                    <span class="ff-help">Shown only when donors allow recognition</span>
                  </span>
                  <span class="ff-pill" aria-hidden="true">Open</span>
                </summary>

                <div class="ff-disc__body">
                  <div class="ff-row ff-row--between ff-wrap">
                    <div>
                      <div class="ff-kicker">Recent gifts</div>
                      <p class="ff-help">Live list updates when donors opt into recognition.</p>
                    </div>
                    <button class="ff-btn ff-btn--ghost ff-btn--sm" type="button" data-ff-refresh="recentGifts">Refresh</button>
                  </div>

                  <div class="ff-slimlist" data-ff-el="recentGifts" aria-live="polite" aria-atomic="false"></div>
                </div>
              </details>

            </article>
          </section>
        </aside>
        <!-- /Right -->

      </div>
    </div>
  </div>
</section>
<!-- /HERO -->
<!-- ===================== IMPACT ===================== -->
<section
  id="impact"
  class="ff-section"
  data-ff-section="impact"
  tabindex="-1"
  aria-labelledby="impactTitle"
>
  <div class="ff-container">
    <article class="ff-card ff-card--lift ff-card--premium ff-pad ff-glass">

      <header class="ff-sectionhead">
        <div class="ff-sectionhead__text ff-minw-0">
          <div class="ff-kicker">Impact</div>
          <h2 id="impactTitle" class="ff-h2">Pick an impact</h2>
          <p class="ff-lead">
            Choose a tier to support <strong class="ff-num" data-ff-el="impactOrgName">{{ _org_name|e }}</strong>.
            Tap any option to prefill checkout.
          </p>
        </div>

        <div class="ff-sectionhead__actions" role="group" aria-label="Impact actions">
          <a class="ff-btn ff-btn--ghost" href="#donate" data-ff-cta="impact-custom">Custom amount</a>
          <a class="ff-btn ff-btn--ghost" href="#sponsors">Sponsor leaderboard</a>
        </div>
      </header>

      <div class="ff-divider ff-my-3" aria-hidden="true"></div>

      <div class="ff-panel ff-panel--premium" data-ff-impact-panel="true">
        <div class="ff-row ff-row--between ff-wrap ff-ais">
          <div class="ff-minw-0">
            <div class="ff-kicker">Impact ladder</div>
            <div class="ff-label">Choose what your gift funds</div>
            <p class="ff-help ff-muted ff-m-0">Each tier is a one-tap shortcut into secure checkout.</p>
          </div>

          <div class="ff-row ff-wrap" role="group" aria-label="Tier quick actions">
            <button class="ff-btn ff-btn--primary ff-btn--sm" type="button" data-ff-prefill-amount="75" data-ff-cta="impact-75">Give $75</button>
            <button class="ff-btn ff-btn--ghost ff-btn--sm" type="button" data-ff-prefill-amount="150" data-ff-cta="impact-150">Give $150</button>
          </div>
        </div>

        <div
          class="ff-grid ff-grid--cards ff-mt-3"
          data-ff-impact-tiergrid="true"
          data-ff-el="impactTierGrid"
          aria-label="Impact tiers"
          role="list"
        >
          <!-- Starter cards (safe defaults). JS can replace or upgrade. -->
          <article class="ff-mini ff-mini--premium" role="listitem" tabindex="0" data-ff-tier-id="starter" data-ff-prefill-amount="25">
            <div class="ff-kicker">Starter</div>
            <div class="ff-label">Essentials</div>
            <p class="ff-help ff-muted">Snacks • supplies • basics</p>
            <button class="ff-btn ff-btn--primary ff-btn--sm ff-mt-2" type="button" data-ff-prefill-amount="25">Give $25</button>
          </article>

          <article class="ff-mini ff-mini--premium" role="listitem" tabindex="0" data-ff-tier-id="momentum" data-ff-prefill-amount="75">
            <div class="ff-kicker">Momentum</div>
            <div class="ff-label">Cover the week</div>
            <p class="ff-help ff-muted">Gym time • consistency</p>
            <button class="ff-btn ff-btn--primary ff-btn--sm ff-mt-2" type="button" data-ff-prefill-amount="75">Give $75</button>
          </article>

          <article class="ff-mini ff-mini--premium" role="listitem" tabindex="0" data-ff-tier-id="travel" data-ff-prefill-amount="150">
            <div class="ff-kicker">Travel</div>
            <div class="ff-label">Tournament-ready</div>
            <p class="ff-help ff-muted">Fees • travel support</p>
            <button class="ff-btn ff-btn--primary ff-btn--sm ff-mt-2" type="button" data-ff-prefill-amount="150">Give $150</button>
          </article>
        </div>

        <p class="ff-help ff-muted ff-mt-3 ff-mb-0">Secure checkout • Instant email receipt • No login required</p>

        <!-- Hidden hooks (optional future expansion) -->
        <section hidden aria-hidden="true">
          <div data-ff-impact-filters="true"></div>
          <div data-ff-impact-grid="true"></div>
          <div data-ff-impact-skeleton="true"></div>
        </section>
      </div>

    </article>
  </div>
</section>
<!-- /IMPACT -->

  <!-- 4) SPONSORS & RECOGNITION -->
  <!-- ===================== SPONSORS ===================== -->
<section
  id="sponsors"
  class="ff-section"
  data-ff-section="sponsors"
  tabindex="-1"
  aria-labelledby="sponsorsTitle"
>
  <div class="ff-container">
    <article class="ff-card ff-card--lift ff-card--premium ff-pad ff-glass" data-ff-sponsors="true">
      <header class="ff-sectionhead">
        <div class="ff-sectionhead__text ff-minw-0">
          <div class="ff-kicker">Sponsors</div>
          <h2 id="sponsorsTitle" class="ff-h2">
            Sponsor <span data-ff-el="sponsorsOrgName">{{ _org_name|e }}</span>
          </h2>
          <p class="ff-lead">
            Put your brand on the wall, climb the VIP leaderboard, and get an instant receipt in seconds.
          </p>

          <div class="ff-row ff-wrap ff-ais ff-mt-2" role="group" aria-label="Sponsor highlights">
            <span class="ff-pill">Logo wall</span>
            <span class="ff-pill">VIP leaderboard</span>
            <span class="ff-pill">Instant receipt</span>
            <span class="ff-pill" hidden data-ff-el="sponsorMatchPill">Match active</span>
          </div>
        </div>

        <div class="ff-sectionhead__actions" role="group" aria-label="Sponsor actions">
          <button
            class="ff-btn ff-btn--primary"
            type="button"
            data-ff-prefill-amount="1000"
            data-ff-cta="sponsor-primary"
          >
            Become a sponsor
          </button>

          <button
            class="ff-btn ff-btn--secondary"
            type="button"
            data-ff-modal-open="proofModal"
            aria-haspopup="dialog"
            data-ff-cta="sponsor-packet"
          >
            Sponsor packet
          </button>

          <button
            class="ff-btn ff-btn--ghost"
            type="button"
            data-ff-modal-open="shareModal"
            aria-haspopup="dialog"
            data-ff-cta="sponsor-share"
          >
            Share
          </button>
        </div>
      </header>

      <div class="ff-divider ff-my-3" aria-hidden="true"></div>

      <!-- Quick tiers -->
      <section class="ff-mini ff-mini--premium" aria-label="Sponsor quick tiers" data-ff-el="sponsorQuick">
        <div class="ff-row ff-row--between ff-wrap ff-ais">
          <div class="ff-minw-0">
            <div class="ff-kicker">Quick sponsor</div>
            <p class="ff-help ff-muted ff-m-0">One tap prefills secure checkout.</p>
          </div>

          <button class="ff-btn ff-btn--ghost ff-btn--sm" type="button" data-ff-el="refreshSponsorsBtn">
            Refresh
          </button>
        </div>

        <div class="ff-row ff-wrap ff-mt-2" role="group" aria-label="Sponsor quick amount buttons">
          <button class="ff-chip" type="button" data-ff-prefill-amount="250" data-ff-cta="sponsor-250">$250</button>
          <button class="ff-chip" type="button" data-ff-prefill-amount="500" data-ff-cta="sponsor-500">$500</button>
          <button class="ff-chip" type="button" data-ff-prefill-amount="1000" data-ff-cta="sponsor-1000">$1,000</button>
          <button class="ff-chip" type="button" data-ff-prefill-amount="2500" data-ff-cta="sponsor-2500">$2,500</button>
        </div>

        <div class="ff-row ff-wrap ff-mt-2" role="group" aria-label="Sponsor tools">
          <button class="ff-btn ff-btn--ghost ff-btn--sm" type="button" data-ff-copy-link="true" data-ff-cta="sponsor-copy">
            Copy link
          </button>

          <button
            class="ff-btn ff-btn--ghost ff-btn--sm"
            type="button"
            data-ff-modal-open="policyModal"
            aria-haspopup="dialog"
            data-ff-cta="sponsor-trust"
          >
            Trust center
          </button>

          <span class="ff-help ff-muted" hidden data-ff-el="sponsorUpdated">
            Updated <span class="ff-num" data-ff-el="sponsorUpdatedText">—</span>
          </span>
        </div>
      </section>

      <!-- Dashboard -->
      <div class="ff-sponsorsDash ff-mt-4" data-ff-el="sponsorsDash">
        <section class="ff-sponsorsDash__main" aria-label="Sponsor leaderboard and tiers">
          <!-- Leaderboard -->
          <section class="ff-mini ff-mini--premium" aria-label="Leaderboard" data-ff-el="sponsorLeaderboardCard">
            <div class="ff-row ff-row--between ff-wrap ff-ais">
              <div class="ff-minw-0">
                <div class="ff-kicker">Leaderboard</div>
                <div class="ff-label">Climb the VIP list</div>
                <p class="ff-help ff-muted ff-m-0">Recognition is opt-in. Sponsors can choose anonymity at checkout.</p>
              </div>

              <div class="ff-row ff-wrap" role="group" aria-label="Leaderboard sort" data-ff-el="sponsorSort">
                <button class="ff-chip is-selected" type="button" aria-pressed="true" data-ff-sort="rank">Rank</button>
                <button class="ff-chip" type="button" aria-pressed="false" data-ff-sort="recent">Recent</button>
              </div>
            </div>

            <ol class="ff-stack ff-mt-2" id="ffSponsorLeaderboard" data-ff-el="sponsorLeaderboard" aria-live="polite"></ol>

            <ol
              class="ff-stack ff-mt-2"
              id="ffSponsorLeaderboardSkeleton"
              data-ff-el="sponsorLeaderboardSkeleton"
              aria-hidden="true"
              hidden
            >
              <li class="ff-mini ff-mini--premium"></li>
              <li class="ff-mini ff-mini--premium"></li>
              <li class="ff-mini ff-mini--premium"></li>
            </ol>
          </section>

          <!-- Tiers -->
          <section class="ff-mini ff-mini--premium ff-mt-2" aria-label="Sponsor tiers" data-ff-el="sponsorTiersCard">
            <div class="ff-row ff-row--between ff-wrap ff-ais">
              <div class="ff-minw-0">
                <div class="ff-kicker">Sponsor tiers</div>
                <div class="ff-label">Choose your placement</div>
                <p class="ff-help ff-muted ff-m-0">Recognition preferences are chosen at checkout.</p>
              </div>

              <div class="ff-row ff-wrap" role="group" aria-label="Tier actions">
                <button
                  class="ff-btn ff-btn--ghost ff-btn--sm"
                  type="button"
                  data-ff-modal-open="proofModal"
                  aria-haspopup="dialog"
                  data-ff-cta="sponsor-tier-packet"
                >
                  Sponsor packet
                </button>

                <button
                  class="ff-btn ff-btn--ghost ff-btn--sm"
                  type="button"
                  data-ff-modal-open="policyModal"
                  aria-haspopup="dialog"
                  data-ff-cta="sponsor-tier-trust"
                >
                  Trust
                </button>
              </div>
            </div>

            <ul class="ff-stack ff-mt-2" id="ffSponsorTiers" data-ff-el="sponsorTiers" aria-label="Sponsor tiers list"></ul>

            <div class="ff-row ff-wrap ff-mt-2" role="group" aria-label="Sponsor CTAs">
              <button class="ff-btn ff-btn--primary" type="button" data-ff-prefill-amount="1000" data-ff-cta="sponsor-cta-1000">
                Sponsor $1,000
              </button>

              <button
                class="ff-btn ff-btn--secondary"
                type="button"
                data-ff-modal-open="shareModal"
                aria-haspopup="dialog"
                data-ff-cta="sponsor-cta-share"
              >
                Share
              </button>

              <button class="ff-btn ff-btn--ghost" type="button" data-ff-copy-link="true" data-ff-cta="sponsor-cta-copy">
                Copy link
              </button>
            </div>

            <p class="ff-help ff-muted ff-mt-2">Updates automatically after successful payments.</p>
          </section>
        </section>

        <!-- Sponsor wall -->
        <aside class="ff-sponsorsDash__rail" aria-label="Sponsor wall">
          <section class="ff-mini ff-mini--premium" aria-label="Sponsor wall" data-ff-el="sponsorWallCard">
            <div class="ff-row ff-row--between ff-wrap ff-ais">
              <div class="ff-minw-0">
                <div class="ff-kicker">Sponsor wall</div>
                <div class="ff-label">Logos appear automatically</div>
                <p class="ff-help ff-muted ff-m-0">Logos appear after successful payments.</p>
              </div>

              <div class="ff-row ff-wrap" role="group" aria-label="Wall view" data-ff-el="sponsorWallView">
                <button class="ff-chip is-selected" type="button" aria-pressed="true" data-ff-wall="logos">Logos</button>
                <button class="ff-chip" type="button" aria-pressed="false" data-ff-wall="list">List</button>
              </div>
            </div>

            <div class="ff-sponsors__wallwrap ff-mt-2">
              <div id="ffSponsorWall" class="ff-sponsors__wall" data-ff-el="sponsorWall" aria-live="polite"></div>

              <div
                id="ffSponsorWallSkeleton"
                class="ff-sponsors__wall"
                data-ff-el="sponsorWallSkeleton"
                aria-hidden="true"
                hidden
              >
                <div class="ff-mini ff-mini--premium"></div>
                <div class="ff-mini ff-mini--premium"></div>
                <div class="ff-mini ff-mini--premium"></div>
                <div class="ff-mini ff-mini--premium"></div>
              </div>
            </div>

            <div class="ff-row ff-wrap ff-mt-2" role="group" aria-label="Wall actions">
              <button class="ff-btn ff-btn--primary ff-btn--sm" type="button" data-ff-prefill-amount="1000" data-ff-cta="sponsor-wall-1000">
                Sponsor $1,000
              </button>

              <button
                class="ff-btn ff-btn--ghost ff-btn--sm"
                type="button"
                data-ff-modal-open="proofModal"
                aria-haspopup="dialog"
                data-ff-cta="sponsor-wall-packet"
              >
                Sponsor packet
              </button>

              <button
                class="ff-btn ff-btn--ghost ff-btn--sm"
                type="button"
                data-ff-modal-open="shareModal"
                aria-haspopup="dialog"
                data-ff-cta="sponsor-wall-share"
              >
                Share
              </button>

              <button class="ff-btn ff-btn--ghost ff-btn--sm" type="button" data-ff-el="refreshSponsorsBtn2">
                Refresh
              </button>
            </div>

            <p class="ff-help ff-muted ff-mt-2">Secure checkout • Instant receipt • No account required</p>
          </section>
        </aside>
      </div>

      <!-- Templates -->
      <template id="ffSponsorRowTemplate" data-ff-sponsor-row-template>
        <li class="ff-sponsorRow">
          <div class="ff-row ff-row--between ff-wrap ff-ais">
            <div class="ff-minw-0">
              <div class="ff-label" data-ff-sponsor-name></div>
              <p class="ff-help ff-muted ff-m-0" data-ff-sponsor-meta></p>
            </div>
            <div class="ff-pill"><span class="ff-num" data-ff-sponsor-amount>$0</span></div>
          </div>
        </li>
      </template>

      <template id="ffSponsorLogoTemplate" data-ff-sponsor-logo-template>
        <a class="ff-sponsorLogo" href="#" target="_blank" rel="noopener noreferrer" aria-label="Sponsor">
          <img src="" alt="" loading="lazy" decoding="async" />
        </a>
      </template>
    </article>
  </div>
</section>
<!-- /SPONSORS -->

  <!-- 5) TEAMS / ATHLETES -->
  <!-- ===================== TEAMS ===================== -->
<section
  id="teams"
  class="ff-section"
  data-ff-section="teams"
  tabindex="-1"
  aria-labelledby="teamsTitle"
>
  <div class="ff-container">
    <article
      class="ff-card ff-card--lift ff-card--premium ff-pad ff-glass"
      data-ff-teams="true"
    >
      <header class="ff-sectionhead">
        <div class="ff-sectionhead__text ff-minw-0">
          <div class="ff-kicker">Teams</div>
          <h2 id="teamsTitle" class="ff-h2">Support a specific team</h2>
          <p class="ff-lead">
            Selecting a team is optional — donations can credit them automatically and unlock a team share link.
          </p>

          <div class="ff-row ff-wrap ff-ais ff-mt-2" aria-label="Team selection status">
            <span
              class="ff-pill ff-pill--accent"
              hidden
              data-ff-el="teamSelectedPill"
              aria-live="polite"
              aria-atomic="true"
            >
              Selected — <span class="ff-num" data-ff-el="teamSelectedName">Team</span>
            </span>

            <span class="ff-help ff-muted" data-ff-el="teamsHelp">
              Search or sort, then press Enter/Space on a card to attribute your gift.
            </span>
          </div>
        </div>

        <div class="ff-sectionhead__actions" role="group" aria-label="Team actions">
          <a class="ff-btn ff-btn--primary" href="#donate" data-ff-cta="teams-donate">Donate</a>

          <button
            class="ff-btn ff-btn--secondary"
            type="button"
            data-ff-modal-open="shareModal"
            aria-haspopup="dialog"
            data-ff-cta="teams-share"
          >
            Share
          </button>

          <button
            class="ff-btn ff-btn--ghost ff-btn--sm"
            type="button"
            data-ff-copy-link="true"
            data-ff-cta="teams-copylink"
          >
            Copy link
          </button>
        </div>
      </header>

      <div class="ff-divider ff-my-3" aria-hidden="true"></div>

      <!-- Filters -->
      <section aria-label="Find and sort teams" data-ff-teams-controls="true">
        <div class="ff-grid ff-grid--2">
          <div>
            <label class="ff-sr" for="teamSearch">Search teams</label>
            <input
              id="teamSearch"
              class="ff-input"
              type="search"
              inputmode="search"
              autocomplete="off"
              placeholder="Search teams…"
              data-ff-el="teamSearch"
              aria-describedby="teamSearchHelp"
            />
            <div id="teamSearchHelp" class="ff-help ff-muted">
              Search by name, grade, or color (try “7th”, “Gold”, “Black”).
            </div>
          </div>

          <div
            class="ff-row ff-wrap ff-ais"
            role="group"
            aria-label="Sort teams"
            data-ff-el="teamSortGroup"
          >
            <button
              class="ff-chip is-selected"
              type="button"
              aria-pressed="true"
              data-ff-sort="featured"
              data-ff-cta="teams-sort-featured"
            >
              Featured
            </button>
            <button
              class="ff-chip"
              type="button"
              aria-pressed="false"
              data-ff-sort="pct"
              data-ff-cta="teams-sort-pct"
            >
              Goal %
            </button>
            <button
              class="ff-chip"
              type="button"
              aria-pressed="false"
              data-ff-sort="name"
              data-ff-cta="teams-sort-name"
            >
              Name
            </button>

            <button
              class="ff-btn ff-btn--ghost ff-btn--sm"
              type="button"
              hidden
              data-ff-el="attribClearBtn"
              data-ff-cta="teams-clear"
            >
              Clear
            </button>
          </div>
        </div>

        <!-- SR status updates (search results, selection, etc.) -->
        <div class="ff-sr" role="status" aria-live="polite" aria-atomic="true" data-ff-el="teamsStatus"></div>

        <div class="ff-row ff-row--between ff-wrap ff-ais ff-mt-2">
          <p class="ff-help ff-muted ff-m-0">
            Tip: selecting a team credits the donation and can generate a team-specific share link.
          </p>

          <p
            class="ff-help ff-muted ff-m-0"
            hidden
            data-ff-el="teamsCount"
            aria-live="polite"
            aria-atomic="true"
          >
            <span class="ff-num" data-ff-el="teamsCountNum">0</span> teams
          </p>
        </div>
      </section>

      <!-- Grid -->
      <section class="ff-mt-3" aria-label="Teams list">
        <div
          class="ff-grid ff-grid--cards ff-teams__grid"
          id="ffTeamsGrid"
          data-ff-el="teamsGrid"
          role="list"
          aria-live="polite"
          aria-atomic="false"
        ></div>

        <!-- Skeleton -->
        <div class="ff-grid ff-grid--cards" data-ff-el="teamsSkeleton" aria-hidden="true" hidden>
          <div class="ff-mini ff-mini--premium"></div>
          <div class="ff-mini ff-mini--premium"></div>
          <div class="ff-mini ff-mini--premium"></div>
          <div class="ff-mini ff-mini--premium"></div>
        </div>

        <!-- Empty state -->
        <div
          class="ff-mini ff-mini--premium ff-mt-3"
          hidden
          data-ff-el="teamsEmpty"
          role="status"
          aria-label="No teams found"
        >
          <div class="ff-kicker">No matches</div>
          <div class="ff-label">Try a different search</div>
          <p class="ff-help ff-muted">Try “7th”, “Gold”, or “Black”. Or clear the search field.</p>
          <div class="ff-row ff-wrap ff-mt-2" role="group" aria-label="Empty state actions">
            <a class="ff-btn ff-btn--primary ff-btn--sm" href="#donate" data-ff-cta="teams-empty-donate">
              Donate without picking a team
            </a>
            <button class="ff-btn ff-btn--ghost ff-btn--sm" type="button" data-ff-copy-link="true" data-ff-cta="teams-empty-copy">
              Copy fundraiser link
            </button>
          </div>
        </div>

        <!-- Team sheet mount (optional modal/drawer/details) -->
        <div id="ffTeamSheetMount" hidden data-ff-el="teamSheetMount" aria-hidden="true"></div>

        <!-- Template: Team card (cloned by JS) -->
        <template id="ffTeamCardTemplate" data-ff-el="teamCardTemplate">
          <article class="ff-teamCard" role="listitem" tabindex="0" data-team-id="">
            <header class="ff-row ff-row--between ff-ais">
              <div class="ff-minw-0">
                <div class="ff-kicker" data-ff-team-grade></div>
                <div class="ff-label" data-ff-team-name></div>
                <p class="ff-help ff-muted ff-m-0" data-ff-team-meta></p>
              </div>

              <div class="ff-row ff-wrap ff-aic" data-ff-team-tools>
                <button class="ff-btn ff-btn--ghost ff-btn--sm" type="button" data-ff-team-select>Select</button>
              </div>
            </header>

            <figure class="ff-teamCard__media ff-mt-2" data-ff-team-media hidden>
              <img
                class="ff-teamCard__img"
                src=""
                alt=""
                loading="lazy"
                decoding="async"
                width="640"
                height="360"
                data-ff-team-photo
              />
            </figure>

            <div class="ff-mt-2" data-ff-team-meter aria-hidden="true"></div>

            <div class="ff-row ff-row--between ff-wrap ff-mt-2" aria-label="Team totals">
              <span class="ff-help">Raised <strong class="ff-num" data-ff-team-raised>$0</strong></span>
              <span class="ff-help">Goal <strong class="ff-num" data-ff-team-goal>$0</strong></span>
              <span class="ff-help"><strong class="ff-num" data-ff-team-pct>0</strong>%</span>
            </div>

            <div class="ff-row ff-wrap ff-mt-2" role="group" aria-label="Team actions">
              <a class="ff-btn ff-btn--primary ff-btn--sm" href="#donate" data-ff-team-donate data-ff-cta="team-donate">
                Donate
              </a>
              <button class="ff-btn ff-btn--secondary ff-btn--sm" type="button" data-ff-team-share data-ff-cta="team-share">
                Share
              </button>
            </div>
          </article>
        </template>
      </section>
    </article>
  </div>
</section>
<!-- /TEAMS -->

  <!-- 6) CHECKOUT ENTRY -->
  <!-- ===================== DONATE ===================== -->
<section
  id="donate"
  class="ff-section"
  data-ff-section="donate"
  tabindex="-1"
  aria-labelledby="donateTitle"
>
  <div class="ff-container">
    <article
      class="ff-card ff-card--lift ff-card--premium ff-pad ff-glass"
      data-ff-donate="true"
    >
      <header class="ff-sectionhead">
        <div class="ff-sectionhead__text ff-minw-0">
          <div class="ff-kicker">Checkout</div>
          <h2 id="donateTitle" class="ff-h2">
            Secure checkout for <span data-ff-el="donateOrgName">{{ _org_name|e }}</span>
          </h2>
          <p class="ff-lead">
            Fast, encrypted payment. Instant email receipt. Donate with a name — or go anonymous.
          </p>

          <div class="ff-row ff-wrap ff-mt-2" role="group" aria-label="Checkout trust">
            <span class="ff-pill">Stripe-secured</span>
            <span class="ff-pill">Instant receipt</span>
            <span class="ff-pill">No login</span>
            <span class="ff-pill" aria-live="polite" aria-atomic="true">
              Status — <span class="ff-num" data-ff-el="checkoutStatusText">Ready</span>
            </span>
          </div>

          <p class="ff-help ff-muted ff-mt-2">
            Cards + wallets via Stripe • Apple Pay / Google Pay when available
          </p>
        </div>

        <div class="ff-sectionhead__actions" role="group" aria-label="Checkout actions">
          <button
            class="ff-btn ff-btn--ghost"
            type="button"
            data-ff-modal-open="policyModal"
            aria-haspopup="dialog"
            data-ff-cta="donate-trust"
          >
            Trust center
          </button>
          <button
            class="ff-btn ff-btn--ghost"
            type="button"
            data-ff-modal-open="proofModal"
            aria-haspopup="dialog"
            data-ff-cta="donate-proof"
          >
            Proof kit
          </button>
        </div>
      </header>

      <!-- Notices -->
      <div class="ff-stack ff-mt-3" aria-label="Checkout notices" data-ff-el="checkoutNotices">
        <div
          class="ff-notice ff-notice--error ff-notice--premium"
          role="alert"
          hidden
          aria-live="assertive"
          aria-atomic="true"
          data-ff-el="payError"
        >
          <div class="ff-kicker">Payment error</div>
          <p class="ff-help" data-ff-el="payErrorText">Something went wrong. Please try again.</p>
        </div>

        <div
          class="ff-notice ff-notice--success ff-notice--premium"
          role="status"
          hidden
          aria-live="polite"
          aria-atomic="true"
          data-ff-el="paySuccess"
        >
          <div class="ff-kicker">Payment successful</div>
          <p class="ff-help" data-ff-el="paySuccessText">Thank you — your receipt has been emailed.</p>
        </div>
      </div>

      <!-- Checkout shell -->
      <div class="ff-checkoutShell ff-mt-3" data-ff-el="donateShell">
        <!-- Real skip-target -->
        <span id="donateForm" class="ff-sr" tabindex="-1">Donation form</span>

        <form
          class="ff-form ff-checkoutGrid"
          novalidate
          id="ffDonationForm"
          data-ff-el="donationForm"
        >
          <!-- Main -->
          <div class="ff-checkoutGrid__main">
            <!-- Amount -->
            <section class="ff-mini ff-mini--premium" aria-label="Donation amount" data-ff-el="amountSection">
              <div class="ff-row ff-row--between ff-wrap ff-ais">
                <div class="ff-minw-0">
                  <div class="ff-label">Amount</div>
                  <p class="ff-help ff-muted ff-m-0">Enter a custom amount, or tap a suggestion.</p>
                </div>

                <span
                  class="ff-pill"
                  hidden
                  aria-live="polite"
                  aria-atomic="true"
                  data-ff-el="popularAmountPill"
                >
                  Most popular — <span class="ff-num" data-ff-el="popularAmountText">$75</span>
                </span>
              </div>

              <div class="ff-inputrow ff-mt-2">
                <span class="ff-inputrow__prefix" aria-hidden="true">$</span>
                <input
                  id="donationAmount"
                  class="ff-input"
                  name="amount"
                  type="text"
                  inputmode="decimal"
                  autocomplete="off"
                  spellcheck="false"
                  placeholder="25"
                  required
                  aria-describedby="amountHelp"
                  data-ff-el="amountInput"
                />
              </div>
              <div id="amountHelp" class="ff-help ff-muted">
                Minimum $1. Enter numbers only (no dollar sign).
              </div>

              <div class="ff-row ff-wrap ff-mt-2" role="group" aria-label="Suggested amounts" data-ff-el="quickAmounts">
                <button class="ff-chip" type="button" data-ff-prefill-amount="25" data-ff-cta="donate-amt-25">$25</button>
                <button class="ff-chip" type="button" data-ff-prefill-amount="75" data-ff-cta="donate-amt-75">$75</button>
                <button class="ff-chip" type="button" data-ff-prefill-amount="150" data-ff-cta="donate-amt-150">$150</button>
                <button class="ff-chip" type="button" data-ff-prefill-amount="500" data-ff-cta="donate-amt-500">$500</button>
                <button class="ff-chip" type="button" data-ff-prefill-amount="1000" data-ff-cta="donate-amt-1000">Sponsor $1,000</button>
                <button class="ff-chip" type="button" hidden data-ff-el="clearAmountBtn" data-ff-cta="donate-amt-clear">Clear</button>
              </div>
            </section>

            <!-- Receipt & options -->
            <section class="ff-mini ff-mini--premium ff-mt-2" aria-label="Receipt email and options" data-ff-el="receiptSection">
              <div class="ff-row ff-row--between ff-wrap ff-ais">
                <div class="ff-minw-0">
                  <div class="ff-kicker">Receipt</div>
                  <div class="ff-label">Email (for receipt)</div>
                </div>
                <span class="ff-pill">Instant <span class="ff-num">email receipt</span></span>
              </div>

              <input
                id="donationEmail"
                class="ff-input ff-mt-2"
                name="email"
                type="email"
                inputmode="email"
                autocomplete="email"
                placeholder="you@example.com"
                required
                data-ff-el="emailInput"
              />
              <p class="ff-help">We’ll email your receipt immediately after payment.</p>

              <div class="ff-grid ff-grid--2 ff-mt-2" aria-label="Donation toggles">
                <label class="ff-check ff-check--premium">
                  <input type="checkbox" name="anonymous" data-ff-el="anonymousToggle" />
                  Donate anonymously (hide name)
                </label>

                <label class="ff-check ff-check--premium" data-ff-el="coverFeesToggle">
                  <input type="checkbox" name="coverFees" data-ff-el="coverFeesInput" />
                  Cover processing fees (helps the program)
                </label>
              </div>

              <!-- Attribution box (shown when a team is selected) -->
              <div class="ff-mini ff-mini--premium ff-mt-2" hidden data-ff-el="attribBox" aria-label="Team attribution">
                <div class="ff-row ff-row--between ff-ais">
                  <div class="ff-minw-0">
                    <div class="ff-kicker">Crediting a team</div>
                    <p class="ff-help ff-m-0">
                      Your donation will credit: <strong class="ff-num" data-ff-el="attribTeamName">Team</strong>
                    </p>
                  </div>
                  <button class="ff-btn ff-btn--ghost ff-btn--sm" type="button" data-ff-el="attribClearBtn">
                    Clear
                  </button>
                </div>
              </div>
            </section>

            <!-- Optional name/message -->
            <details class="ff-disc ff-disc--premium ff-mt-2" data-ff-el="optionalDetails">
              <summary class="ff-disc__sum">
                <span>
                  <span class="ff-kicker">Optional</span><br />
                  <span class="ff-help">Name + message</span>
                </span>
                <span class="ff-pill" aria-hidden="true">Open</span>
              </summary>

              <div class="ff-disc__body">
                <label class="ff-label">
                  Name (optional)
                  <input class="ff-input" name="fullName" autocomplete="name" placeholder="Your name" data-ff-el="nameInput" />
                </label>

                <label class="ff-label ff-mt-2">
                  Message (optional)
                  <textarea class="ff-input" name="message" placeholder="Add a short note of support" data-ff-el="messageInput"></textarea>
                </label>
              </div>
            </details>

            <!-- Payment mount -->
            <section class="ff-mini ff-mini--premium ff-mt-2" aria-label="Payment details" data-ff-el="paymentSection">
              <div class="ff-row ff-row--between ff-wrap ff-ais">
                <div class="ff-minw-0">
                  <div class="ff-kicker">Payment</div>
                  <p class="ff-help ff-muted ff-m-0">Card or wallet options appear below (Stripe).</p>
                </div>
              </div>

              <div class="ff-mt-2" aria-label="Stripe payment element" data-ff-el="stripeMount">
                <div data-ff-stripe-element data-ff-id="paymentElement" aria-hidden="true"></div>
                <p class="ff-help ff-mt-2">Securely processed by Stripe.</p>
              </div>

              <div class="ff-mini ff-mini--premium ff-mt-2" hidden data-ff-el="paypalMount">
                <div class="ff-kicker">PayPal</div>
                <div data-ff-paypal-buttons data-ff-id="paypalButtons"></div>
              </div>

              <div class="ff-sr" role="status" aria-live="polite" aria-atomic="true" data-ff-el="paymentLive"></div>
            </section>
          </div>

          <!-- Rail -->
          <aside class="ff-checkoutGrid__rail" role="complementary" aria-label="Checkout summary and trust">
            <div class="ff-railStack">
              <section class="ff-mini ff-mini--premium ff-railCard" aria-label="Donation summary" data-ff-el="summaryCard">
                <div class="ff-row ff-row--between ff-wrap ff-ais">
                  <div>
                    <div class="ff-kicker">Summary</div>
                    <div class="ff-label">Instant receipt, clear total</div>
                  </div>
                  <span class="ff-pill">Total — <span class="ff-num" data-ff-el="summaryTotal">—</span></span>
                </div>

                <div class="ff-slimlist ff-mt-2">
                  <div class="ff-slimitem">
                    <span class="ff-help">Donation</span>
                    <strong class="ff-help ff-num" data-ff-el="summaryAmount" data-ff-money="donation">$0</strong>
                  </div>
                  <div class="ff-slimitem">
                    <span class="ff-help">Fees</span>
                    <strong class="ff-help ff-num" data-ff-el="summaryFees">—</strong>
                  </div>
                  <div class="ff-slimitem" hidden data-ff-el="summaryTeamRow">
                    <span class="ff-help">Crediting</span>
                    <strong class="ff-help ff-num" data-ff-el="summaryTeam">Team</strong>
                  </div>
                </div>

                <p class="ff-help ff-mt-2">
                  Receipt email: <span class="ff-num" data-ff-el="receiptEmail">—</span>
                </p>
              </section>

              <section class="ff-mini ff-mini--premium ff-railCard" aria-label="Checkout trust" data-ff-el="trustCard">
                <div class="ff-row ff-row--between ff-wrap ff-ais">
                  <div class="ff-minw-0">
                    <div class="ff-kicker">Trust</div>
                    <div class="ff-label">Secure by design</div>
                    <p class="ff-help ff-muted">Encrypted checkout • No account required • Receipt in seconds</p>
                  </div>
                  <button
                    class="ff-btn ff-btn--ghost ff-btn--sm"
                    type="button"
                    data-ff-modal-open="policyModal"
                    aria-haspopup="dialog"
                    data-ff-cta="donate-trust-rail"
                  >
                    Trust center
                  </button>
                </div>

                <div class="ff-row ff-wrap ff-mt-2" aria-label="Trust indicators">
                  <span class="ff-pill">Encrypted</span>
                  <span class="ff-pill">Receipt email</span>
                  <span class="ff-pill">Refund support</span>
                </div>

                {% if not _whitelabel %}
                  <p class="ff-help ff-muted ff-mt-2">
                    Powered by <strong class="ff-num">{{ _ff_name|default('FutureFunded')|e }}</strong>
                  </p>
                {% endif %}
              </section>

              <div class="ff-checkoutbar ff-mt-2" role="group" aria-label="Checkout actions">
                <button
                  class="ff-btn ff-btn--primary ff-btn--lg ff-w-100"
                  type="submit"
                  data-ff-el="payBtn"
                  data-ff-cta="donate-submit"
                >
                  Continue to payment
                </button>

                <p class="ff-help ff-muted ff-mt-2">
                  By donating, you agree to the campaign’s terms and privacy policy.
                  <button
                    class="ff-btn ff-btn--ghost ff-btn--sm"
                    type="button"
                    data-ff-modal-open="policyModal"
                    aria-haspopup="dialog"
                    data-ff-cta="donate-terms-view"
                  >
                    View
                  </button>
                </p>
              </div>
            </div>
          </aside>
        </form>
      </div>
    </article>
  </div>
</section>
<!-- /DONATE -->

  <!-- ===================== FAQ / TRUST ===================== -->
<section
  id="faq"
  class="ff-section"
  data-ff-section="faq"
  tabindex="-1"
  aria-labelledby="faqTitle"
>
  <div class="ff-container">
    <article class="ff-card ff-card--lift ff-card--premium ff-pad ff-glass" data-ff-faq="true">
      <header class="ff-sectionhead">
        <div class="ff-sectionhead__text ff-minw-0">
          <div class="ff-kicker">FAQ</div>
          <h2 id="faqTitle" class="ff-h2">Quick answers</h2>
          <p class="ff-lead">Straightforward info for supporters — less hesitation, more confidence.</p>

          <div class="ff-row ff-wrap ff-ais ff-mt-2" aria-label="FAQ highlights">
            <span class="ff-pill">Stripe-secured</span>
            <span class="ff-pill">Instant receipt</span>
            <span class="ff-pill">Anonymous option</span>
          </div>
        </div>

        <div class="ff-sectionhead__actions" role="group" aria-label="FAQ actions">
          <button
            class="ff-btn ff-btn--ghost ff-btn--sm"
            type="button"
            data-ff-modal-open="shareModal"
            aria-haspopup="dialog"
            data-ff-cta="faq-share"
          >
            Share
          </button>

          <button
            class="ff-btn ff-btn--ghost ff-btn--sm"
            type="button"
            data-ff-modal-open="policyModal"
            aria-haspopup="dialog"
            data-ff-cta="faq-trust"
          >
            Trust
          </button>

          <a class="ff-btn ff-btn--primary ff-btn--sm" href="#donate" data-ff-cta="faq-donate">
            Donate
          </a>
        </div>
      </header>

      <div class="ff-divider ff-my-3" aria-hidden="true"></div>

      <div class="ff-faqDash" data-ff-el="faqDash">
        <!-- Main FAQ items -->
        <section class="ff-faqDash__main" aria-label="FAQ items" data-ff-el="faqItems">
          <details class="ff-disc ff-disc--premium" open>
            <summary class="ff-disc__sum">
              <span>
                <span class="ff-kicker">Is this secure?</span><br />
                <span class="ff-help">Yes — Stripe-secured checkout</span>
              </span>
              <span class="ff-pill" aria-hidden="true">Open</span>
            </summary>

            <div class="ff-disc__body" role="region" tabindex="-1">
              <ul class="ff-bul">
                <li>Payments are processed via Stripe and encrypted in transit.</li>
                <li>Receipts are emailed instantly after successful payment.</li>
                <li>No account or login required.</li>
              </ul>
            </div>
          </details>

          <details class="ff-disc ff-disc--premium">
            <summary class="ff-disc__sum">
              <span>
                <span class="ff-kicker">Can I donate anonymously?</span><br />
                <span class="ff-help">Yes — toggle “anonymous” at checkout</span>
              </span>
              <span class="ff-pill" aria-hidden="true">Open</span>
            </summary>

            <div class="ff-disc__body" role="region" tabindex="-1">
              <ul class="ff-bul">
                <li>Check “Donate anonymously” to hide your name from public lists.</li>
                <li>Your receipt still arrives by email instantly.</li>
                <li>You can donate without selecting a team or sponsor tier.</li>
              </ul>

              <p class="ff-help ff-muted ff-mt-2">
                Anonymous hides your name publicly, not from organizer/payment processor records.
              </p>
            </div>
          </details>

          <details class="ff-disc ff-disc--premium">
            <summary class="ff-disc__sum">
              <span>
                <span class="ff-kicker">Do you offer refunds?</span><br />
                <span class="ff-help">Campaign policy may apply</span>
              </span>
              <span class="ff-pill" aria-hidden="true">Open</span>
            </summary>

            <div class="ff-disc__body" role="region" tabindex="-1">
              <p class="ff-help">
                Refund policies vary by campaign. For help, email:
                <a href="mailto:{{ _email|e }}" data-ff-el="supportEmailLink">{{ _email|e }}</a>
              </p>

              <p class="ff-help ff-muted ff-mt-2">
                Include your receipt email and approximate donation time so support can help faster.
              </p>
            </div>
          </details>
        </section>

        <!-- Rail: help + trust -->
        <aside class="ff-faqDash__rail" aria-label="Help and trust" data-ff-el="faqRail">
          <section class="ff-mini ff-mini--premium">
            <div class="ff-kicker">Need help?</div>
            <div class="ff-label">Support in one step</div>
            <p class="ff-help ff-muted">Email support with your receipt email + approximate donation time.</p>

            <div class="ff-row ff-wrap ff-mt-2" role="group" aria-label="Support actions">
              <a class="ff-btn ff-btn--secondary" href="mailto:{{ _email|e }}" data-ff-cta="faq-email-support">
                Email support
              </a>

              <button
                class="ff-btn ff-btn--ghost"
                type="button"
                data-ff-modal-open="policyModal"
                aria-haspopup="dialog"
                data-ff-cta="faq-trust-rail"
              >
                Trust center
              </button>

              <button
                class="ff-btn ff-btn--ghost"
                type="button"
                data-ff-copy-link="true"
                data-ff-cta="faq-copy"
              >
                Copy link
              </button>
            </div>

            <div class="ff-divider ff-my-2" aria-hidden="true"></div>

            <div class="ff-kicker">Share</div>
            <p class="ff-help ff-muted">Send the link to a sponsor, parent group, or team chat.</p>

            <div class="ff-row ff-wrap ff-mt-2" role="group" aria-label="Share tools">
              <button
                class="ff-btn ff-btn--ghost"
                type="button"
                data-ff-modal-open="shareModal"
                aria-haspopup="dialog"
                data-ff-cta="faq-share-rail"
              >
                Share tools
              </button>

              <a class="ff-btn ff-btn--primary" href="#donate" data-ff-cta="faq-donate-rail">
                Donate
              </a>
            </div>
          </section>
        </aside>
      </div>
    </article>
  </div>
</section>
<!-- /FAQ -->
  
  </main>
  
<!-- ===================== FOOTER ===================== -->
<footer
  class="ff-footer"
  role="contentinfo"
  data-ff-section="footer"
  data-ff-footer="true"
  aria-labelledby="ffFooterLabel"
>
  <div class="ff-container">
    <span id="ffFooterLabel" class="ff-sr">Footer</span>

    <!-- CTA -->
    <section
      aria-label="Footer call to action"
      id="ffFooterCta"
      class="ff-footer__cta"
      data-ff-footer-cta="true"
    >
      <div class="ff-card ff-card--premium ff-glass ff-pad" role="region" aria-labelledby="ffCtaTitle">
        <div class="ff-row ff-row--between ff-wrap ff-ais">
          <div class="ff-minw-0">
            <div class="ff-kicker">Ready to help?</div>
            <div id="ffCtaTitle" class="ff-footerCta__title">Donate in under a minute.</div>
            <p class="ff-help ff-muted">Secure checkout • instant receipt • no account required</p>

            <div class="ff-row ff-wrap ff-mt-2" aria-label="Accepted payment methods">
              <span class="ff-pill ff-pill--sm">Visa</span>
              <span class="ff-pill ff-pill--sm">Mastercard</span>
              <span class="ff-pill ff-pill--sm">AmEx</span>
              <span class="ff-pill ff-pill--sm">Apple Pay</span>
              <span class="ff-pill ff-pill--sm">Google Pay</span>
            </div>

            {% if not _whitelabel %}
              <p class="ff-help ff-muted ff-mt-2 ff-mb-0">
                Hosted on
                <a href="{{ _ff_home|e }}" target="_blank" rel="noopener noreferrer">
                  {{ _ff_name|default('FutureFunded')|e }}
                </a>.
              </p>
            {% endif %}
          </div>

          <div class="ff-row ff-wrap" role="group" aria-label="Footer CTA actions">
            <a class="ff-btn ff-btn--primary" href="#donate" data-ff-cta="footer-donate">Donate</a>

            <button
              class="ff-btn ff-btn--secondary"
              type="button"
              data-ff-modal-open="shareModal"
              aria-haspopup="dialog"
              aria-controls="ffShareModal"
              data-ff-cta="footer-share"
            >
              Share
            </button>

            <button
              class="ff-iconbtn ff-ml-2"
              type="button"
              data-ff-footer-cta-dismiss="true"
              aria-label="Dismiss donate prompt"
              title="Dismiss"
            >
              ✕
            </button>
          </div>
        </div>
      </div>
    </section>

    <!-- Footer grid -->
    <div class="ff-footer__grid" aria-label="Footer content">

      <!-- Org panel -->
      <section class="ff-footer__panel ff-stack" aria-label="Organization">
        <a class="ff-brand" href="#top" aria-label="Back to top">
          <span class="ff-brand__mark" aria-hidden="true">
            <picture>
              <source type="image/svg+xml" srcset="{{ _org_logo_svg|default(_org_logo)|e }}">
              <img
                class="ff-logo"
                src="{{ _org_logo|e }}"
                alt="{{ (_org_name ~ ' logo')|e }}"
                width="46"
                height="46"
                loading="lazy"
                decoding="async"
              />
            </picture>
          </span>

          <span class="ff-brand__meta">
            <span class="ff-brand__title" data-ff-el="footerOrgName">{{ _org_name|e }}</span>
            <span class="ff-brand__sub" data-ff-el="footerOrgLocation">{{ _org_location|e }}</span>
          </span>
        </a>

        <p class="ff-help ff-maxw-62 ff-m-0">
          Built for supporters: secure checkout, instant receipts, sponsor recognition, and share-ready tools.
        </p>

        <div class="ff-row ff-wrap ff-ais ff-mt-2" role="group" aria-label="Trust indicators">
          <span class="ff-pill ff-pill--verified"><span aria-hidden="true">✓</span> Instant receipt</span>
          <span class="ff-pill ff-pill--accent"><span class="ff-dot" aria-hidden="true"></span> Live</span>
          <span class="ff-pill">No login</span>
        </div>

        <div class="ff-footer__rule" aria-hidden="true"></div>

        <div class="ff-row ff-wrap ff-ais" role="group" aria-label="Footer actions">
          <button
            class="ff-btn ff-btn--ghost ff-btn--sm"
            type="button"
            data-ff-modal-open="policyModal"
            aria-haspopup="dialog"
            data-ff-cta="footer-trust"
          >
            Trust center
          </button>

          <button
            class="ff-btn ff-btn--ghost ff-btn--sm"
            type="button"
            data-ff-modal-open="proofModal"
            aria-haspopup="dialog"
            data-ff-cta="footer-proof"
          >
            Proof kit
          </button>

          <button
            class="ff-btn ff-btn--ghost ff-btn--sm"
            type="button"
            data-ff-copy-link="true"
            data-ff-cta="footer-copy"
          >
            Copy link
          </button>
        </div>

        {% if not _whitelabel %}
          <p class="ff-help ff-muted ff-mt-2 ff-mb-0">
            Powered by
            <a href="{{ _ff_home|e }}" target="_blank" rel="noopener noreferrer">
              <strong>{{ _ff_name|default('FutureFunded')|e }}</strong>
            </a>
          </p>
        {% endif %}
      </section>

      <!-- Explore nav -->
      <nav class="ff-footer__panel" aria-label="Explore">
        <div class="ff-kicker">Explore</div>
        <ul class="ff-footer__linkgrid" aria-label="Footer links">
          <li><a href="#progress">Progress</a></li>
          <li><a href="#impact">Impact</a></li>
          <li><a href="#sponsors">Sponsors</a></li>
          <li><a href="#teams">Teams</a></li>
          <li><a href="#donate">Donate</a></li>
          <li><a href="#faq">FAQ</a></li>
        </ul>

        <div class="ff-footer__rule" aria-hidden="true"></div>

        <div class="ff-stack" role="group" aria-label="Quick tools">
          <button
            class="ff-btn ff-btn--ghost ff-btn--sm ff-justify-start"
            type="button"
            data-ff-modal-open="shareModal"
            aria-haspopup="dialog"
            data-ff-cta="footer-share-tools"
          >
            Share tools
          </button>

          <button
            class="ff-btn ff-btn--ghost ff-btn--sm ff-justify-start"
            type="button"
            data-ff-copy-link="true"
            data-ff-cta="footer-copy-link"
          >
            Copy fundraiser link
          </button>
        </div>
      </nav>

      <!-- Support panel -->
      <section class="ff-footer__panel ff-stack" aria-label="Support">
        <div class="ff-kicker">Support</div>
        <div class="ff-label ff-mt-1">Help in one step</div>

        <div class="ff-kpi" aria-label="Support details">
          <div class="ff-kpi__row">
            <div class="ff-kpi__name">Email</div>
            <div class="ff-kpi__val">
              <a href="mailto:{{ _email|e }}" data-ff-el="supportEmail">{{ _email|e }}</a>
            </div>
          </div>

          <div class="ff-kpi__row">
            <div class="ff-kpi__name">Receipts</div>
            <div class="ff-kpi__val">Instant email delivery</div>
          </div>

          {% if not _whitelabel %}
            <div class="ff-kpi__row">
              <div class="ff-kpi__name">Platform</div>
              <div class="ff-kpi__val">
                <a href="{{ _ff_home|e }}" target="_blank" rel="noopener noreferrer">
                  {{ _ff_name|default('FutureFunded')|e }}
                </a>
              </div>
            </div>
          {% endif %}
        </div>

        <p class="ff-help ff-muted ff-mt-2 ff-mb-0">
          Tip: if your receipt doesn’t show up, check Promotions/Spam, then email support.
        </p>

        <div class="ff-footer__rule" aria-hidden="true"></div>

        <div class="ff-row ff-row--between ff-wrap ff-ais" aria-label="Footer legal">
          <p class="ff-help ff-muted ff-m-0">
            © <span data-ff-el="footerYear">{{ _year|default(2026) }}</span> {{ _ff_name|default('FutureFunded')|e }}. All rights reserved.
          </p>

          <a class="ff-btn ff-btn--ghost ff-btn--sm ff-justify-start" href="#top" aria-label="Back to top">
            Back to top ↑
          </a>
        </div>
      </section>

    </div>
  </div>
</footer>
<!-- /FOOTER -->

<!-- ===================== MOBILE DRAWER ===================== -->
<div
  class="ff-drawer"
  id="mobileDrawer"
  role="dialog"
  aria-modal="true"
  aria-labelledby="drawerTitle"
  aria-describedby="drawerDesc"
  aria-hidden="true"
  hidden
  data-ff-drawer="true"
>
  <button
    class="ff-drawer__backdrop"
    type="button"
    data-ff-drawer-close="true"
    aria-label="Close menu overlay"
  ></button>

  <div class="ff-drawer__panel" tabindex="-1" role="document" data-ff-drawer-panel="true">
    <header class="ff-drawer__head">
      <div>
        <div class="ff-kicker" id="drawerTitle">Menu</div>
        <p class="ff-help" id="drawerDesc">Jump fast • share tools • trust.</p>
      </div>

      <div class="ff-drawer__head-actions">
        <button class="ff-iconbtn" type="button" data-ff-drawer-close="true" aria-label="Close menu">
          <span aria-hidden="true">✕</span>
        </button>
      </div>
    </header>

    <main class="ff-drawer__body" role="region" aria-label="Menu content">
      <section aria-label="Organization summary">
        <div class="ff-mini ff-mini--premium">
          <div class="ff-row ff-ais">
            <img
              src="{{ _org_logo|e }}"
              alt="{{ _org_name|e }} logo"
              width="44"
              height="44"
              loading="lazy"
              decoding="async"
              class="ff-drawer__orglogo"
            />
            <div class="ff-minw-0">
              <div class="ff-label">{{ _org_name|e }}</div>
              <div class="ff-help ff-muted">{{ _org_location|e }}</div>
            </div>
          </div>

          <div class="ff-row ff-wrap ff-mt-2" aria-label="Trust indicators">
            <span class="ff-pill ff-pill--verified"><span aria-hidden="true">✓</span> Verified</span>
            <span class="ff-pill">Instant receipt</span>
            <span class="ff-pill">Secure checkout</span>
          </div>
        </div>
      </section>

      <div class="ff-divider ff-my-2" aria-hidden="true"></div>

      <section aria-label="Quick actions">
        <div class="ff-row ff-wrap" role="group" aria-label="Quick actions">
          <button
            class="ff-btn ff-btn--secondary"
            type="button"
            data-ff-modal-open="shareModal"
            aria-haspopup="dialog"
            aria-controls="ffShareModal"
          >
            Share
          </button>

          <button class="ff-btn ff-btn--ghost" type="button" data-ff-copy-link="true">
            Copy link
          </button>

          <button
            class="ff-btn ff-btn--ghost"
            type="button"
            data-ff-modal-open="policyModal"
            aria-haspopup="dialog"
            aria-controls="ffPolicyModal"
          >
            Trust center
          </button>

          <a class="ff-btn ff-btn--primary" href="#donate" data-ff-cta="drawer-donate" data-ff-drawer-close="true">
            Donate
          </a>
        </div>
      </section>

      <div class="ff-divider ff-my-2" aria-hidden="true"></div>

      <nav class="ff-drawer__list" aria-label="Drawer links">
        <a class="ff-drawer__link" href="#progress" data-ff-drawer-close="true">Progress <span aria-hidden="true">→</span></a>
        <a class="ff-drawer__link" href="#impact" data-ff-drawer-close="true">Impact <span aria-hidden="true">→</span></a>
        <a class="ff-drawer__link" href="#sponsors" data-ff-drawer-close="true">Sponsors <span aria-hidden="true">→</span></a>
        <a class="ff-drawer__link" href="#teams" data-ff-drawer-close="true">Teams <span aria-hidden="true">→</span></a>
        <a class="ff-drawer__link" href="#faq" data-ff-drawer-close="true">FAQ <span aria-hidden="true">→</span></a>
      </nav>

      <div class="ff-divider ff-my-2" aria-hidden="true"></div>

      <section aria-label="Support and platform">
        <div class="ff-kicker">Support</div>
        <p class="ff-help ff-muted">Need help? Email <a href="mailto:{{ _email|e }}">{{ _email|e }}</a></p>

        {% if not _whitelabel %}
          <div class="ff-divider ff-my-2" aria-hidden="true"></div>
          <div class="ff-kicker">Platform</div>
          <p class="ff-help ff-muted">
            Hosted on
            <a href="{{ _ff_home|e }}" target="_blank" rel="noopener noreferrer" data-ff-poweredby="true">
              <strong>{{ _ff_name|default('FutureFunded')|e }}</strong>
            </a>.
          </p>
        {% endif %}

        <div class="ff-row ff-row--between ff-ais ff-mt-2">
          <button class="ff-btn ff-btn--ghost ff-btn--sm" type="button" data-ff-drawer-close="true">Close</button>
          <a class="ff-btn ff-btn--ghost ff-btn--sm" href="#top" data-ff-drawer-close="true">Back to top ↑</a>
        </div>
      </section>
    </main>
  </div>
</div>
<!-- /MOBILE DRAWER -->


<!-- ===================== SHARE MODAL ===================== -->
<div
  class="ff-modal"
  id="ffShareModal"
  role="dialog"
  aria-modal="true"
  aria-labelledby="shareTitle"
  aria-hidden="true"
  hidden
  data-ff-modal="shareModal"
>
  <button class="ff-modal__backdrop" type="button" data-ff-modal-close="true" aria-label="Close share modal"></button>

  <div class="ff-modal__panel" tabindex="-1" role="document" aria-describedby="shareDesc">
    <header class="ff-modal__head">
      <div>
        <div class="ff-modal__title" id="shareTitle">Share this fundraiser</div>
        <p class="ff-help ff-muted ff-mt-1" id="shareDesc">Copy the link, share to sponsors, or drop the QR in group chats.</p>
      </div>
      <button class="ff-iconbtn" type="button" data-ff-modal-close="true" aria-label="Close">
        <span aria-hidden="true">✕</span>
      </button>
    </header>

    <div class="ff-divider ff-my-2" aria-hidden="true"></div>

    <section class="ff-grid ff-grid--2" aria-label="Share options">
      <div class="ff-mini ff-mini--premium">
        <div class="ff-kicker">Link</div>
        <div class="ff-inputrow ff-mt-2">
          <span class="ff-inputrow__prefix" aria-hidden="true">🔗</span>
          <input class="ff-input" type="text" readonly data-ff-share-url aria-label="Share link input" />
        </div>
        <div class="ff-row ff-wrap ff-mt-2" role="group" aria-label="Link actions">
          <button class="ff-btn ff-btn--primary ff-btn--sm" type="button" data-ff-copy-link="true">Copy link</button>
          <button class="ff-btn ff-btn--ghost ff-btn--sm" type="button" data-ff-native-share="true">Share…</button>
        </div>
        <p class="ff-help ff-muted ff-mt-2 ff-mb-0">Works great for sponsor emails + team chats.</p>
      </div>

      <div class="ff-mini ff-mini--premium">
        <div class="ff-kicker">QR</div>
        <div class="ff-row ff-wrap ff-ais ff-mt-2">
          <img class="ff-qr" data-ff-qr="modal" alt="Scan to donate QR code" width="140" height="140" loading="lazy" decoding="async" />
          <div class="ff-minw-0">
            <div class="ff-label">Scan to donate</div>
            <p class="ff-help ff-muted ff-m-0">Fast mobile checkout.</p>
            <div class="ff-row ff-wrap ff-mt-2" role="group">
              <button class="ff-btn ff-btn--ghost ff-btn--sm" type="button" data-ff-qr-download="true">Download</button>
            </div>
          </div>
        </div>
      </div>
    </section>

    <div class="ff-divider ff-my-2" aria-hidden="true"></div>

    <section class="ff-mini ff-mini--premium" aria-label="Sponsor message">
      <div class="ff-kicker">Sponsor-ready</div>
      <div class="ff-label">Copy/paste message</div>
      <textarea class="ff-input ff-mt-2" readonly data-ff-share-blurb rows="3">Hi — we’re raising funds for {{ _org_name|e }}. Can you help sponsor? Donate here: </textarea>
      <div class="ff-row ff-wrap ff-mt-2" role="group">
        <button class="ff-btn ff-btn--primary ff-btn--sm" type="button" data-ff-copy-blurb="true">Copy message</button>
        <button class="ff-btn ff-btn--ghost ff-btn--sm" type="button" data-ff-modal-close="true">Done</button>
      </div>
    </section>
  </div>
</div>
<!-- /SHARE MODAL -->


<!-- ===================== POLICY MODAL ===================== -->
<div
  class="ff-modal ff-modal--compact"
  id="ffPolicyModal"
  role="dialog"
  aria-modal="true"
  aria-labelledby="policyTitle"
  aria-hidden="true"
  hidden
  data-ff-modal="policyModal"
>
  <button class="ff-modal__backdrop" type="button" data-ff-modal-close="true" aria-label="Close policy modal"></button>

  <div class="ff-modal__panel" tabindex="-1" role="document" aria-describedby="policyDesc">
    <header class="ff-modal__head">
      <div>
        <div class="ff-modal__title" id="policyTitle">Trust center</div>
        <p class="ff-help ff-muted ff-mt-1" id="policyDesc">Security, receipts, and support — all in one place.</p>
      </div>
      <button class="ff-iconbtn" type="button" data-ff-modal-close="true" aria-label="Close">
        <span aria-hidden="true">✕</span>
      </button>
    </header>

    <div class="ff-divider ff-my-2" aria-hidden="true"></div>

    <section class="ff-stack" aria-label="Policy content" role="region">
      <div class="ff-mini ff-mini--premium">
        <div class="ff-kicker">Security</div>
        <p class="ff-help ff-muted ff-mt-1 ff-mb-0">
          Payments are encrypted and processed by Stripe (and PayPal when enabled). No account required.
        </p>
      </div>

      <div class="ff-mini ff-mini--premium">
        <div class="ff-kicker">Receipts</div>
        <p class="ff-help ff-muted ff-mt-1 ff-mb-0">
          Receipts are emailed instantly after successful payment.
        </p>
      </div>

      <div class="ff-mini ff-mini--premium">
        <div class="ff-kicker">Refund support</div>
        <p class="ff-help ff-muted ff-mt-1 ff-mb-0" data-ff-policy-blurb>{{ _policy_blurb|e }}</p>
        <p class="ff-help ff-muted ff-mt-2 ff-mb-0">
          Need help? Email <a href="mailto:{{ _email|e }}">{{ _email|e }}</a>.
        </p>
      </div>
    </section>

    <div class="ff-row ff-wrap ff-mt-3" role="group" aria-label="Policy actions">
      <button class="ff-btn ff-btn--primary" type="button" data-ff-modal-close="true">Got it</button>
      <button class="ff-btn ff-btn--ghost" type="button" data-ff-copy-link="true">Copy fundraiser link</button>
    </div>
  </div>
</div>
<!-- /POLICY MODAL -->


<!-- ===================== PROOF MODAL ===================== -->
<div
  class="ff-modal"
  id="ffProofModal"
  role="dialog"
  aria-modal="true"
  aria-labelledby="proofTitle"
  aria-hidden="true"
  hidden
  data-ff-modal="proofModal"
>
  <button class="ff-modal__backdrop" type="button" data-ff-modal-close="true" aria-label="Close proof modal"></button>

  <div class="ff-modal__panel" tabindex="-1" role="document" aria-describedby="proofDesc">
    <header class="ff-modal__head">
      <div>
        <div class="ff-modal__title" id="proofTitle">Proof kit</div>
        <p class="ff-help ff-muted ff-mt-1" id="proofDesc">Optional artifacts that increase donor confidence.</p>
      </div>
      <button class="ff-iconbtn" type="button" data-ff-modal-close="true" aria-label="Close">
        <span aria-hidden="true">✕</span>
      </button>
    </header>

    <div class="ff-divider ff-my-2" aria-hidden="true"></div>

    <section class="ff-grid ff-grid--2" aria-label="Proof kit content" role="region">
      <div class="ff-mini ff-mini--premium">
        <div class="ff-kicker">What this is</div>
        <p class="ff-help ff-muted ff-mt-1 ff-mb-0" data-ff-proof-blurb>{{ _proof_blurb|e }}</p>
        <div class="ff-row ff-wrap ff-mt-2" role="group">
          <button class="ff-btn ff-btn--ghost ff-btn--sm" type="button" data-ff-copy-link="true">Copy link</button>
          <button
            class="ff-btn ff-btn--secondary ff-btn--sm"
            type="button"
            data-ff-modal-open="shareModal"
            aria-haspopup="dialog"
            aria-controls="ffShareModal"
          >
            Share
          </button>
        </div>
      </div>

      <div class="ff-mini ff-mini--premium">
        <div class="ff-kicker">Sponsor packet</div>
        <p class="ff-help ff-muted ff-mt-1 ff-mb-0">Provide a PDF (admin) to enable one-click downloads for sponsors.</p>

        {% set packet = sponsor_packet_url|default('', true)|trim %}
        <a
          class="ff-btn ff-btn--primary ff-btn--sm ff-mt-2"
          href="{{ packet|e if packet else '#' }}"
          data-ff-packet-download="true"
          {% if not packet %}aria-disabled="true" tabindex="-1"{% endif %}
          target="_blank"
          rel="noopener noreferrer"
        >
          {% if packet %}Download packet{% else %}Packet unavailable{% endif %}
        </a>
      </div>
    </section>

    <div class="ff-row ff-wrap ff-mt-3" role="group" aria-label="Proof actions">
      <button class="ff-btn ff-btn--primary" type="button" data-ff-modal-close="true">Done</button>
      <a class="ff-btn ff-btn--ghost" href="#donate" data-ff-modal-close="true" data-ff-cta="proof-donate">Donate</a>
    </div>
  </div>
</div>
<!-- /PROOF MODAL -->

<!-- ===================== GLOBAL OVERLAYS ===================== -->
<!-- Sticky support bar -->
<div class="ff-sticky" id="stickyBar" aria-hidden="true" data-ff-sticky="true">
  <div class="ff-sticky__inner">
    <div class="ff-row ff-wrap ff-ais" aria-label="Sticky fundraiser totals">
      <div class="ff-stack ff-stack--tight">
        <span class="ff-kicker">Raised</span>
        <strong class="ff-num" id="stickyRaised" data-ff-money="raised">{{ _raised_amount }}</strong>
      </div>

      <div class="ff-stack ff-stack--tight">
        <span class="ff-kicker">Goal</span>
        <strong class="ff-num" id="stickyGoal" data-ff-money="goal">{{ _goal_amount }}</strong>
      </div>

      <div class="ff-stack ff-stack--tight" hidden data-ff-premium="match">
        <span class="ff-kicker">Match</span>
        <strong class="ff-num" id="stickyMatch" data-ff-match="state">—</strong>
      </div>
    </div>

    <div class="ff-row ff-wrap ff-aic" aria-label="Sticky actions">
      <button
        class="ff-btn ff-btn--secondary"
        type="button"
        data-ff-modal-open="shareModal"
        aria-haspopup="dialog"
        aria-controls="ffShareModal"
      >
        Share
      </button>
      <a class="ff-btn ff-btn--primary" href="#donate" data-ff-cta="sticky-donate">Donate</a>
    </div>
  </div>
</div>

<!-- Back to top -->
<a class="ff-backtotop ff-backtotop--v14_2_1" href="#top" aria-label="Back to top" data-ff-backtotop="true">↑</a>

<!-- Mobile tabs -->
<nav class="ff-tabs" aria-label="Quick navigation" data-ff-tabs="true">
  <ul class="ff-tabs__bar">
    <li><a class="ff-tab" href="#progress" data-ff-tab="progress" aria-label="Progress"><span class="ff-tab__icon" aria-hidden="true">▦</span><span class="ff-tab__label">Progress</span></a></li>
    <li><a class="ff-tab" href="#sponsors" data-ff-tab="sponsors" aria-label="Sponsors"><span class="ff-tab__icon" aria-hidden="true">◎</span><span class="ff-tab__label">Sponsors</span></a></li>
    <li><a class="ff-tab" href="#teams" data-ff-tab="teams" aria-label="Teams"><span class="ff-tab__icon" aria-hidden="true">▣</span><span class="ff-tab__label">Teams</span></a></li>
    <li><a class="ff-tab" href="#donate" data-ff-tab="donate" aria-label="Donate"><span class="ff-tab__icon" aria-hidden="true">↳</span><span class="ff-tab__label">Donate</span></a></li>
  </ul>
</nav>

</div><!-- /ff-shell -->
</body>
</html>
