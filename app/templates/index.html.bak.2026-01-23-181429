<!doctype html>
<html
  lang="en"
  data-theme="dark"
  data-ff-build="flagship-v15"
  data-ff-compat="compat-first"
>
<head>
  <!-- ============================================================
    FutureFunded • Flagship v15 — PRODUCTION HEAD (CLEAN / DEDUPED)
    Principles:
    - ONE config script: #ffConfig (application/json)
    - ONE ff-app loader script
    - Stripe.js loaded ONCE (by ff-app.js via meta ff-stripe-js)
    - Payment wiring owned by ff-app.js (no inline boot scripts)
  ============================================================ -->

  <!-- Core -->
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width,initial-scale=1,viewport-fit=cover,maximum-scale=5" />
  <meta name="format-detection" content="telephone=no,date=no,address=no,email=no" />
  <meta name="referrer" content="strict-origin-when-cross-origin" />
  <meta name="color-scheme" content="dark light" />
  <meta name="robots" content="index,follow,max-image-preview:large" />
  <meta name="generator" content="FutureFunded Flagship v15" />

  <!-- Permissions policy (meta form). Prefer setting as an HTTP response header at CDN/proxy.
       NOTE: intentionally NOT setting `payment=(...)` here to avoid Stripe Elements edge cases. -->
  <meta name="permissions-policy" content="camera=(), microphone=(), geolocation=()" />

  <!-- SEO -->
  {% set page_title_final = page_title|default('FutureFunded • Fundraiser')|trim %}
  {% set page_desc_final  = page_description|default('Fuel the season. Fund the future.')|trim %}
  {% set canonical        = canonical_url|default(request.base_url)|trim %}
  {% set org_name_final   = org_name|default('FutureFunded')|trim %}

  <title>{{ page_title_final|e }}</title>
  <meta name="description" content="{{ page_desc_final|e }}" />
  <link rel="canonical" href="{{ canonical }}" />
  <link rel="alternate" hreflang="en" href="{{ canonical }}" />
  <link rel="alternate" hreflang="x-default" href="{{ canonical }}" />

  <!-- Social -->
  {% set og_title_final  = og_title|default(page_title_final)|trim %}
  {% set og_desc_final   = og_description|default(page_desc_final)|trim %}
  {% set _og_img_webp    = og_image|default(url_for('static', filename='images/og.webp', _external=True)) %}
  {% set _og_img_jpg     = og_image_jpg|default(url_for('static', filename='images/og.jpg', _external=True)) %}
  {% set _og_alt         = og_image_alt|default('Fundraiser preview')|trim %}

  <meta property="og:type" content="website" />
  <meta property="og:url" content="{{ canonical }}" />
  <meta property="og:site_name" content="{{ org_name_final|e }}" />
  <meta property="og:locale" content="en_US" />
  <meta property="og:title" content="{{ og_title_final|e }}" />
  <meta property="og:description" content="{{ og_desc_final|e }}" />

  <meta property="og:image" content="{{ _og_img_webp }}" />
  <meta property="og:image:secure_url" content="{{ _og_img_webp }}" />
  <meta property="og:image:type" content="image/webp" />
  <meta property="og:image:alt" content="{{ _og_alt|e }}" />
  <meta property="og:image:width" content="{{ og_image_width|default(1200) }}" />
  <meta property="og:image:height" content="{{ og_image_height|default(630) }}" />

  <meta property="og:image" content="{{ _og_img_jpg }}" />
  <meta property="og:image:secure_url" content="{{ _og_img_jpg }}" />
  <meta property="og:image:type" content="image/jpeg" />

  {% if page_modified_iso|default('') %}
    <meta property="og:updated_time" content="{{ page_modified_iso }}" />
  {% endif %}

  <meta name="twitter:card" content="summary_large_image" />
  <meta name="twitter:url" content="{{ canonical }}" />
  <meta name="twitter:title" content="{{ og_title_final|e }}" />
  <meta name="twitter:description" content="{{ og_desc_final|e }}" />
  <meta name="twitter:image" content="{{ _og_img_webp }}" />
  <meta name="twitter:image:alt" content="{{ _og_alt|e }}" />
  {% if twitter_site|default('') %}<meta name="twitter:site" content="{{ twitter_site|trim|e }}" />{% endif %}
  {% if twitter_creator|default('') %}<meta name="twitter:creator" content="{{ twitter_creator|trim|e }}" />{% endif %}

  <!-- PWA / Brand -->
  <meta name="theme-color" media="(prefers-color-scheme: light)" content="{{ theme_color_light|default(theme_color|default('#f97316'))|trim }}" />
  <meta name="theme-color" media="(prefers-color-scheme: dark)"  content="{{ theme_color_dark|default('#0b0f17')|trim }}" />
  <meta name="application-name" content="{{ org_name_final|e }}" />
  <meta name="apple-mobile-web-app-capable" content="yes" />
  <meta name="mobile-web-app-capable" content="yes" />
  <meta name="apple-mobile-web-app-title" content="{{ org_name_final|e }}" />
  <meta name="apple-mobile-web-app-status-bar-style" content="black-translucent" />
  <meta name="msapplication-TileColor" content="{{ theme_color|default('#f97316')|trim }}" />
  <meta name="msapplication-config" content="{{ url_for('static', filename='browserconfig.xml') }}" />

  <link rel="icon" href="{{ url_for('static', filename='images/favicon.ico') }}" sizes="any" />
  <link rel="icon" type="image/png" href="{{ url_for('static', filename='images/icon-32.png') }}" sizes="32x32" />
  <link rel="apple-touch-icon" href="{{ url_for('static', filename='images/apple-touch-icon.png') }}" />
  <link rel="manifest" href="{{ url_for('static', filename='manifest.webmanifest') }}" />
  <link rel="mask-icon" href="{{ url_for('static', filename='images/safari-pinned-tab.svg') }}" color="{{ theme_color|default('#f97316')|trim }}" />

  <!-- Runtime / app meta -->
  <meta name="csrf-token" content="{{ csrf_token() }}" />
  <meta name="ff-canonical" content="{{ canonical }}" />

  <!-- Payments meta (contract aligned to payments.py + ff-app.js) -->
  {% set _stripe_pk = (stripe_pk or STRIPE_PUBLISHABLE_KEY or '')|trim %}
  {% set _pp = paypal_client_id|default('')|trim %}
  {% set _qr = qr_endpoint|default('https://api.qrserver.com/v1/create-qr-code/')|trim %}

  <!-- Stripe (ff-app.js will read these) -->
  <meta name="ff-stripe-pk" content="{{ _stripe_pk }}" />
  <meta name="ff-stripe-intent-endpoint" content="/payments/stripe/intent" />
  <meta name="ff-stripe-js" content="https://js.stripe.com/v3/" />
  <!-- Optional but recommended for redirect-based methods -->
  <meta name="ff-stripe-return-url" content="{{ stripe_return_url|default(canonical)|trim }}" />

  <!-- Payments service endpoints -->
  <meta name="ff-payments-config-endpoint" content="/payments/config" />
  <meta name="ff-payments-health-endpoint" content="/payments/health" />

  <!-- PayPal -->
  <meta name="ff-paypal-client-id" content="{{ _pp }}" />
  <meta name="ff-paypal-currency" content="{{ paypal_currency|default('USD')|trim }}" />
  <meta name="ff-paypal-intent" content="{{ paypal_intent|default('capture')|trim }}" />
  <meta name="ff-paypal-create-endpoint" content="{{ paypal_create_endpoint|default('/payments/paypal/order')|trim }}" />
  <meta name="ff-paypal-capture-endpoint" content="{{ paypal_capture_endpoint|default('/payments/paypal/capture')|trim }}" />

  <!-- QR (optional feature) -->
  <meta name="ff-qr-endpoint" content="{{ _qr }}" />

  <!-- Optional behavior switches -->
  <meta name="ff-payments-payload-shape" content="{{ payments_payload_shape|default('nested_donor_v1')|trim }}" />
  <meta name="ff-payments-extra-json" content="{{ payments_extra_json|default('')|trim }}" />
  <meta name="ff-cover-fees-exact" content="{{ 'true' if (COVER_FEES_EXACT|default(false)) else 'false' }}" />
  <!-- Default TRUE unless explicitly overridden -->
  <meta name="ff-require-email" content="{{ 'true' if (REQUIRE_EMAIL|default(true)) else 'false' }}" />
  <!-- Performance hints -->
  {% if _stripe_pk %}
    <link rel="preconnect" href="https://js.stripe.com" crossorigin />
    <link rel="preconnect" href="https://m.stripe.network" crossorigin />
    <link rel="dns-prefetch" href="https://js.stripe.com" />
    <link rel="dns-prefetch" href="https://m.stripe.network" />
  {% endif %}

  {% if _pp %}
    <link rel="preconnect" href="https://www.paypal.com" crossorigin />
    <link rel="preconnect" href="https://www.paypalobjects.com" crossorigin />
    <link rel="dns-prefetch" href="https://www.paypal.com" />
    <link rel="dns-prefetch" href="https://www.paypalobjects.com" />
  {% endif %}

  {% if 'qrserver.com' in _qr %}
    <link rel="preconnect" href="https://api.qrserver.com" crossorigin />
    <link rel="dns-prefetch" href="https://api.qrserver.com" />
  {% endif %}

  <!-- Optional: preload the most-visible hero image (set hero_preload_src to a final URL) -->
  {% if hero_preload_src|default('') %}
    <link rel="preload" as="image" href="{{ hero_preload_src|trim }}" fetchpriority="high" />
  {% endif %}

  <!-- CSS -->
  {% set _css = url_for('static', filename='css/ff.css') ~ '?v=' ~ ((asset_version|default('99'))|trim) %}
  <link rel="preload" as="style" href="{{ _css }}" />
  <link rel="stylesheet" href="{{ _css }}" />

  <!-- ffConfig — SINGLE SOURCE OF TRUTH (JSON ONLY) -->
  <script id="ffConfig" type="application/json"{% if csp_nonce|default('') %} nonce="{{ csp_nonce }}"{% endif %}>
  {
    "org": {
      "name": "Connect ATX Elite",
      "meta": "Youth Basketball • Austin, TX",
      "seasonPill": "Season Fund",
      "sportPill": "Youth program",
      "heroAccentLine": "Help more kids stay on the floor.",
      "footerTagline": "Fuel the season. Fund the future."
    },
    "fundraiser": { "goalAmount": 25000, "deadlineISO": "2026-02-28T23:59:59-06:00" },
    "gallery": {
      "enabled": true,
      "items": [
        { "src": "/static/images/connect-atx-team.jpg", "alt": "Connect ATX Elite", "caption": "Connect ATX Elite" },
        { "src": "/static/images/7thGold.jpg", "alt": "7th Grade Gold", "caption": "7th Grade Gold" },
        { "src": "/static/images/7thBlack.png", "alt": "7th Grade Black", "caption": "7th Grade Black" },
        { "src": "/static/images/8thGold.jpg", "alt": "8th Grade Gold", "caption": "8th Grade Gold" },
        { "src": "/static/images/connect-atx-team_3.jpg", "alt": "8th Grade Black", "caption": "8th Grade Black" }
      ]
    },
    "teams": [
      { "id": "6g", "name": "6th Grade Gold", "meta": "First AAU reps — fundamentals + communication.", "goal": 5000, "raised": 3420, "featured": true,  "needs": false, "restricted": false, "photo": "/static/images/connect-atx-team.jpg", "ask": "Suggested ask: “Help cover tournament fees + gym time this month.”" },
      { "id": "7g", "name": "7th Grade Gold", "meta": "Speed + spacing — film + pressure reps.",         "goal": 6000, "raised": 4680, "featured": true,  "needs": false, "restricted": false, "photo": "/static/images/7thGold.jpg",         "ask": "Suggested ask: “Help fund travel + entry fees for the next weekend.”" },
      { "id": "7b", "name": "7th Grade Black","meta": "Defense travels — stops into transition.",        "goal": 5000, "raised": 2210, "featured": false, "needs": true,  "restricted": false, "photo": "/static/images/7thBlack.png",        "ask": "Suggested ask: “Help with uniforms + team meals on tournament days.”" },
      { "id": "8g", "name": "8th Grade Gold", "meta": "Finish strong — intensity + leadership.",        "goal": 6000, "raised": 4680, "featured": false, "needs": false, "restricted": false, "photo": "/static/images/8thGold.jpg",         "ask": "Suggested ask: “Help with gym rentals + training sessions.”" },
      { "id": "8b", "name": "8th Grade Black","meta": "Next-gym ready — advanced reads + weekends.",    "goal": 5000, "raised": 2210, "featured": false, "needs": true,  "restricted": false, "photo": "/static/images/connect-atx-team_3.jpg","ask": "Suggested ask: “Help with tournament fees and travel support.”" }
    ],
    "flagship": {
      "version": "15.0.0",
      "features": {
        "pwa": true,
        "shareAttribution": true,
        "monthlyGivingPlaceholder": true,
        "trustCenter": true,
        "sponsorPacket": true,
        "milestones": true,
        "impactLadder": true,
        "prestigeSurfaces": true,
        "gallery": true
      },
      "defaults": { "currency": "USD", "locale": "en-US", "timezone": "America/Chicago" },
      "analytics": { "utm": { "enabled": true, "sourceParam": "src", "campaignParam": "cmp" } },
      "accessibility": { "reducedMotionRespect": true, "focusRestore": true, "ariaLiveToasts": true }
    }
  }
  </script>

  <!-- Structured data (SINGLE) -->
  {% set jsonld_default = {
    "@context": "https://schema.org",
    "@graph": [
      { "@type": "Organization", "name": org_name_final, "url": canonical, "logo": url_for("static", filename="images/icon-512.png", _external=True) },
      { "@type": "WebPage", "name": page_title_final, "url": canonical, "description": page_desc_final, "primaryImageOfPage": {"@type":"ImageObject","url": _og_img_webp} }
    ]
  } %}
  {% set jsonld_obj = jsonld if (jsonld is defined and jsonld) else jsonld_default %}
  <script type="application/ld+json"{% if csp_nonce|default('') %} nonce="{{ csp_nonce }}"{% endif %}>
    {{ jsonld_obj|tojson }}
  </script>

  <!-- App loader (SINGLE) -->
  {% set _app = url_for('static', filename='js/ff-app.js') ~ '?v=' ~ ((asset_version|default('99'))|trim) %}
  <link rel="preload" as="script" href="{{ _app }}" />
  <script src="{{ _app }}" defer{% if csp_nonce|default('') %} nonce="{{ csp_nonce }}"{% endif %}></script>

  <!-- Optional: analytics hook without inline JS (ff-app.js can read and init) -->
  {% if analytics_id|default('') %}
    <meta name="ff-analytics-id" content="{{ analytics_id|trim }}" />
  {% endif %}
</head>


<body class="ff-body" data-ff-skin="flagship" data-ff-body="v15.0">
  <!-- ============================================================
    Flagship body (REFACTORED)
    Goals:
    - Keep your existing premium layout intact
    - Ensure ff-app.js has every required mount id/data attribute it expects
    - Ensure Stripe mounts exist: #paymentElement + status/success/error regions
    - Ensure PayPal mounts exist (hidden by default, lazy-loaded)
    - Keep a11y: skip links, aria-live regions, focus targets
  ============================================================ -->

  <!-- Skip links for accessibility -->
  <nav class="ff-skiplinks" aria-label="Skip links">
    <a class="ff-skip" href="#content">Skip to content</a>
    <a class="ff-skip" href="#donate">Skip to donate</a>
    <a class="ff-skip" href="#payBtn">Skip to continue button</a>
  </nav>

  <!-- Top anchor for "back to top" -->
  <div id="top" class="ff-sr" tabindex="-1">Top</div>

  <!-- No-JS notice -->
  <noscript>
    <div class="ff-container">
      <div class="ff-notice ff-notice--noscript" role="alert" aria-live="assertive" aria-atomic="true">
        <div class="ff-kicker">JavaScript required</div>
        <p class="ff-help ff-mt-1">JavaScript is required for secure checkout and dynamic fundraiser features.</p>
        <p class="ff-help ff-mt-2">
          Enable JavaScript or use a modern browser.
          {% set _email = support_email|default('support@getfuturefunded.com') %}
          Contact: <a href="mailto:{{ _email|e }}">{{ _email|e }}</a>
        </p>
      </div>
    </div>
  </noscript>

  <!-- Toast host (for notifications) -->
  <div class="ff-toasts" data-ff-toasts aria-live="polite" aria-atomic="false"></div>

  <!-- Announcement bar -->
  <aside class="ff-announce" role="region" aria-label="Site announcement" data-ff-announcement hidden>
    <div class="ff-container">
      <div class="ff-announce__row">
        <div class="ff-announce__content" aria-live="polite" aria-atomic="true">
          <p class="ff-announce__text" id="announcementText"></p>
        </div>
        <button class="ff-icon btn ghost" type="button" aria-label="Dismiss announcement" data-ff-announcement-dismiss>
          <span aria-hidden="true">✕</span>
        </button>
      </div>
    </div>
  </aside>

  <!-- ============================================================
    SINGLE SHELL (required by ff-app.js)
  ============================================================ -->
  <div
    id="ffShell"
    class="ff-shell"
    data-ff-shell
    data-ff-page="fundraiser"
    data-ff-tier="{{ 'premium' if PREMIUM_MODE else 'standard' }}"
    data-ff-org="{{ org_id|default('default')|trim }}"
    data-ff-campaign="{{ campaign_id|default('season')|trim }}"
    data-ff-env="{{ (config.get('ENV', 'production') if config is mapping else (config or 'production')) | trim }}"
    data-ff-embed="{{ 'true' if EMBED_MODE else 'false' }}"
    data-ff-whitelabel="{{ 'true' if WHITELABEL_MODE else 'false' }}"
    data-ff-version="{{ flagship_version|default('15.0.0')|trim }}"
  >

    <!-- Sticky chrome (topbar + header) -->
    <div class="ff-chrome" data-ff-chrome>
      <!-- TOPBAR: fundraising stats & actions -->
      <section class="ff-topbar" id="ffTopbar" data-ff-topbar role="region" aria-label="Fundraiser status">
        <div class="ff-container">
          <div class="ff-topbar__row">
            <div class="ff-topbar__left" aria-label="Live status">
              <!-- Fundraising totals -->
              <div class="ff-strip" title="Live fundraising totals">
                <span class="ff-pill ff-pill--live" aria-label="Live">
                  <span class="ff-dot" aria-hidden="true"></span>Live
                </span>
                <span class="ff-help">Raised</span>
                <strong class="ff-num" id="topbarRaised" data-ff-raised aria-live="polite" aria-atomic="true">$0</strong>
                <span class="sep" aria-hidden="true">•</span>
                <span class="ff-help">Goal</span>
                <strong class="ff-num" id="topbarGoal" data-ff-goal aria-live="polite" aria-atomic="true">$0</strong>
              </div>

              <!-- Deadline countdown -->
              <div class="ff-strip" title="Deadline countdown" aria-live="polite" aria-atomic="true" data-ff-deadline-strip>
                <span class="ff-pill">Deadline</span>
                <strong class="ff-num" id="topbarDeadline" data-ff-deadline>—</strong>
                <span class="sep" aria-hidden="true">•</span>
                <span class="ff-help">Time left</span>
                <strong class="ff-num" id="topbarCountdown" data-ff-countdown>—</strong>
              </div>

              <!-- Matching status (optional) -->
              <div class="ff-strip" title="Match status" hidden data-ff-match-strip aria-live="polite" aria-atomic="true">
                <span class="ff-pill ff-pill--accent">Match</span>
                <strong class="ff-num" id="topbarMatchText">Active</strong>
                <span class="sep" aria-hidden="true">•</span>
                <span class="ff-help">Remaining</span>
                <strong class="ff-num" id="topbarMatchRemaining">—</strong>
              </div>
            </div>

            <div class="ff-topbar__right" role="group" aria-label="Topbar actions">
              <button class="ff-btn btn ghost ff-btn--sm" type="button" data-ff-share-open aria-label="Open share tools">Share</button>
              <button class="ff-btn btn ghost ff-btn--sm" type="button" data-ff-copy-link aria-label="Copy fundraiser link">Copy link</button>
              <a class="ff-btn btn ff-btn--primary ff-btn--sm" href="#donate" data-ff-donate-cta aria-label="Jump to donate">Donate</a>
              <button class="ff-icon btn ghost" type="button" data-ff-topbar-dismiss aria-label="Dismiss top bar" title="Dismiss">
                <span aria-hidden="true">✕</span>
              </button>
            </div>
          </div>
        </div>
      </section>

      <!-- ============================================================
  HEADER (complete) + MOBILE DRAWER (complete)
  - Matches your IDs: #orgName, #orgMeta, #scrollProgressBar, #mobileDrawer
  - Matches ff-app.js selectors: [data-ff-theme-toggle], [data-ff-drawer-open],
    [data-ff-drawer-close], [data-ff-share-open], [data-ff-copy-link], [data-ff-policy-open]
  - A11y: aria-controls/expanded, dialog semantics, focusable panel
=============================================================== -->

<!-- HEADER -->
<header class="ff-header" data-ff-header>
  <div class="ff-container">
    <div class="ff-header__row">
      <!-- Brand logo + org name -->
      <a class="ff-brand" href="#top" data-ff-brand aria-label="Back to top">
        <span class="ff-brand__mark" aria-hidden="true">
          <img
            class="ff-brand__logo ff-logo"
            id="brandLogo"
            src="{{ org_logo|default(url_for('static', filename='images/logo.webp')) }}"
            alt="{{ org_name|default('Organization') }} logo"
            width="46"
            height="46"
            loading="eager"
            decoding="async"
            fetchpriority="high"
          />
        </span>
        <span class="ff-brand__meta">
          <span class="ff-brand__title" id="orgName" data-ff-org-name>
            {{ org_name|default('Organization') }}
          </span>
          <span class="ff-brand__sub" id="orgMeta" data-ff-org-location>
            {{ org_location|default('City • State') }}
          </span>
        </span>
      </a>

      <!-- Nav -->
      <nav class="ff-nav" aria-label="Primary navigation">
        <a href="#progress" data-spy="progress">Progress</a>
        <a href="#impact" data-spy="impact">Impact</a>
        <a href="#sponsors" data-spy="sponsors">Sponsors</a>
        <a href="#teams" data-spy="teams">Teams</a>
        <a href="#donate" data-spy="donate">Donate</a>
        <a href="#faq" data-spy="faq">FAQ</a>
      </nav>

      <!-- Header actions -->
      <div class="ff-header__actions" role="group" aria-label="Header actions">
        <span
          class="ff-pill ff-pill--verified"
          title="Verified fundraiser"
          aria-label="Verified fundraiser"
        >Verified</span>

        <button
          class="ff-icon btn ghost"
          type="button"
          data-ff-theme-toggle
          data-ff-owner="app"
          aria-pressed="false"
          aria-label="Toggle theme"
          title="Toggle theme"
        >
          <span aria-hidden="true">☾</span>
          <span class="ff-sr">Toggle theme</span>
        </button>

        <button
          class="ff-icon btn ghost"
          type="button"
          data-ff-drawer-open
          aria-controls="mobileDrawer"
          aria-expanded="false"
          aria-label="Open menu"
          title="Open menu"
        >
          <span aria-hidden="true">≡</span>
          <span class="ff-sr">Open menu</span>
        </button>

        <a
          class="ff-btn btn ff-btn--primary ff-btn--sm"
          href="#donate"
          data-ff-donate-cta
          aria-label="Jump to donate"
        >Donate</a>
      </div>
    </div>
  </div>

  <!-- Scroll progress indicator -->
  <div class="ff-scroll" aria-hidden="true">
    <span id="scrollProgressBar"></span>
  </div>
</header>

</div><!-- /.ff-chrome -->

<!-- ============================================================
  MOBILE DRAWER (Navigation, trust & share actions)
  - ff-app.js uses:
    - #mobileDrawer
    - [data-ff-drawer-open]
    - [data-ff-drawer-close]
    - links with a[data-ff-drawer-close] to auto-close on nav
=============================================================== -->
<div
  class="ff-drawer"
  id="mobileDrawer"
  role="dialog"
  aria-modal="true"
  aria-labelledby="drawerTitle"
  aria-describedby="drawerDesc"
  aria-hidden="true"
  hidden
  data-ff-drawer
>
  <button
    class="ff-drawer__backdrop"
    type="button"
    data-ff-drawer-close
    aria-label="Close menu overlay"
  ></button>

  <div class="ff-drawer__panel" tabindex="-1" role="document" data-ff-drawer-panel>
    <header class="ff-row ff-row--between ff-ais">
      <div>
        <div class="ff-kicker" id="drawerTitle">Navigate</div>
        <p class="ff-help" id="drawerDesc">Fast links + share tools.</p>
      </div>
      <button class="ff-icon btn ghost" type="button" data-ff-drawer-close aria-label="Close menu">
        <span aria-hidden="true">✕</span>
      </button>
    </header>

    <div class="ff-divider" aria-hidden="true"></div>

    <nav class="ff-drawer__list" aria-label="Drawer links">
      <a class="ff-drawer__link" href="#progress" data-ff-drawer-close>Progress →</a>
      <a class="ff-drawer__link" href="#impact" data-ff-drawer-close>Impact →</a>
      <a class="ff-drawer__link" href="#sponsors" data-ff-drawer-close>Sponsors →</a>
      <a class="ff-drawer__link" href="#teams" data-ff-drawer-close>Teams →</a>
      <a class="ff-drawer__link" href="#donate" data-ff-drawer-close>Donate →</a>
      <a class="ff-drawer__link" href="#faq" data-ff-drawer-close>FAQ →</a>
    </nav>

    <div class="ff-divider" aria-hidden="true"></div>

    <div class="ff-row ff-wrap" role="group" aria-label="Share actions">
      <button class="ff-btn btn secondary" type="button" data-ff-share-open>Share</button>
      <button class="ff-btn btn ghost" type="button" data-ff-copy-link>Copy link</button>
      <button class="ff-btn btn ghost" type="button" data-ff-policy-open>Trust center</button>
      <button class="ff-btn btn ghost ff-btn--sm" type="button" data-ff-drawer-close>Close</button>
    </div>

    <div class="ff-divider" aria-hidden="true"></div>

    <section class="ff-mini" aria-label="Trust and security">
      <div class="ff-kicker">Trust</div>
      <p class="ff-help">Secure checkout + instant receipts.</p>

      <div class="ff-row ff-wrap" aria-label="Trust indicators">
        <span class="ff-pill ff-pill--verified">Verified</span>
        <span class="ff-pill">Encrypted</span>
        <span class="ff-pill">Receipt email</span>
      </div>

      <details class="ff-disc ff-mt-2">
        <summary class="ff-disc__sum">
          <span class="ff-disc__meta">
            <span class="ff-kicker">After you donate</span><br />
            <span class="ff-help">What happens next</span>
          </span>
          <span class="ff-pill" aria-hidden="true">Open</span>
        </summary>
        <div class="ff-disc__body">
          <ul class="ff-bul">
            <li>Payment confirms in seconds.</li>
            <li>Receipt emails immediately.</li>
            <li>Recognition is optional (anonymous anytime).</li>
          </ul>
        </div>
      </details>

      <div class="ff-divider ff-my-2" aria-hidden="true"></div>

      <div class="ff-help">
        {% set _email = support_email|default('support@getfuturefunded.com') %}
        Support: <a href="mailto:{{ _email|e }}">{{ _email|e }}</a>
      </div>
    </section>
  </div>
</div><!-- /#mobileDrawer -->
      
      <!-- ============================================================
  HERO + PROGRESS (v14.2 polished) — COMPLETE, DROP-IN
  Contracts satisfied:
  - IDs used by ff-app.js: #seasonPill #sportPill #heroOrgLine #heroAccentLine
    #heroCountdown #raisedBig #goalPill #remainingText #deadlineText
    #overallBar #raisedRow #goalRow #pctText #progressQr
    [data-ff-recent-gifts] [data-ff-share-open] [data-ff-copy-link]
    [data-ff-proof-open] [data-ff-policy-open] [data-ff-download-qr] [data-ff-refresh]
  - Matches your “v14.2 polished” structure (left hero card + right rail console)
=============================================================== -->

<section
  class="ff-hero ff-hero--v14 ff-hero--v14_2"
  data-ff-hero
  data-ff-section="hero"
  aria-labelledby="heroTitle"
>
  <div class="ff-hero__bg" aria-hidden="true" data-ff-hero-bg>
    <div class="ff-hero__glow"></div>
    <div class="ff-hero__grid"></div>
    <div class="ff-hero__noise"></div>
    <div class="ff-hero__vignette"></div>
  </div>

  <div class="ff-container">
    <div class="ff-hero__layout ff-hero__layout--v14_2" data-ff-hero-layout>
      <!-- LEFT: HERO CARD -->
      <div class="ff-hero__left" data-ff-hero-left>
        <ul
          class="ff-row ff-wrap ff-hero__pills"
          aria-label="Fundraiser context"
          data-ff-hero-pills
        >
          <li>
            <span class="ff-pill" id="seasonPill" data-ff-season-pill
              >Season Fund</span
            >
          </li>
          <li>
            <span class="ff-pill" id="sportPill" data-ff-sport-pill
              >Youth program</span
            >
          </li>
          <li>
            <span
              class="ff-pill"
              id="heroEventPill"
              hidden
              data-ff-event-pill
              >Next event</span
            >
          </li>
          <li>
            <span
              class="ff-pill ff-pill--accent"
              id="heroMatchPill"
              hidden
              data-ff-match-pill
              aria-live="polite"
              aria-atomic="true"
              >Match active</span
            >
          </li>
          <li>
            <span
              class="ff-pill"
              aria-live="polite"
              aria-atomic="true"
              title="Deadline countdown"
            >
              Time left <span class="ff-num" id="heroCountdown">—</span>
            </span>
          </li>
        </ul>

        <article
          class="ff-card ff-card--lift ff-card--premium ff-pad ff-glass ff-hero__card"
          data-ff-hero-card
        >
          <header class="ff-row ff-hero__cardhead">
            <img
              id="heroLogo"
              class="ff-hero__logo ff-logo"
              src="{{ org_logo|default(url_for('static', filename='images/logo.webp')) }}"
              alt="{{ org_name|default('Organization') }} logo"
              width="44"
              height="44"
              loading="eager"
              decoding="async"
              fetchpriority="high"
            />
            <div class="ff-minw-0">
              <p class="ff-kicker ff-m-0" id="heroOrgLine" data-ff-hero-orgline>
                Organization • Fundraiser
              </p>
              <div
                class="ff-row ff-wrap ff-mt-2"
                aria-label="Trust badges"
                data-ff-trust-badges
              >
                <span class="ff-badge"
                  ><i
                    class="ff-badge__dot ff-badge__dot--verified"
                    aria-hidden="true"
                  ></i
                  >Verified</span
                >
                <span class="ff-badge"
                  ><i
                    class="ff-badge__dot ff-badge__dot--secure"
                    aria-hidden="true"
                  ></i
                  >Secure checkout</span
                >
                <span class="ff-badge"
                  ><i
                    class="ff-badge__dot ff-badge__dot--receipt"
                    aria-hidden="true"
                  ></i
                  >Instant receipt</span
                >
              </div>
            </div>
          </header>

          <div class="ff-hero__headline">
            <h1 id="heroTitle" class="ff-h1 ff-mt-3">
              Fuel the season. Fund the future.<br />
              <span class="ff-accent" id="heroAccentLine"
                >Help more kids stay on the floor.</span
              >
            </h1>
          </div>

          <p id="heroCopy" class="ff-lead ff-mt-2" data-ff-hero-copy>
            Clear allocation, secure checkout, instant receipts — and every gift
            makes practice, travel, and gear possible.
          </p>

          <!-- Tight CTA row -->
          <div
            class="ff-hero__cta ff-hero__cta--v14_2 ff-mt-2"
            role="group"
            aria-label="Primary actions"
          >
            <a
              class="ff-btn ff-btn--primary"
              href="#donate"
              data-ff-donate-cta
              aria-describedby="heroDonateHint"
              >Donate</a
            >
            <button
              class="ff-btn ff-btn--secondary"
              type="button"
              data-ff-share-open
              aria-label="Open share tools"
            >
              Share
            </button>
            <button
              class="ff-btn ff-btn--ghost"
              type="button"
              data-ff-copy-link
              aria-label="Copy fundraiser link"
            >
              Copy link
            </button>
          </div>
          <p id="heroDonateHint" class="ff-sr">
            Donating opens secure checkout. You will receive an instant email
            receipt.
          </p>

          <!-- Compact disclosure -->
          <details
            class="ff-disc ff-disc--tight ff-mt-3"
            aria-label="Why this matters"
            data-ff-hero-disclosure
          >
            <summary class="ff-disc__sum">
              <span class="ff-disc__meta">
                <span class="ff-kicker">Why this matters</span><br />
                <span class="ff-help">Proof, what it funds, and trust</span>
              </span>
              <span class="ff-pill" aria-hidden="true">Open</span>
            </summary>

            <div class="ff-disc__body">
              <div
                class="ff-grid ff-grid--3 ff-hero__proof ff-hero__proof--tight"
                aria-label="Proof points"
                data-ff-hero-proof
              >
                <div class="ff-mini ff-glass ff-mini--premium">
                  <div class="ff-kicker">Respect</div>
                  <div class="ff-card__title">Donor-first</div>
                  <p class="ff-help">Clarity and dignity over pressure.</p>
                </div>
                <div class="ff-mini ff-glass ff-mini--premium">
                  <div class="ff-kicker">Trust</div>
                  <div class="ff-card__title">Instant receipt</div>
                  <p class="ff-help">Delivered by email after payment.</p>
                </div>
                <div class="ff-mini ff-glass ff-mini--premium">
                  <div class="ff-kicker">Stewardship</div>
                  <div class="ff-card__title">Transparent</div>
                  <p class="ff-help">Allocation visible and maintained.</p>
                </div>
              </div>

              <div class="ff-mini ff-mini--premium ff-mt-3">
                <div class="ff-kicker">What this funds</div>
                <ul class="ff-bul ff-mt-2">
                  <li>Gym time, tournament fees, travel support</li>
                  <li>Uniforms, gear, player development</li>
                  <li>Scholarships for families who need help</li>
                  <li>Sponsor recognition + share tools</li>
                </ul>
              </div>

              <div
                class="ff-row ff-wrap ff-mt-2"
                role="group"
                aria-label="Confidence links"
                data-ff-hero-links
              >
                <button
                  class="ff-btn ff-btn--ghost ff-btn--sm"
                  type="button"
                  data-ff-proof-open
                >
                  Proof kit
                </button>
                <button
                  class="ff-btn ff-btn--ghost ff-btn--sm"
                  type="button"
                  data-ff-policy-open
                >
                  Trust center
                </button>
                <a class="ff-btn ff-btn--ghost ff-btn--sm" href="#sponsors"
                  >Sponsor leaderboard</a
                >
              </div>
            </div>
          </details>
        </article>
      </div>

      <!-- RIGHT: PROGRESS CONSOLE (rail on desktop) -->
      <aside
        class="ff-hero__rail ff-hero__rail--v14_2"
        aria-label="Fundraising console"
        data-ff-progress-rail
      >
        <section id="progress" data-ff-progress aria-label="Progress">
          <article
            class="ff-card ff-card--lift ff-card--premium ff-pad ff-glass"
            aria-labelledby="progressTitle"
            data-ff-progress-card
          >
            <header class="ff-row ff-row--between ff-wrap ff-ais">
              <div class="ff-minw-0">
                <div class="ff-kicker">Progress</div>
                <h2 id="progressTitle" class="ff-h2">Fundraising progress</h2>

                <div
                  class="ff-big ff-num"
                  id="raisedBig"
                  aria-live="polite"
                  aria-atomic="true"
                >
                  $0
                </div>

                <p class="ff-help" id="progressMeta">
                  <span class="ff-num" id="remainingText">$0</span> to go •
                  <span class="ff-num" id="deadlineText">—</span>
                </p>

                <div
                  class="ff-row ff-wrap"
                  id="matchDetailRow"
                  role="status"
                  aria-live="polite"
                  aria-atomic="true"
                  data-ff-match-details
                  hidden
                >
                  <span class="ff-pill ff-pill--accent" id="matchDetailPill"
                    >Match</span
                  >
                  <span class="ff-pill" id="matchRemainingPill"
                    >Remaining —</span
                  >
                  <span class="ff-pill" id="matchEndsPill">Ends —</span>
                </div>
              </div>

              <span class="ff-pill" aria-label="Goal amount">
                Goal <span class="ff-num" id="goalPill">$0</span>
              </span>
            </header>

            <div class="ff-divider" aria-hidden="true"></div>

            <!-- Quick Gifts -->
            <section aria-label="Quick gifts" data-ff-quickgifts>
              <div class="ff-kicker">Quick gifts</div>
              <p class="ff-help">One tap prefills secure checkout.</p>

              <ul
                class="ff-row ff-wrap ff-quickgifts--tight"
                role="list"
                aria-label="Quick gift buttons"
              >
                <li>
                  <button
                    class="ff-chip"
                    type="button"
                    data-ff-quick-amount="25"
                    aria-pressed="false"
                  >
                    $25
                  </button>
                </li>
                <li>
                  <button
                    class="ff-chip"
                    type="button"
                    data-quick-amount="75"
                    data-ff-quick-amount="75"
                    aria-pressed="false"
                  >
                    $75
                  </button>
                </li>
                <li>
                  <button
                    class="ff-chip"
                    type="button"
                    data-quick-amount="150"
                    data-ff-quick-amount="150"
                    aria-pressed="false"
                  >
                    $150
                  </button>
                </li>
                <li>
                  <button
                    class="ff-chip"
                    type="button"
                    data-quick-amount="500"
                    data-ff-quick-amount="500"
                    aria-pressed="false"
                  >
                    $500
                  </button>
                </li>
                <li>
                  <button
                    class="ff-chip"
                    type="button"
                    data-quick-amount="1000"
                    data-ff-quick-amount="1000"
                    aria-pressed="false"
                  >
                    Sponsor $1,000
                  </button>
                </li>
              </ul>
            </section>

            <!-- Collapsed details for mobile -->
            <details
              class="ff-disc ff-disc--tight ff-mt-3"
              aria-label="Progress details"
              data-ff-progress-details
            >
              <summary class="ff-disc__sum">
                <span class="ff-disc__meta">
                  <span class="ff-kicker">Details</span><br />
                  <span class="ff-help">Meter, QR, and recent gifts</span>
                </span>
                <span class="ff-pill" aria-hidden="true">Open</span>
              </summary>

              <div class="ff-disc__body">
                <div
                  class="ff-meter"
                  role="progressbar"
                  aria-label="Overall fundraising progress"
                  aria-valuemin="0"
                  aria-valuemax="100"
                  aria-valuenow="0"
                  aria-valuetext="0% funded"
                  data-ff-meter="overall"
                >
                  <div class="ff-meter__ticks" aria-hidden="true">
                    <i class="ff-meter__tick ff-meter__tick--25"></i>
                    <i class="ff-meter__tick ff-meter__tick--50"></i>
                    <i class="ff-meter__tick ff-meter__tick--75"></i>
                    <i class="ff-meter__tick ff-meter__tick--100"></i>
                  </div>
                  <span id="overallBar"></span>
                </div>

                <div
                  class="ff-row ff-row--between ff-wrap ff-mt-2"
                  aria-label="Raised and goal numbers"
                >
                  <span class="ff-help"
                    >Raised: <span class="ff-num" id="raisedRow">$0</span></span
                  >
                  <span class="ff-help"
                    >Goal: <span class="ff-num" id="goalRow">$0</span></span
                  >
                  <span class="ff-help"
                    ><strong class="ff-num" id="pctText">0</strong>%</span
                  >
                </div>

                <div class="ff-divider" aria-hidden="true"></div>

                <div
                  class="ff-row ff-wrap ff-ais"
                  aria-label="Share QR and actions"
                  data-ff-qr-block
                >
                  <img
                    class="ff-qr"
                    id="progressQr"
                    alt="Fundraiser QR code"
                    width="112"
                    height="112"
                    loading="lazy"
                    decoding="async"
                    data-ff-qr
                  />
                  <div class="ff-minw-0 ff-grow">
                    <div class="ff-kicker">Share</div>
                    <div class="ff-card__title">Scan to donate</div>

                    <div
                      class="ff-row ff-wrap ff-mt-2"
                      role="group"
                      aria-label="Share actions"
                    >
                      <button
                        class="ff-btn ff-btn--secondary ff-btn--sm"
                        type="button"
                        data-ff-share-open
                      >
                        Share tools
                      </button>
                      <button
                        class="ff-btn ff-btn--ghost ff-btn--sm"
                        type="button"
                        data-ff-copy-link
                      >
                        Copy link
                      </button>
                      <button
                        class="ff-btn ff-btn--ghost ff-btn--sm"
                        type="button"
                        data-ff-download-qr
                      >
                        Download QR
                      </button>
                    </div>
                  </div>
                </div>

                <div class="ff-divider" aria-hidden="true"></div>

                <section aria-label="Recent gifts" data-ff-recent>
                  <div class="ff-row ff-row--between ff-wrap">
                    <div>
                      <div class="ff-kicker">Recent gifts</div>
                      <p class="ff-help">
                        Shown only when donors allow recognition.
                      </p>
                    </div>
                    <button
                      class="ff-btn ff-btn--ghost ff-btn--sm"
                      type="button"
                      data-ff-refresh
                    >
                      Refresh
                    </button>
                  </div>

                  <div
                    class="ff-slimlist"
                    data-ff-recent-gifts
                    aria-live="polite"
                    aria-atomic="false"
                  ></div>
                </section>
              </div>
            </details>

            <!-- Console action row -->
            <div
              class="ff-row ff-wrap ff-mt-3"
              role="group"
              aria-label="Console actions"
            >
              <a class="ff-btn ff-btn--primary" href="#donate" data-ff-donate-cta
                >Donate now</a
              >
              <button
                class="ff-btn ff-btn--secondary"
                type="button"
                data-ff-share-open
              >
                Share
              </button>
              <button
                class="ff-btn ff-btn--ghost"
                type="button"
                data-ff-copy-link
              >
                Copy link
              </button>
            </div>

            <p class="ff-help ff-muted ff-mt-2">
              Secure checkout via Stripe. Instant email receipt after payment.
            </p>
          </article>
        </section>
      </aside>
    </div>
  </div>
</section>


<!-- ============================================================
  IMPACT (v14.2) — COMPLETE, DROP-IN
  Contracts satisfied:
  - #impactSearch, #impactGrid used by ff-app.js Impact.bind()
  - [data-ff-impact-filter] chips used by Impact.bind()
  - [data-ff-impact-empty] empty state used by Impact.render()
  - Prefill buttons use [data-ff-prefill] + data-ff-prefill-amount
  - Share/proof/policy triggers match Panels.bind() + Share.bind()
=============================================================== -->

<section
  id="impact"
  class="ff-section ff-impact ff-impact--sv ff-impact--v14 ff-impact--v14_2"
  aria-labelledby="impactTitle"
  data-ff-impact
  data-ff-section="impact"
>
  <div class="ff-container">
    <article
      class="ff-card ff-card--lift ff-card--premium ff-pad"
      data-ff-impact-card
    >
      <header class="ff-impact__head ff-impact__head--v14_2">
        <div class="ff-minw-0">
          <div class="ff-kicker">Impact</div>
          <h2 id="impactTitle" class="ff-h2">Pick an impact</h2>
          <p class="ff-lead ff-impact__lead" id="impactLead">
            Choose a tier or a need. Checkout is fast, secure, and respectful.
            Recognition is optional.
          </p>

          <!-- Impact Ladder (Premium tiers, always visible) -->
          <section
            class="ff-mt-2"
            aria-label="Impact ladder"
            data-ff-impact-ladder
          >
            <div class="ff-truststrip ff-truststrip--premium ff-truststrip--tight">
              <div class="ff-row ff-row--between ff-wrap ff-ais">
                <div class="ff-minw-0">
                  <div class="ff-kicker">Impact ladder</div>
                  <div class="ff-card__title">Four ways to help</div>
                  <p class="ff-help">Each tier maps to real needs.</p>
                </div>
                <span
                  class="ff-pill"
                  hidden
                  data-ff-match-callout
                  aria-live="polite"
                  aria-atomic="true"
                >
                  Match active —
                  <span class="ff-num" data-ff-match-callout-text
                    >doubling gifts</span
                  >
                </span>
              </div>

              <div
                class="ff-tiergrid ff-tiergrid--tight ff-mt-2"
                role="list"
                aria-label="Impact tier options"
              >
                <!-- Tier Cards -->
                <article
                  class="ff-tiercard ff-tiercard--premium"
                  role="listitem"
                  data-ff-tier="starter"
                >
                  <div class="ff-tiercard__top">
                    <div>
                      <div class="ff-kicker">Starter</div>
                      <div class="ff-card__title">Help with essentials</div>
                      <div class="ff-help ff-tiercard__meta">
                        A clean first step.
                      </div>
                    </div>
                    <div class="ff-tiercard__amt ff-num">$25</div>
                  </div>
                  <div class="ff-tiercard__cta">
                    <button
                      class="ff-btn ff-btn--primary ff-btn--sm"
                      type="button"
                      data-ff-prefill
                      data-ff-prefill-amount="25"
                      data-ff-prefill-purpose="impact"
                      data-ff-prefill-sku="impact_starter"
                    >
                      Give $25
                    </button>
                    <button
                      class="ff-btn ff-btn--ghost ff-btn--sm"
                      type="button"
                      data-ff-share-open
                    >
                      Share
                    </button>
                  </div>
                </article>

                <article
                  class="ff-tiercard ff-tiercard--premium"
                  role="listitem"
                  data-ff-tier="momentum"
                >
                  <div class="ff-tiercard__top">
                    <div>
                      <div class="ff-kicker">Momentum</div>
                      <div class="ff-card__title">Cover the week</div>
                      <div class="ff-help ff-tiercard__meta">
                        Gym time + small gaps.
                      </div>
                    </div>
                    <div class="ff-tiercard__amt ff-num">$75</div>
                  </div>
                  <div class="ff-tiercard__cta">
                    <button
                      class="ff-btn ff-btn--primary ff-btn--sm"
                      type="button"
                      data-ff-prefill
                      data-ff-prefill-amount="75"
                      data-ff-prefill-purpose="impact"
                      data-ff-prefill-sku="impact_momentum"
                    >
                      Give $75
                    </button>
                    <button
                      class="ff-btn ff-btn--ghost ff-btn--sm"
                      type="button"
                      data-ff-copy-link
                    >
                      Copy link
                    </button>
                  </div>
                </article>

                <article
                  class="ff-tiercard ff-tiercard--premium"
                  role="listitem"
                  data-ff-tier="travel"
                >
                  <div class="ff-tiercard__top">
                    <div>
                      <div class="ff-kicker">Travel</div>
                      <div class="ff-card__title">Tournament-ready</div>
                      <div class="ff-help ff-tiercard__meta">
                        Fees + travel support.
                      </div>
                    </div>
                    <div class="ff-tiercard__amt ff-num">$150</div>
                  </div>
                  <div class="ff-tiercard__cta">
                    <button
                      class="ff-btn ff-btn--primary ff-btn--sm"
                      type="button"
                      data-ff-prefill
                      data-ff-prefill-amount="150"
                      data-ff-prefill-purpose="impact"
                      data-ff-prefill-sku="impact_travel"
                    >
                      Give $150
                    </button>
                    <button
                      class="ff-btn ff-btn--ghost ff-btn--sm"
                      type="button"
                      data-ff-policy-open
                    >
                      Trust
                    </button>
                  </div>
                </article>

                <article
                  class="ff-tiercard ff-tiercard--premium"
                  role="listitem"
                  data-ff-tier="legacy"
                >
                  <div class="ff-tiercard__top">
                    <div>
                      <div class="ff-kicker">Legacy</div>
                      <div class="ff-card__title">Season-defining</div>
                      <div class="ff-help ff-tiercard__meta">
                        Scholarships + development.
                      </div>
                    </div>
                    <div class="ff-tiercard__amt ff-num">$500</div>
                  </div>
                  <div class="ff-tiercard__cta">
                    <button
                      class="ff-btn ff-btn--primary ff-btn--sm"
                      type="button"
                      data-ff-prefill
                      data-ff-prefill-amount="500"
                      data-ff-prefill-purpose="impact"
                      data-ff-prefill-sku="impact_legacy"
                    >
                      Give $500
                    </button>
                    <button
                      class="ff-btn ff-btn--ghost ff-btn--sm"
                      type="button"
                      data-ff-proof-open
                    >
                      Proof kit
                    </button>
                  </div>
                </article>
              </div>
            </div>
          </section>
        </div>

        <!-- Impact Section Actions -->
        <div
          class="ff-row ff-wrap ff-impact__actions ff-impact__actions--tight"
          role="group"
          aria-label="Impact actions"
        >
          <a
            class="ff-btn ff-btn--primary"
            href="#donate"
            data-ff-donate-cta
            aria-describedby="impactLead"
            >Donate</a
          >
          <button
            class="ff-btn ff-btn--secondary"
            type="button"
            data-ff-share-open
            aria-describedby="impactLead"
          >
            Share
          </button>
          <button
            class="ff-btn ff-btn--ghost"
            type="button"
            data-ff-copy-link
          >
            Copy link
          </button>
        </div>
      </header>

      <!-- Impact Console (Dynamic needs, search/filter) -->
      <section
        class="ff-impact__console ff-mt-3"
        aria-label="Browse needs"
        data-ff-impact-console
      >
        <!-- Filters Tray -->
        <div
          class="ff-impact__chips ff-impact__chips--tight"
          role="group"
          aria-label="Impact filters"
          data-ff-impact-filters
        >
          <button
            class="ff-chip is-selected"
            type="button"
            data-ff-impact-filter="all"
            aria-pressed="true"
          >
            All
          </button>
          <button
            class="ff-chip"
            type="button"
            data-ff-impact-filter="gear"
            aria-pressed="false"
          >
            Gear
          </button>
          <button
            class="ff-chip"
            type="button"
            data-ff-impact-filter="travel"
            aria-pressed="false"
          >
            Travel
          </button>
          <button
            class="ff-chip"
            type="button"
            data-ff-impact-filter="fees"
            aria-pressed="false"
          >
            Fees
          </button>
          <button
            class="ff-chip"
            type="button"
            data-ff-impact-filter="scholarship"
            aria-pressed="false"
          >
            Scholarships
          </button>
        </div>

        <div class="ff-impact__gridwrap ff-impact__gridwrap--v14_2 ff-mt-2">
          <!-- Dynamic Impact Needs Grid -->
          <section aria-label="Impact options" class="ff-impact__needs">
            <div class="ff-row ff-row--between ff-wrap ff-ais">
              <div class="ff-minw-0">
                <div class="ff-kicker">Needs</div>
                <p class="ff-help" id="impactGridHelp">
                  Pick a card to prefill checkout.
                </p>
              </div>

              <div
                class="ff-impact__searchwrap"
                role="group"
                aria-label="Search needs"
              >
                <label class="ff-sr" for="impactSearch">Search needs</label>
                <input
                  id="impactSearch"
                  class="ff-input ff-impact__search ff-impact__search--tight"
                  type="search"
                  inputmode="search"
                  autocomplete="off"
                  placeholder="Search…"
                  aria-label="Search needs"
                  aria-describedby="impactGridHelp"
                  aria-controls="impactGrid"
                  data-ff-impact-search
                />
              </div>
            </div>

            <div
              class="ff-grid ff-grid--2 ff-mt-2 ff-impact__grid ff-impact__grid--tight"
              id="impactGrid"
              data-ff-impact-grid
              role="list"
              aria-live="polite"
              aria-atomic="false"
            >
              <!-- Populated dynamically by ff-app.js -->
            </div>

            <div
              class="ff-mini"
              hidden
              data-ff-impact-empty
              aria-label="No matching needs"
            >
              <div class="ff-kicker">No matches</div>
              <div class="ff-card__title">Try a different search</div>
              <p class="ff-help">Try “travel”, “fees”, “uniform”, “gear”.</p>
            </div>
          </section>

          <!-- Allocation + Trust Aside -->
          <aside class="ff-impact__aside" aria-label="Allocation and transparency">
            <div class="ff-mini ff-mini--premium">
              <div class="ff-kicker">Allocation</div>
              <div class="ff-card__title">Clear breakdown</div>
              <div
                class="ff-stack ff-mt-2"
                data-ff-allocation-list
                aria-label="Allocation list"
              ></div>
              <p class="ff-help ff-muted ff-mt-2" hidden data-ff-allocation-updated>
                Updated —
                <span class="ff-num" data-ff-allocation-updated-text>—</span>
              </p>
            </div>

            <div class="ff-mini ff-mini--premium">
              <div class="ff-kicker">Trust</div>
              <div class="ff-card__title">Built for donors</div>
              <ul class="ff-bul ff-mt-2">
                <li>Instant email receipt</li>
                <li>Anonymous option</li>
                <li>Stripe-secured checkout</li>
              </ul>
              <div
                class="ff-row ff-wrap ff-mt-2"
                role="group"
                aria-label="Trust actions"
              >
                <button
                  class="ff-btn ff-btn--ghost ff-btn--sm"
                  type="button"
                  data-ff-share-open
                >
                  Share tools
                </button>
                <button
                  class="ff-btn ff-btn--ghost ff-btn--sm"
                  type="button"
                  data-ff-proof-open
                >
                  Proof kit
                </button>
                <button
                  class="ff-btn ff-btn--ghost ff-btn--sm"
                  type="button"
                  data-ff-policy-open
                >
                  Trust center
                </button>
              </div>
            </div>
          </aside>
        </div>

        <!-- Tiny bottom CTA row -->
        <div
          class="ff-row ff-wrap ff-mt-3 ff-impact__miniCta ff-impact__miniCta--tight"
          role="group"
          aria-label="Impact quick actions"
        >
          <a
            class="ff-btn ff-btn--primary ff-btn--sm"
            href="#donate"
            data-ff-donate-cta
            >Donate</a
          >
          <button
            class="ff-btn ff-btn--secondary ff-btn--sm"
            type="button"
            data-ff-share-open
          >
            Share
          </button>
          <button
            class="ff-btn ff-btn--ghost ff-btn--sm"
            type="button"
            data-ff-copy-link
          >
            Copy link
          </button>
        </div>
      </section>
    </article>
  </div>
</section>

<!-- ============================================================
  SPONSORS (v14.2) — COMPLETE, DROP-IN
  Contracts satisfied:
  - Sponsors.bind() expects: [data-ff-sponsor-cta], [data-ff-sponsor-tiers],
    [data-ff-sponsor-podium], [data-ff-sponsor-leaderboard], [data-ff-sponsor-wall],
    [data-ff-refresh-sponsors]
  - Prefill integration via Prefill.setAmount() on [data-ff-sponsor-cta]
  - Share/proof/policy triggers align with Share.bind() + Panels.bind()
=============================================================== -->

<section
  id="sponsors"
  class="ff-section ff-sponsors ff-sponsors--sv ff-sponsors--v14 ff-sponsors--v14_2"
  aria-labelledby="sponsorsTitle"
  data-ff-sponsors
  data-ff-section="sponsors"
>
  <div class="ff-container">
    <article
      class="ff-card ff-card--lift ff-card--premium ff-pad"
      data-ff-sponsors-card
    >
      <!-- Compact head -->
      <header class="ff-sponsors__head ff-sponsors__head--v14_2">
        <div class="ff-minw-0">
          <div class="ff-kicker">Sponsors</div>
          <h2 id="sponsorsTitle" class="ff-h2">VIP sponsor leaderboard</h2>
          <p
            class="ff-lead ff-sponsors__lead ff-sponsors__lead--tight"
            id="sponsorsLead"
          >
            Brand-safe recognition: logo wall, leaderboard, and share-ready
            proof.
          </p>

          <div
            class="ff-row ff-wrap ff-sponsors__pills"
            aria-label="Sponsor highlights"
          >
            <span class="ff-pill">Logo wall</span>
            <span class="ff-pill">Top sponsors</span>
            <span class="ff-pill" hidden data-ff-premium-only
              >Spotlight posts (premium)</span
            >
            <span
              class="ff-pill ff-pill--accent"
              hidden
              data-ff-sponsor-match-pill
              aria-live="polite"
              aria-atomic="true"
              >Match active</span
            >
          </div>

          <!-- Sponsor quick-tier tray -->
          <div
            class="ff-sponsors__tiers ff-sponsors__tiers--tray"
            role="group"
            aria-label="Sponsor quick tiers"
            data-ff-sponsor-quick
          >
            <span class="ff-pill ff-pill--accent" aria-hidden="true"
              >Sponsor</span
            >
            <button
              class="ff-chip"
              type="button"
              data-ff-sponsor-cta
              data-ff-prefill-amount="250"
              aria-pressed="false"
            >
              $250
            </button>
            <button
              class="ff-chip"
              type="button"
              data-ff-sponsor-cta
              data-ff-prefill-amount="500"
              aria-pressed="false"
            >
              $500
            </button>
            <button
              class="ff-chip"
              type="button"
              data-ff-sponsor-cta
              data-ff-prefill-amount="1000"
              aria-pressed="false"
            >
              $1,000
            </button>
            <button
              class="ff-chip"
              type="button"
              data-ff-sponsor-cta
              data-ff-prefill-amount="2500"
              aria-pressed="false"
            >
              $2,500
            </button>
          </div>
        </div>

        <!-- Primary sponsor actions -->
        <nav
          class="ff-row ff-wrap ff-sponsors__actions ff-sponsors__actions--tight"
          aria-label="Sponsor actions"
        >
          <button
            class="ff-btn ff-btn--primary"
            type="button"
            data-ff-sponsor-cta
            data-ff-prefill-amount="1000"
            aria-describedby="sponsorsLead"
          >
            Become a sponsor
          </button>
          <button
            class="ff-btn ff-btn--secondary"
            type="button"
            data-ff-proof-open
            aria-label="Open sponsor packet"
          >
            Sponsor packet
          </button>
          <button
            class="ff-btn ff-btn--ghost"
            type="button"
            data-ff-share-open
            aria-label="Open share tools"
          >
            Share
          </button>
        </nav>

        <!-- Tiny tool row -->
        <div class="ff-row ff-wrap ff-mt-2" role="group" aria-label="Sponsor tools">
          <button
            class="ff-btn ff-btn--ghost ff-btn--sm"
            type="button"
            data-ff-policy-open
          >
            Trust center
          </button>
          <button
            class="ff-btn ff-btn--ghost ff-btn--sm"
            type="button"
            data-ff-copy-link
          >
            Copy link
          </button>
          <button
            class="ff-btn ff-btn--ghost ff-btn--sm"
            type="button"
            data-ff-refresh-sponsors
          >
            Refresh
          </button>
        </div>
      </header>

      <!-- Sponsors body: leaderboard + wall, always mobile-optimized -->
      <div
        class="ff-sponsors__layout ff-sponsors__layout--v14_2 ff-mt-3"
        data-ff-sponsors-grid
      >
        <!-- LEFT: Leaderboard, tiers -->
        <section class="ff-sponsors__left" aria-label="Sponsor tiers and leaderboard">
          <div class="ff-row ff-row--between ff-wrap ff-ais">
            <div class="ff-minw-0">
              <div class="ff-kicker">Leaderboard</div>
              <p class="ff-help" id="sponsorBoardHelp">
                Top sponsors appear after successful payments.
              </p>
            </div>
            <div
              class="ff-sponsors__tray"
              role="group"
              aria-label="Leaderboard sort"
              data-ff-sponsor-sort
            >
              <button
                class="ff-chip is-selected"
                type="button"
                aria-pressed="true"
                data-ff-sort="rank"
              >
                Rank
              </button>
              <button
                class="ff-chip"
                type="button"
                aria-pressed="false"
                data-ff-sort="recent"
              >
                Recent
              </button>
            </div>
          </div>

          <!-- Podium always visible (hydrated by API.refreshSponsors() if present) -->
          <ol
            class="ff-podium ff-sponsors__podium"
            data-ff-sponsor-podium
            aria-label="Top sponsors podium"
          ></ol>

          <!-- Full sponsor list (collapsible) -->
          <details
            class="ff-disc ff-disc--tight ff-mt-2"
            data-ff-sponsor-collapse
          >
            <summary class="ff-disc__sum">
              <span class="ff-disc__meta">
                <span class="ff-kicker">All sponsors</span><br />
                <span class="ff-help">Tap to view full list</span>
              </span>
              <span class="ff-pill" aria-hidden="true">Open</span>
            </summary>
            <div class="ff-disc__body">
              <ol
                class="ff-stack"
                data-ff-sponsor-leaderboard
                aria-label="Sponsor leaderboard list"
              ></ol>
            </div>
          </details>

          <div class="ff-divider" aria-hidden="true"></div>

          <!-- Tiers list -->
          <div class="ff-row ff-row--between ff-wrap ff-ais">
            <div class="ff-minw-0">
              <div class="ff-kicker">Tiers</div>
              <p class="ff-help">Recognition is optional at payment.</p>
            </div>
            <div class="ff-row ff-wrap" role="group" aria-label="Tier actions">
              <button
                class="ff-btn ff-btn--ghost ff-btn--sm"
                type="button"
                data-ff-proof-open
              >
                Proof kit
              </button>
              <button
                class="ff-btn ff-btn--ghost ff-btn--sm"
                type="button"
                data-ff-policy-open
              >
                Trust
              </button>
            </div>
          </div>

          <ul
            class="ff-stack ff-mt-2"
            data-ff-sponsor-tiers
            aria-label="Sponsor tiers list"
          ></ul>

          <div class="ff-row ff-wrap ff-mt-2" role="group" aria-label="Sponsor CTAs">
            <button
              class="ff-btn ff-btn--primary"
              type="button"
              data-ff-sponsor-cta
              data-ff-prefill-amount="1000"
            >
              Sponsor $1,000
            </button>
            <button
              class="ff-btn ff-btn--secondary"
              type="button"
              data-ff-share-open
            >
              Share
            </button>
            <button
              class="ff-btn ff-btn--ghost"
              type="button"
              data-ff-copy-link
            >
              Copy link
            </button>
          </div>

          <p class="ff-help ff-muted ff-mt-2">
            Updates automatically after successful payments.
          </p>
        </section>

        <!-- RIGHT: Sponsor wall, recognition, perks -->
        <aside class="ff-sponsors__aside" aria-label="Sponsor wall and perks">
          <details class="ff-disc ff-disc--tight ff-sponsors__fold" data-ff-sponsors-fold>
            <summary class="ff-disc__sum">
              <span class="ff-disc__meta">
                <span class="ff-kicker">Sponsor wall</span><br />
                <span class="ff-help">Logos + recognition surfaces</span>
              </span>
              <span class="ff-pill" aria-hidden="true">Open</span>
            </summary>

            <div class="ff-disc__body">
              <!-- LOGO WALL -->
              <div class="ff-mini ff-mini--premium">
                <div class="ff-row ff-row--between ff-wrap ff-ais">
                  <div class="ff-minw-0">
                    <div class="ff-kicker">Wall</div>
                    <p class="ff-help" id="sponsorWallHelp">
                      Logos appear after successful payments.
                    </p>
                  </div>
                  <div
                    class="ff-sponsors__tray"
                    role="group"
                    aria-label="Wall view"
                    data-ff-sponsor-wall-view
                  >
                    <button
                      class="ff-chip is-selected"
                      type="button"
                      aria-pressed="true"
                      data-ff-wall="logos"
                      aria-controls="ffSponsorWall"
                    >
                      Logos
                    </button>
                    <button
                      class="ff-chip"
                      type="button"
                      aria-pressed="false"
                      data-ff-wall="list"
                      aria-controls="ffSponsorWall"
                    >
                      List
                    </button>
                  </div>
                </div>

                <div class="ff-sponsors__wallwrap ff-mt-2">
                  <div
                    id="ffSponsorWall"
                    class="ff-sponsors__wall ff-sponsors__wall--premium"
                    data-ff-sponsor-wall
                    aria-label="Sponsor wall"
                    aria-describedby="sponsorWallHelp"
                    aria-live="polite"
                  ></div>
                </div>

                <div class="ff-row ff-wrap ff-mt-2" role="group" aria-label="Wall actions">
                  <button
                    class="ff-btn ff-btn--primary ff-btn--sm"
                    type="button"
                    data-ff-sponsor-cta
                    data-ff-prefill-amount="1000"
                  >
                    Sponsor $1,000
                  </button>
                  <button
                    class="ff-btn ff-btn--ghost ff-btn--sm"
                    type="button"
                    data-ff-proof-open
                  >
                    Sponsor packet
                  </button>
                  <button
                    class="ff-btn ff-btn--ghost ff-btn--sm"
                    type="button"
                    data-ff-share-open
                  >
                    Share
                  </button>
                </div>
              </div>

              <!-- RECOGNITION SURFACES -->
              <div
                class="ff-mini ff-mini--premium ff-mt-2"
                aria-label="Recognition surfaces"
                data-ff-prestige
              >
                <div class="ff-kicker">Recognition</div>
                <div class="ff-card__title">Boardroom-grade visibility</div>
                <p class="ff-help">Clean placement across page + share tools.</p>

                <div
                  class="ff-prestigegrid ff-prestigegrid--tight ff-mt-2"
                  role="list"
                  aria-label="Recognition preview surfaces"
                >
                  <div class="ff-prestige" role="listitem">
                    <div class="ff-kicker">Surface</div>
                    <div class="ff-card__title">Logo wall</div>
                    <p class="ff-help ff-muted">Auto-populates after payment.</p>
                  </div>
                  <div class="ff-prestige" role="listitem">
                    <div class="ff-kicker">Surface</div>
                    <div class="ff-card__title">Leaderboard</div>
                    <p class="ff-help ff-muted">Ranked + recent views.</p>
                  </div>
                  <div class="ff-prestige" role="listitem">
                    <div class="ff-kicker">Surface</div>
                    <div class="ff-card__title">Share proof</div>
                    <p class="ff-help ff-muted">Sponsor-ready assets (premium).</p>
                  </div>
                </div>

                <div class="ff-row ff-wrap ff-mt-2" role="group" aria-label="Recognition tools">
                  <button
                    class="ff-btn ff-btn--ghost ff-btn--sm"
                    type="button"
                    data-ff-proof-open
                  >
                    Proof kit
                  </button>
                  <button
                    class="ff-btn ff-btn--ghost ff-btn--sm"
                    type="button"
                    data-ff-share-open
                  >
                    Share tools
                  </button>
                </div>
              </div>

              <!-- SPONSOR PERKS -->
              <div class="ff-mini ff-mini--premium ff-mt-2" aria-label="Sponsor perks">
                <div class="ff-kicker">Perks</div>
                <div class="ff-card__title">Premium recognition</div>
                <ul class="ff-bul ff-mt-2">
                  <li>Logo wall + leaderboard placement</li>
                  <li>Share-ready proof + QR tools</li>
                  <li>Optional match activation</li>
                </ul>
              </div>
            </div>
          </details>
        </aside>
      </div>
    </article>
  </div>
</section>

<!-- ============================================================
  TEAMS (v14.2) — COMPLETE, DROP-IN
  Contracts satisfied:
  - Teams.bind() expects:
      - Grid mount: #teamsGrid (DOM.teamsGrid)
      - Status SR: #teamsStatus (DOM.teamsStatus)
      - Sort chips: [data-ff-team-sort] [data-ff-sort]
      - Search input: [data-ff-team-search] (or the selector used in JS)
      - Team select buttons created by JS: [data-ff-select-team]
  - Attribution.bind() expects:
      - Selected pill: [data-ff-team-selected], name: [data-ff-team-selected-name]
      - Clear button: [data-ff-attrib-clear]
      - Team share button: [data-ff-team-share]
  Notes:
  - Your current Teams.bind() uses: const search = $("[data-ff-team-search]");
    so this markup must include data-ff-team-search on the input.
  - This block is safe to paste as-is.
=============================================================== -->

<section
  id="teams"
  class="ff-section ff-teams ff-teams--sv ff-teams--v14 ff-teams--v14_2"
  aria-labelledby="teamsTitle"
  data-ff-teams
  data-ff-section="teams"
>
  <div class="ff-container">
    <article class="ff-card ff-card--lift ff-card--premium ff-pad" data-ff-teams-card>
      <!-- Compact header -->
      <header class="ff-teams__head ff-teams__head--v14_2">
        <div class="ff-minw-0">
          <div class="ff-kicker">Teams</div>
          <h2 id="teamsTitle" class="ff-h2">Support a team or group</h2>
          <p class="ff-lead ff-teams__lead ff-teams__lead--tight" id="teamsLead">
            Tap a team to prefill checkout and generate a share link for that group.
          </p>

          <!-- Selection status -->
          <div class="ff-row ff-wrap ff-ais ff-mt-2" aria-label="Selection status">
            <span
              class="ff-pill"
              hidden
              data-ff-team-selected
              aria-live="polite"
              aria-atomic="true"
            >
              Selected — <span class="ff-num" data-ff-team-selected-name>Team</span>
            </span>
            <span class="ff-help ff-muted" id="teamsGridHelp">
              Search, sort, then pick a team. Donation attribution happens automatically.
            </span>
          </div>

          <!-- Team quick actions -->
          <div class="ff-row ff-wrap ff-mt-2" role="group" aria-label="Team quick actions">
            <button
              class="ff-btn ff-btn--secondary ff-btn--sm"
              type="button"
              hidden
              data-ff-team-share
            >
              Share team link
            </button>
            <button class="ff-btn ff-btn--ghost ff-btn--sm" type="button" data-ff-copy-link>
              Copy fundraiser link
            </button>
          </div>
        </div>

        <!-- Compact action row -->
        <nav class="ff-row ff-wrap ff-teams__actions ff-teams__actions--tight" aria-label="Team actions">
          <a
            class="ff-btn ff-btn--primary"
            href="#donate"
            data-ff-donate-cta
            aria-describedby="teamsLead"
          >
            Donate
          </a>
          <button
            class="ff-btn ff-btn--secondary"
            type="button"
            data-ff-share-open
            aria-label="Open share tools"
          >
            Share
          </button>
          <button
            class="ff-btn ff-btn--ghost ff-btn--sm"
            type="button"
            data-ff-copy-link
            aria-label="Copy fundraiser link"
          >
            Copy
          </button>
        </nav>
      </header>

      <!-- Sticky controls (mobile-first) -->
      <div class="ff-teams__controlsWrap ff-teams__controlsWrap--v14_2" data-ff-team-controls>
        <form
          class="ff-teams__controls ff-controls ff-controls--premium ff-teams__controls--v14_2"
          role="search"
          aria-label="Find a team"
          data-ff-team-controls-form
        >
          <div class="ff-controls__search ff-teams__search" role="group" aria-label="Search teams">
            <label class="ff-sr" for="teamSearch">Search teams</label>
            <input
              id="teamSearch"
              class="ff-input"
              type="search"
              inputmode="search"
              autocomplete="off"
              placeholder="Search teams…"
              aria-controls="teamsGrid"
              aria-describedby="teamsGridHelp"
              data-ff-team-search
            />
          </div>

          <fieldset
            class="ff-teams__sort ff-controls__seg ff-teams__seg"
            aria-label="Sort teams"
            data-ff-team-sort
          >
            <legend class="ff-sr">Sort teams</legend>
            <button class="ff-chip is-selected" type="button" aria-pressed="true" data-ff-sort="featured">
              Featured
            </button>
            <button class="ff-chip" type="button" aria-pressed="false" data-ff-sort="goal">
              Goal %
            </button>
            <button class="ff-chip" type="button" aria-pressed="false" data-ff-sort="recent">
              Recent
            </button>
          </fieldset>

          <!-- Clear selection (JS can unhide when team is selected; you can also leave visible always) -->
          <button
            class="ff-btn ff-btn--ghost ff-btn--sm ff-teams__clear"
            type="button"
            hidden
            data-ff-attrib-clear
            aria-label="Clear selected team"
            title="Clear selected team"
          >
            Clear
          </button>
        </form>

        <div
          id="teamsStatus"
          class="ff-sr"
          role="status"
          aria-live="polite"
          aria-atomic="true"
          data-ff-teams-status
        ></div>
      </div>

      <!-- Teams grid (populated by JS) -->
      <section aria-label="Teams list" class="ff-mt-2">
        <div
          id="teamsGrid"
          class="ff-grid ff-grid--2 ff-mt-2 ff-grid--cards ff-teams__grid--v14_2"
          data-ff-teams-grid
          aria-label="Teams grid"
          aria-describedby="teamsGridHelp"
          aria-live="polite"
          aria-atomic="false"
          role="list"
        ></div>

        <p class="ff-help ff-muted ff-mt-2" id="teamsHelp">
          Tip: selecting a team credits the donation and generates a team share link.
        </p>

        <!-- Reserved mount for future “sheet” UX (safe no-op) -->
        <div hidden data-ff-team-sheet-mount aria-hidden="true"></div>
      </section>
    </article>
  </div>
</section>

<!-- ============================================================
  DONATE (FLAGSHIP ELITE v15.0) — DROP-IN REPLACEMENT
  Contract-tight IDs for ff-app.js:
    form:#donationForm
    amount:#amount, email:#email, coverFees:#coverFees, anonymous:#anonymous
    fullName:#fullName, message:#message
    receiptEmail:#receiptEmail
    summaryAmount:#summaryAmount, summaryFees:#summaryFees, summaryTotal:#summaryTotal
    summaryTeamRow:#summaryTeamRow, summaryTeam:#summaryTeam
    payBtn:#payBtn
    payError:#payError, payErrorText:#payErrorText
    paySuccess:#paySuccess, paySuccessText:#paySuccessText
    checkoutStatusText:#checkoutStatusText
    checkoutMethodPill:#checkoutMethodPill, checkoutMethodText:#checkoutMethodText
    attribBox:#attribBox
    Stripe mount:#paymentElement (or [data-ff-stripe-el])
    PayPal mount:#paypalMount + #paypalButtons
=============================================================== -->

<section
  id="donate"
  class="ff-section ff-donate ff-donate--elite ff-donate--v15"
  aria-labelledby="donateTitle"
  data-ff-donate
  data-ff-section="donate"
>
  <div class="ff-container">
    <article class="ff-card ff-card--lift ff-card--premium ff-pad" data-ff-donate-card>
      <!-- Header -->
      <header class="ff-donate__head ff-row ff-row--between ff-wrap ff-ais">
        <div class="ff-minw-0">
          <div class="ff-kicker">Donate</div>
          <h2 id="donateTitle" class="ff-h2">Secure checkout</h2>
          <p class="ff-lead ff-donate__lead ff-donate__lead--tight" id="donateLead">
            Fast, secure payment. Instant receipt by email. Recognition is optional.
          </p>

          <!-- Trust + status -->
          <div class="ff-row ff-wrap ff-mt-2 ff-donate__metaRow" aria-label="Checkout status and trust">
            <span class="ff-pill" data-ff-trust-chip>Stripe-secured</span>
            <span class="ff-pill" data-ff-trust-chip>Instant receipt</span>
            <span class="ff-pill" data-ff-trust-chip>No login</span>

            <span
              class="ff-pill ff-pill--accent"
              hidden
              data-ff-verified-pill
              aria-live="polite"
              aria-atomic="true"
            >
              Verified
            </span>

            <span class="ff-pill" id="checkoutStatusPill" aria-live="polite" aria-atomic="true">
              Status — <span class="ff-num" id="checkoutStatusText">Ready</span>
            </span>

            <span
              class="ff-pill"
              id="checkoutMethodPill"
              hidden
              data-ff-payment-method
              aria-live="polite"
              aria-atomic="true"
            >
              Method — <span class="ff-num" id="checkoutMethodText">—</span>
            </span>

            <span class="ff-pill" id="checkoutHintPill">
              Tip — <span class="ff-num">Tap a chip to prefill</span>
            </span>
          </div>
        </div>

        <!-- Quick actions -->
        <div class="ff-row ff-wrap ff-donate__actions" role="group" aria-label="Donate actions">
          <button class="ff-btn ff-btn--secondary ff-btn--sm" type="button" data-ff-share-open aria-describedby="donateLead">
            Share
          </button>
          <button class="ff-btn ff-btn--ghost ff-btn--sm" type="button" data-ff-copy-link>
            Copy link
          </button>
          <button class="ff-btn ff-btn--ghost ff-btn--sm" type="button" data-ff-policy-open>
            Trust center
          </button>
        </div>
      </header>

      <!-- Alerts -->
      <div
        id="payError"
        class="ff-notice ff-notice--error ff-notice--premium ff-mt-2"
        role="alert"
        hidden
        tabindex="-1"
        aria-live="assertive"
        aria-atomic="true"
      >
        <div class="ff-kicker">Payment error</div>
        <p class="ff-help" id="payErrorText">Something went wrong. Please try again.</p>
      </div>

      <div
        id="paySuccess"
        class="ff-notice ff-notice--success ff-notice--premium ff-mt-2"
        role="status"
        hidden
        tabindex="-1"
        aria-live="polite"
        aria-atomic="true"
      >
        <div class="ff-kicker">Payment successful</div>
        <p class="ff-help" id="paySuccessText">Thank you! Your receipt has been emailed.</p>
      </div>

      <div class="ff-donate-layout ff-mt-2">
        <!-- MAIN -->
        <div class="ff-donate-main">
          <section aria-label="Donation form">
            <form
              class="ff-form ff-form--premium"
              id="donationForm"
              novalidate
              data-ff-donate-form
              aria-describedby="donateFormHelp"
            >
              <p class="ff-sr" id="donateFormHelp">Required fields: Amount and Email.</p>

              <!-- AMOUNT -->
              <fieldset class="ff-fieldset" aria-label="Donation amount">
                <div class="ff-row ff-row--between ff-wrap ff-ais">
                  <div class="ff-minw-0">
                    <label class="ff-label" for="amount">Amount</label>
                    <p class="ff-help ff-muted ff-m-0" id="amountHelp">
                      Enter a custom amount, or tap a suggestion to prefill.
                    </p>
                  </div>
                  <span class="ff-pill" id="popularAmountPill" data-ff-popular-amount hidden aria-live="polite" aria-atomic="true">
                    Most popular — <span class="ff-num" id="popularAmountText">$75</span>
                  </span>
                </div>

                <div class="ff-inputrow ff-inputrow--premium ff-mt-2" aria-label="Donation amount">
                  <span class="ff-inputrow__prefix" aria-hidden="true">$</span>
                  <input
                    class="ff-input"
                    id="amount"
                    name="amount"
                    inputmode="decimal"
                    autocomplete="off"
                    placeholder="25"
                    required
                    data-ff-amount
                    aria-describedby="amountHelp amountError"
                  />
                </div>
                <p class="ff-sr" id="amountError" role="status" aria-live="polite" aria-atomic="true"></p>

                <div class="ff-row ff-wrap ff-mt-2 ff-amounttray" role="group" aria-label="Suggested amounts">
                  <button class="ff-chip" type="button" data-ff-quick-amount="25" aria-pressed="false">$25</button>
                  <button class="ff-chip" type="button" data-ff-quick-amount="75" aria-pressed="false">$75</button>
                  <button class="ff-chip" type="button" data-ff-quick-amount="150" aria-pressed="false">$150</button>
                  <button class="ff-chip" type="button" data-ff-quick-amount="500" aria-pressed="false">$500</button>
                  <button class="ff-chip" type="button" data-ff-quick-amount="1000" aria-pressed="false">Sponsor $1,000</button>
                  <button class="ff-chip" type="button" hidden data-ff-clear-amount aria-label="Clear amount">Clear</button>
                </div>
              </fieldset>

              <!-- EMAIL -->
              <fieldset class="ff-fieldset" aria-label="Receipt email">
                <div class="ff-row ff-row--between ff-wrap ff-ais">
                  <div class="ff-minw-0">
                    <div class="ff-kicker">Receipt</div>
                    <label class="ff-label" for="email">Email (for receipt)</label>
                  </div>
                  <span class="ff-pill" aria-label="Receipt timing">Instant <span class="ff-num">email receipt</span></span>
                </div>

                <input
                  class="ff-input ff-mt-2"
                  id="email"
                  name="email"
                  type="email"
                  autocomplete="email"
                  placeholder="you@example.com"
                  required
                  data-ff-email
                  aria-describedby="emailHelp emailError"
                />
                <p class="ff-help" id="emailHelp">We’ll email your receipt immediately after payment.</p>
                <p class="ff-sr" id="emailError" role="status" aria-live="polite" aria-atomic="true"></p>

                <div class="ff-grid ff-grid--2 ff-mt-2" aria-label="Donation toggles">
                  <label class="ff-check ff-check--premium">
                    <input type="checkbox" id="anonymous" name="anonymous" data-ff-anonymous />
                    Donate anonymously (hide name)
                  </label>
                  <label class="ff-check ff-check--premium" data-ff-coverfees-toggle>
                    <input type="checkbox" id="coverFees" name="coverFees" data-ff-coverfees />
                    Cover processing fees (helps the program)
                  </label>
                </div>

                <!-- Attribution -->
                <div
                  id="attribBox"
                  class="ff-mini ff-mini--premium ff-mt-2"
                  hidden
                  data-ff-attrib-box
                  aria-label="Team attribution"
                >
                  <div class="ff-row ff-row--between ff-ais">
                    <div class="ff-minw-0">
                      <div class="ff-kicker" id="attribTitle" data-ff-attrib-title>Supporting a team</div>
                      <p class="ff-help" id="attribHelp" data-ff-attrib-help>This donation will credit the selected group.</p>
                    </div>
                    <button class="ff-btn ff-btn--ghost ff-btn--sm" type="button" data-ff-attrib-clear aria-label="Clear team selection">
                      Clear
                    </button>
                  </div>
                </div>
              </fieldset>

              <!-- OPTIONAL -->
              <details class="ff-disc ff-donate__optional" data-ff-donate-optional>
                <summary class="ff-disc__sum">
                  <span class="ff-disc__meta">
                    <span class="ff-kicker">Optional</span><br />
                    <span class="ff-help">Name, message, recognition</span>
                  </span>
                  <span class="ff-pill" aria-hidden="true">Open</span>
                </summary>

                <div class="ff-disc__body">
                  <label class="ff-label" for="fullName">Name</label>
                  <input
                    class="ff-input"
                    id="fullName"
                    name="fullName"
                    autocomplete="name"
                    placeholder="Your name"
                    data-ff-name
                    aria-describedby="nameHelp"
                  />
                  <p class="ff-sr" id="nameHelp">Optional. Leave blank to donate without a name.</p>

                  <label class="ff-label" for="message">Message (optional)</label>
                  <textarea class="ff-input" id="message" name="message" placeholder="Add a note of support" data-ff-message></textarea>

                  <div class="ff-mini ff-mini--premium ff-mt-2" aria-label="Recognition settings">
                    <div class="ff-kicker">Recognition</div>
                    <p class="ff-help" id="recognitionHelp">Anonymous hides your name from public lists.</p>

                    <div class="ff-row ff-wrap ff-mt-2 ff-recotr" role="group" aria-label="Recognition quick settings">
                      <button class="ff-chip is-selected" type="button" data-ff-recognition="public" aria-pressed="true" aria-describedby="recognitionHelp">
                        Public
                      </button>
                      <button class="ff-chip" type="button" data-ff-recognition="anonymous" aria-pressed="false" aria-describedby="recognitionHelp">
                        Anonymous
                      </button>

                      <span class="ff-pill" id="recognitionPreviewPill" data-ff-recognition-preview>
                        Shows as — <span class="ff-num" id="recognitionPreviewText">Your name</span>
                      </span>
                    </div>
                  </div>
                </div>
              </details>

              <!-- PAYMENT -->
              <fieldset class="ff-fieldset" aria-label="Payment details">
                <div class="ff-row ff-row--between ff-wrap ff-ais">
                  <div class="ff-minw-0">
                    <div class="ff-kicker">Payment</div>
                    <p class="ff-help ff-muted ff-m-0">Card or wallet options appear below (Stripe).</p>
                  </div>
                </div>

                <!-- Stripe element mount (ONLY ONE) -->
                <div class="ff-mini ff-mini--premium ff-mt-2" id="stripeMount" aria-label="Stripe payment element">
                  <!-- Optional loader text (safe if unused) -->
                  <div class="ff-row ff-row--between ff-wrap ff-ais ff-mb-2" aria-label="Payment loading state">
                    <span class="ff-pill">Secure input</span>
                    <span class="ff-help ff-muted" data-ff-payment-loader hidden>Loading payment form…</span>
                  </div>

                  <div id="paymentElement" data-ff-stripe-element></div>
                  <p class="ff-help ff-mt-2">Securely processed by Stripe.</p>
                </div>

                <!-- PayPal mount (optional) -->
                <div class="ff-mini ff-mini--premium" id="paypalMount" hidden aria-label="PayPal buttons">
                  <div class="ff-kicker">PayPal</div>
                  <div id="paypalButtons" data-ff-paypal-buttons></div>
                </div>

                <div class="ff-sr" role="status" aria-live="polite" aria-atomic="true" data-ff-payment-live></div>
              </fieldset>

              <!-- SUMMARY -->
              <section class="ff-mini ff-mini--premium" aria-label="Donation summary" data-ff-summary>
                <div class="ff-row ff-row--between ff-wrap ff-ais">
                  <div>
                    <div class="ff-kicker">Summary</div>
                    <div class="ff-card__title">You’ll receive an instant receipt</div>
                  </div>
                  <span class="ff-pill">Total — <span class="ff-num" id="summaryTotal">—</span></span>
                </div>

                <div class="ff-slimlist ff-slimlist--premium ff-mt-2">
                  <div class="ff-slimitem">
                    <span class="ff-help">Donation</span>
                    <strong class="ff-help ff-num" id="summaryAmount">$0</strong>
                  </div>
                  <div class="ff-slimitem">
                    <span class="ff-help">Fees</span>
                    <strong class="ff-help ff-num" id="summaryFees">—</strong>
                  </div>
                  <div class="ff-slimitem" id="summaryTeamRow" hidden>
                    <span class="ff-help">Crediting</span>
                    <strong class="ff-help ff-num" id="summaryTeam">Team</strong>
                  </div>
                </div>

                <p class="ff-help ff-mt-2">Receipt email: <span class="ff-num" id="receiptEmail">your email</span></p>
              </section>

              <!-- CHECKOUT BAR -->
              <div class="ff-checkoutbar ff-checkoutbar--premium ff-mt-2" role="group" aria-label="Checkout actions" data-ff-checkoutbar>
                <button
                  class="ff-btn ff-btn--primary"
                  id="payBtn"
                  type="submit"
                  data-ff-pay-btn
                  aria-label="Continue to secure payment"
                >
                  Continue
                </button>

                <button class="ff-btn ff-btn--ghost" type="button" data-ff-share-open aria-label="Share fundraiser">Share</button>
                <button class="ff-btn ff-btn--ghost" type="button" data-ff-proof-open aria-label="Open proof kit">Proof kit</button>
                <button class="ff-btn ff-btn--ghost" type="button" data-ff-policy-open aria-label="Open trust center">Trust center</button>
              </div>

              <p class="ff-help ff-muted ff-mt-2">
                By donating, you agree to the campaign’s terms and privacy policy.
                <button class="ff-btn ff-btn--ghost ff-btn--sm" type="button" data-ff-policy-open aria-label="Open terms and privacy">
                  View
                </button>
              </p>
            </form>
          </section>
        </div>

        <!-- RAIL -->
        <aside class="ff-donate-rail ff-confidence" role="complementary" aria-label="Checkout confidence">
          <details class="ff-disc ff-confidence__fold" open>
            <summary class="ff-disc__sum">
              <span class="ff-disc__meta">
                <span class="ff-kicker">Confidence</span><br />
                <span class="ff-help">Why donors feel safe here</span>
              </span>
              <span class="ff-pill" aria-hidden="true">Open</span>
            </summary>

            <div class="ff-disc__body">
              <div class="ff-mini ff-mini--premium">
                <div class="ff-kicker">Built for donors</div>
                <div class="ff-card__title">Fast + respectful</div>
                <ul class="ff-bul ff-mt-2">
                  <li>Secure processing via Stripe.</li>
                  <li>Instant receipt to your email.</li>
                  <li>Anonymous donations supported.</li>
                  <li>Share tools + QR to spread the word.</li>
                </ul>
                <div class="ff-row ff-wrap ff-mt-2" role="group" aria-label="Confidence actions">
                  <button class="ff-btn ff-btn--secondary" type="button" data-ff-share-open aria-label="Open share tools">Share tools</button>
                  <a class="ff-btn ff-btn--ghost" href="#faq" aria-label="Jump to FAQ">FAQ</a>
                  <button class="ff-btn ff-btn--ghost" type="button" data-ff-policy-open aria-label="Open trust center">Trust center</button>
                </div>
              </div>

              <div class="ff-mini ff-mini--premium">
                <div class="ff-kicker">Sponsor-ready</div>
                <div class="ff-card__title">Make it easy to say “yes”</div>
                <p class="ff-help">Sponsors get premium recognition and a share-ready packet.</p>
                <div class="ff-row ff-wrap ff-mt-2" role="group" aria-label="Sponsor quick actions">
                  <button
                    class="ff-btn ff-btn--primary ff-btn--sm"
                    type="button"
                    data-ff-sponsor-cta
                    data-ff-prefill
                    data-ff-prefill-amount="1000"
                    aria-label="Sponsor $1,000"
                  >
                    Sponsor $1,000
                  </button>
                  <button class="ff-btn ff-btn--ghost ff-btn--sm" type="button" data-ff-proof-open> Sponsor packet </button>
                  <button class="ff-btn ff-btn--ghost ff-btn--sm" type="button" data-ff-share-open> Share </button>
                </div>
              </div>

              <div class="ff-mini ff-mini--premium" aria-label="Need help">
                <div class="ff-kicker">Need help?</div>
                <div class="ff-card__title">One-step support</div>
                <p class="ff-help ff-muted">Receipt not showing up? Check Promotions/Spam, then email support.</p>
                <button class="ff-btn ff-btn--ghost ff-btn--sm" type="button" data-ff-policy-open>Open Trust center</button>
              </div>
            </div>
          </details>
        </aside>
      </div>
    </article>
  </div>
</section>


<section id="faq"
  class="ff-section ff-faq ff-faq--sv ff-faq--v14 ff-faq--v14_2_1"
  aria-labelledby="faqTitle"
  data-ff-faq
  data-ff-section="faq">
  <div class="ff-container">
    <article class="ff-card ff-card--lift ff-card--premium ff-pad" data-ff-faq-card>
      <!-- Header: tight, mobile-first -->
      <header class="ff-faq__head ff-row ff-row--between ff-wrap ff-ais">
        <div class="ff-minw-0">
          <div class="ff-kicker">FAQ</div>
          <h2 id="faqTitle" class="ff-h2">Quick answers</h2>
          <p class="ff-lead ff-faq__lead ff-faq__lead--tight">Clarity reduces hesitation and increases trust.</p>
          <div class="ff-row ff-wrap ff-mt-2 ff-faq__pills" aria-label="FAQ highlights">
            <span class="ff-pill">Stripe-secured</span>
            <span class="ff-pill">Instant receipt</span>
            <span class="ff-pill">Anonymous option</span>
            <span class="ff-pill ff-pill--accent" hidden data-ff-verified-pill aria-live="polite" aria-atomic="true">Verified</span>
          </div>
        </div>
        <!-- Quick action row -->
        <nav class="ff-row ff-wrap ff-faq__actions" aria-label="FAQ actions">
          <button class="ff-btn ff-btn--ghost ff-btn--sm" type="button" data-ff-share-open aria-label="Share fundraiser">Share</button>
          <button class="ff-btn ff-btn--ghost ff-btn--sm" type="button" data-ff-policy-open aria-label="Open trust center">Trust</button>
          <a class="ff-btn ff-btn--primary ff-btn--sm" href="#donate" aria-label="Jump to donate">Donate</a>
        </nav>
      </header>

      <!-- FAQ grid (auto 1-col mobile, 2-col desktop via CSS) -->
      <div class="ff-faq__grid ff-mt-3" data-ff-faq-grid>
        <!-- Q1: Security -->
        <details class="ff-disc ff-disc--premium ff-faq__item" data-ff-faq-item>
          <summary class="ff-disc__sum" aria-controls="faqSecureBody">
            <span class="ff-disc__meta">
              <span class="ff-kicker">Is this secure?</span><br />
              <span class="ff-help">Yes — Stripe-secured checkout</span>
            </span>
            <span class="ff-pill" aria-hidden="true">Open</span>
          </summary>
          <div class="ff-disc__body" id="faqSecureBody">
            <ul class="ff-bul">
              <li>Payments are processed via Stripe and encrypted in transit.</li>
              <li>Receipts are emailed instantly after successful payment.</li>
              <li>No account or login required.</li>
            </ul>
            <div class="ff-row ff-wrap ff-mt-2" role="group" aria-label="Security actions">
              <button class="ff-btn ff-btn--ghost ff-btn--sm" type="button" data-ff-policy-open>Trust center</button>
              <a class="ff-btn ff-btn--ghost ff-btn--sm" href="#donate">Donate</a>
            </div>
          </div>
        </details>
        <!-- Q2: Anonymous -->
        <details class="ff-disc ff-disc--premium ff-faq__item" data-ff-faq-item>
          <summary class="ff-disc__sum" aria-controls="faqAnonBody">
            <span class="ff-disc__meta">
              <span class="ff-kicker">Can I donate anonymously?</span><br />
              <span class="ff-help">Yes — toggle “anonymous”</span>
            </span>
            <span class="ff-pill" aria-hidden="true">Open</span>
          </summary>
          <div class="ff-disc__body" id="faqAnonBody">
            <ul class="ff-bul">
              <li>Check “Donate anonymously” to hide your name from public lists.</li>
              <li>Your receipt still arrives by email.</li>
              <li>You can switch recognition preferences anytime before submitting.</li>
            </ul>
            <p class="ff-help ff-muted ff-mt-2">
              Tip: anonymous hides your name publicly, not from the organizer’s payment processor records.
            </p>
          </div>
        </details>
        <!-- Q3: Where does money go? -->
        <details class="ff-disc ff-disc--premium ff-faq__item" data-ff-faq-item>
          <summary class="ff-disc__sum" aria-controls="faqWhereBody">
            <span class="ff-disc__meta">
              <span class="ff-kicker">Where does the money go?</span><br />
              <span class="ff-help">See Allocation in Impact</span>
            </span>
            <span class="ff-pill" aria-hidden="true">Open</span>
          </summary>
          <div class="ff-disc__body" id="faqWhereBody">
            <ul class="ff-bul">
              <li>Gym time, tournament fees, travel support, and gear.</li>
              <li>Scholarships for families who need help.</li>
              <li>Allocation breakdown is shown and updated by organizers.</li>
            </ul>
            <div class="ff-row ff-wrap ff-mt-2" role="group" aria-label="Allocation actions">
              <a class="ff-btn ff-btn--ghost ff-btn--sm" href="#impact">View Impact</a>
              <a class="ff-btn ff-btn--ghost ff-btn--sm" href="#progress">View Progress</a>
            </div>
          </div>
        </details>
        <!-- Q4: Refunds -->
        <details class="ff-disc ff-disc--premium ff-faq__item" data-ff-faq-item>
          <summary class="ff-disc__sum" aria-controls="faqRefundBody">
            <span class="ff-disc__meta">
              <span class="ff-kicker">Do you offer refunds?</span><br />
              <span class="ff-help">Organizer policy may apply</span>
            </span>
            <span class="ff-pill" aria-hidden="true">Open</span>
          </summary>
          <div class="ff-disc__body" id="faqRefundBody">
            <p class="ff-help">
              Refund policies can vary by campaign. If you need help, email:
              {% set _email = support_email|default('support@getfuturefunded.com') %}
              <a href="mailto:{{ _email|e }}">{{ _email|e }}</a>
            </p>
            <p class="ff-help ff-muted ff-mt-2">
              Include your receipt email and approximate donation time so support can help faster.
            </p>
            <div class="ff-row ff-wrap ff-mt-2" role="group" aria-label="Support actions">
              <button class="ff-btn ff-btn--ghost ff-btn--sm" type="button" data-ff-copy-link>Copy fundraiser link</button>
              <button class="ff-btn ff-btn--ghost ff-btn--sm" type="button" data-ff-policy-open>Open Trust center</button>
            </div>
          </div>
        </details>
      </div>

      <div class="ff-divider" aria-hidden="true"></div>
      <!-- Footer: always visible, conversion-oriented -->
      <div class="ff-faq__foot ff-row ff-row--between ff-wrap ff-ais" aria-label="FAQ footer actions">
        <p class="ff-help ff-muted ff-maxw-68 ff-faq__footcopy">
          Still have a question? Open the Trust center for policies and support, or share the fundraiser link with a sponsor.
        </p>
        <div class="ff-row ff-wrap ff-faq__footbtns" role="group" aria-label="FAQ footer buttons">
          <button class="ff-btn ff-btn--secondary ff-btn--sm" type="button" data-ff-policy-open>Trust center</button>
          <button class="ff-btn ff-btn--ghost ff-btn--sm" type="button" data-ff-share-open>Share</button>
          <a class="ff-btn ff-btn--primary ff-btn--sm" href="#donate">Donate</a>
        </div>
      </div>
    </article>
  </div>
</section>

    </main>

{# ---------- Shared template fallbacks (JS will override org name/meta via applyBrand) ---------- #}
{% set _org_logo = org_logo|default(url_for('static', filename='images/logo.webp')) %}
{% set _org_name = org_name|default('Organization') %}
{% set _org_meta = org_location|default('City • State') %}
{% set _support_email = support_email|default('support@getfuturefunded.com') %}
{% set _share_url = canonical_url|default(request.base_url) %}
{% set _year = current_year|default(2026) %}

<footer class="ff-footer ff-footer--sv ff-footer--v14" role="contentinfo" data-ff-footer data-ff-section="footer">
  <div class="ff-container">
    <div class="ff-footer__grid" aria-label="Footer content">

      <!-- Brand / Org -->
      <section class="ff-footer__panel ff-footer__panel--brand ff-stack" aria-label="Organization">
        <a class="ff-brand ff-brand--footer" href="#top" aria-label="Back to top">
          <span class="ff-brand__mark" aria-hidden="true">
            <img
              class="ff-brand__logo ff-logo"
              src="{{ _org_logo }}"
              alt="Organization logo"
              width="46"
              height="46"
              loading="lazy"
              decoding="async"
            />
          </span>
          <span class="ff-brand__meta">
            <span class="ff-brand__title" id="footerOrgName">{{ _org_name }}</span>
            <span class="ff-brand__sub" id="footerOrgMeta">{{ _org_meta }}</span>
          </span>
        </a>

        <p class="ff-help ff-maxw-62 ff-m-0">
          Secure checkout, instant receipts, sponsor recognition, and share-ready tools — built for youth programs.
        </p>

        <div class="ff-footer__cta ff-row ff-wrap ff-ais" role="group" aria-label="Footer actions">
          <a class="ff-btn ff-btn--primary ff-btn--sm" href="#donate" data-ff-donate-cta>Donate</a>
          <button class="ff-btn ff-btn--secondary ff-btn--sm" type="button" data-ff-share-open aria-controls="ffShareModal" aria-expanded="false">Share</button>
          <button class="ff-btn ff-btn--ghost ff-btn--sm" type="button" data-ff-copy-link>Copy link</button>
          <button class="ff-btn ff-btn--ghost ff-btn--sm" type="button" data-ff-policy-open aria-controls="ffPolicyModal" aria-expanded="false">Trust center</button>
        </div>

        <div class="ff-row ff-wrap ff-ais" aria-label="Trust indicators">
          <span class="ff-pill ff-pill--verified" title="Receipts are sent immediately">
            <span aria-hidden="true">✓</span> Instant receipt
          </span>
          <span class="ff-pill ff-pill--accent" title="Live totals update as donations come in">
            <span class="ff-dot" aria-hidden="true"></span> Live
          </span>
          <span class="ff-pill" title="No login required">No login</span>
        </div>

        <div class="ff-footer__rule" aria-hidden="true"></div>

        <p class="ff-footer__fine ff-m-0" data-ff-premium-copy>
          Premium: white-label footer + custom domain, and optional “Powered by” removal on Pro.
        </p>
      </section>

      <!-- Explore -->
      <nav class="ff-footer__panel ff-footer__links" aria-label="Explore">
        <div class="ff-kicker">Explore</div>

        <ul class="ff-footer__linkgrid" aria-label="Footer links">
          <li><a href="#progress">Progress</a></li>
          <li><a href="#impact">Impact</a></li>
          <li><a href="#sponsors">Sponsors</a></li>
          <li><a href="#teams">Teams</a></li>
          <li><a href="#donate">Donate</a></li>
          <li><a href="#faq">FAQ</a></li>
        </ul>

        <div class="ff-footer__rule" aria-hidden="true"></div>

        <div class="ff-stack" role="group" aria-label="Quick tools">
          <button class="ff-btn ff-btn--ghost ff-btn--sm ff-justify-start" type="button" data-ff-share-open aria-controls="ffShareModal" aria-expanded="false">Share tools</button>
          <button class="ff-btn ff-btn--ghost ff-btn--sm ff-justify-start" type="button" data-ff-proof-open aria-controls="ffProofModal" aria-expanded="false">Proof kit</button>
          <button class="ff-btn ff-btn--ghost ff-btn--sm ff-justify-start" type="button" data-ff-policy-open aria-controls="ffPolicyModal" aria-expanded="false">Trust center</button>
        </div>
      </nav>

      <!-- Support -->
      <section class="ff-footer__panel ff-footer__panel--support ff-stack" aria-label="Support">
        <div class="ff-kicker">Support</div>
        <div class="ff-card__title ff-mt-1">Help in one step</div>

        <div class="ff-kpi" aria-label="Support details">
          <div class="ff-kpi__row">
            <div class="ff-kpi__name">Email</div>
            <div class="ff-kpi__val">
              <a href="mailto:{{ _support_email|e }}">{{ _support_email|e }}</a>
            </div>
          </div>

          <div class="ff-kpi__row">
            <div class="ff-kpi__name">Receipts</div>
            <div class="ff-kpi__val">Instant email delivery</div>
          </div>

          <div class="ff-kpi__row">
            <div class="ff-kpi__name">Powered by</div>
            <div class="ff-kpi__val">
              <span class="ff-powered" data-ff-powered>FutureFunded</span>
            </div>
          </div>
        </div>

        <p class="ff-help ff-muted ff-mt-2 ff-mb-0">
          Tip: if your receipt doesn’t show up, check Promotions/Spam, then email support.
        </p>

        <div class="ff-footer__rule" aria-hidden="true"></div>

        <div class="ff-row ff-row--between ff-wrap ff-ais" aria-label="Footer legal">
          <p class="ff-help ff-muted ff-m-0">© <span id="ffYear">{{ _year }}</span> FutureFunded. All rights reserved.</p>
          <a class="ff-btn ff-btn--ghost ff-btn--sm ff-justify-start" href="#top" aria-label="Back to top">Back to top ↑</a>
        </div>
      </section>

    </div>
  </div>
</footer>

<!-- Sticky support bar (v14.2) -->
<div class="ff-sticky ff-sticky--v14" data-ff-support data-ff-sticky="support" aria-hidden="true">
  <div class="ff-sticky__inner" role="region" aria-label="Live totals and donate">
    <div class="ff-sticky__left" aria-label="Live totals">
      <span class="ff-pill ff-pill--live" aria-label="Live">
        <span class="ff-dot" aria-hidden="true"></span>
        Live
      </span>
      <div class="ff-sticky__hint" aria-live="polite" aria-atomic="true">
        <span class="ff-help">Raised:</span> <span class="ff-num" id="stickyRaised">$0</span>
        <span class="ff-help" aria-hidden="true">•</span>
        <span class="ff-help">Goal:</span> <span class="ff-num" id="stickyGoal">$0</span>
      </div>
    </div>

    <div class="ff-sticky__actions" role="group" aria-label="Sticky actions">
      <button class="ff-btn ff-btn--ghost ff-btn--sm" type="button" data-ff-share-open aria-controls="ffShareModal" aria-expanded="false">Share</button>
      <a class="ff-btn ff-btn--primary ff-btn--sm" href="#donate" data-ff-sticky-donate aria-label="Jump to donate">Donate</a>
    </div>
  </div>
</div>

<!-- Mobile quick nav (v14.2.1) -->
<nav class="ff-tabs ff-tabs--v14 ff-tabs--v14_2_1" aria-label="Quick navigation" data-ff-tabs>
  <ul class="ff-tabs__bar" role="tablist" aria-label="Section shortcuts" data-ff-tabs-bar>
    <li role="presentation">
      <a class="ff-tab" href="#progress" data-ff-tab="progress" role="tab" aria-label="Progress">
        <span class="ff-tab__icon" aria-hidden="true">▦</span>
        <span class="ff-tab__label">Progress</span>
      </a>
    </li>
    <li role="presentation">
      <a class="ff-tab" href="#impact" data-ff-tab="impact" role="tab" aria-label="Impact">
        <span class="ff-tab__icon" aria-hidden="true">◎</span>
        <span class="ff-tab__label">Impact</span>
      </a>
    </li>
    <li role="presentation">
      <a class="ff-tab" href="#sponsors" data-ff-tab="sponsors" role="tab" aria-label="Sponsors">
        <span class="ff-tab__icon" aria-hidden="true">★</span>
        <span class="ff-tab__label">Sponsors</span>
      </a>
    </li>
    <li role="presentation">
      <a class="ff-tab" href="#donate" data-ff-tab="donate" role="tab" aria-label="Donate">
        <span class="ff-tab__icon" aria-hidden="true">♡</span>
        <span class="ff-tab__label">Donate</span>
      </a>
    </li>
  </ul>
</nav>

<!-- Back to top (IMPORTANT: no [hidden] attribute; JS toggles class/aria only) -->
<a
  class="ff-backtotop ff-icon ff-backtotop--v14_2_1"
  href="#top"
  aria-label="Back to top"
  aria-hidden="true"
  data-ff-backtotop
>
  <span aria-hidden="true">↑</span>
</a>

<!-- ===================== MODALS ===================== -->

<!-- SHARE MODAL -->
<div class="ff-modal ff-modal--v14 ff-modal--compact"
     hidden
     aria-hidden="true"
     id="ffShareModal"
     data-ff-modal="share"
     data-ff-share-modal>
  <button class="ff-modal__backdrop" type="button" data-ff-modal-close aria-label="Close share dialog"></button>

  <div class="ff-modal__panel ff-modal__panel--share"
       role="dialog"
       aria-modal="true"
       aria-labelledby="shareTitle"
       aria-describedby="shareDesc"
       tabindex="-1"
       data-ff-modal-panel>
    <header class="ff-modal__head ff-modal__head--tight">
      <div class="ff-minw-0">
        <div class="ff-kicker">Share</div>
        <div id="shareTitle" class="ff-card__title">Share this fundraiser</div>
        <p id="shareDesc" class="ff-help ff-muted ff-m-0">Copy link, text it, or print the QR.</p>
      </div>
      <button class="ff-icon ff-btn--ghost" type="button" data-ff-modal-close aria-label="Close share dialog">
        <span aria-hidden="true">✕</span>
      </button>
    </header>

    <div class="ff-modal__body ff-modal__body--tight">
      <div class="ff-sharegrid" role="document">

        <section class="ff-mini ff-mini--tight" aria-label="Fundraiser link">
          <div class="ff-row ff-row--between ff-ais">
            <div class="ff-minw-0">
              <div class="ff-kicker">Link</div>
              <div class="ff-card__title">One-tap share</div>
            </div>
            <span class="ff-pill ff-pill--sm" aria-hidden="true">No login</span>
          </div>

          <div class="ff-sharelink ff-mt-2">
            <label class="ff-sr" for="shareLink">Fundraiser link</label>
            <input class="ff-input ff-input--tight"
                   id="shareLink"
                   type="text"
                   readonly
                   value="{{ _share_url }}"
                   aria-label="Fundraiser link"
                   inputmode="url"
                   autocomplete="off" />
            <button class="ff-btn ff-btn--primary ff-btn--sm" type="button" data-ff-copy-link>Copy</button>
          </div>

          <div class="ff-shareactions ff-mt-2" role="group" aria-label="Share actions">
            <button class="ff-btn ff-btn--secondary ff-btn--sm" type="button" data-ff-native-share>Share…</button>
            <button class="ff-btn ff-btn--ghost ff-btn--sm" type="button" data-ff-sms-share>Text</button>
            <button class="ff-btn ff-btn--ghost ff-btn--sm" type="button" data-ff-email-share>Email</button>
            <button class="ff-btn ff-btn--ghost ff-btn--sm" type="button" data-ff-proof-open aria-controls="ffProofModal" aria-expanded="false">Proof kit</button>
          </div>

          <div class="ff-divider ff-my-2" aria-hidden="true"></div>

          <div class="ff-sharehint">
            <div class="ff-kicker">Timing</div>
            <p class="ff-help ff-muted ff-m-0">Best send: after practice, after games, and when a match is active.</p>
          </div>
        </section>

        <section class="ff-mini ff-mini--tight" aria-label="QR code">
          <div class="ff-row ff-row--between ff-ais">
            <div class="ff-minw-0">
              <div class="ff-kicker">QR</div>
              <div class="ff-card__title">Scan to donate</div>
              <p class="ff-help ff-muted ff-m-0">Perfect for flyers and sponsor asks.</p>
            </div>
            <span class="ff-pill ff-pill--sm" aria-hidden="true">Print-ready</span>
          </div>

          <div class="ff-qrrow ff-mt-2" aria-label="QR and actions">
            <img class="ff-qr ff-qr--lg"
                 id="shareQr"
                 alt="Fundraiser QR code"
                 width="176"
                 height="176"
                 loading="lazy"
                 decoding="async"
                 data-ff-qr />
            <div class="ff-qrmeta ff-minw-0">
              <div class="ff-kicker">Use it anywhere</div>
              <ul class="ff-bul ff-bul--tight ff-mt-1">
                <li>Flyers + posters</li>
                <li>Team group chats</li>
                <li>Sponsor packet</li>
              </ul>

              <div class="ff-row ff-wrap ff-mt-2" role="group" aria-label="QR actions">
                <button class="ff-btn ff-btn--primary ff-btn--sm" type="button" data-ff-download-qr>Download QR</button>
                <button class="ff-btn ff-btn--ghost ff-btn--sm" type="button" data-ff-copy-link>Copy link</button>
              </div>
            </div>
          </div>
        </section>

      </div>
    </div>
  </div>
</div>

<!-- PROOF KIT MODAL -->
<div class="ff-modal ff-modal--v14 ff-modal--compact"
     hidden
     aria-hidden="true"
     id="ffProofModal"
     data-ff-modal="proof"
     data-ff-proof-modal>
  <button class="ff-modal__backdrop" type="button" data-ff-modal-close aria-label="Close proof kit"></button>

  <div class="ff-modal__panel ff-modal__panel--proof"
       role="dialog"
       aria-modal="true"
       aria-labelledby="proofTitle"
       aria-describedby="proofDesc"
       tabindex="-1"
       data-ff-modal-panel>
    <header class="ff-modal__head ff-modal__head--tight">
      <div class="ff-minw-0">
        <div class="ff-kicker">Proof kit</div>
        <div id="proofTitle" class="ff-card__title">Sponsor + donor proof (share-ready)</div>
        <p id="proofDesc" class="ff-help ff-muted ff-m-0">Copy captions, preview recognition, and ship sponsor-ready assets.</p>
      </div>
      <button class="ff-icon ff-btn--ghost" type="button" data-ff-modal-close aria-label="Close proof kit">
        <span aria-hidden="true">✕</span>
      </button>
    </header>

    <div class="ff-modal__body ff-modal__body--tight">
      <div class="ff-proofgrid" role="document">

        <section class="ff-mini ff-mini--tight" aria-label="Share caption">
          <div class="ff-row ff-row--between ff-ais">
            <div class="ff-minw-0">
              <div class="ff-kicker">Share caption</div>
              <div class="ff-card__title">Copy-paste ready</div>
            </div>
            <span class="ff-pill ff-pill--sm" aria-hidden="true">Works everywhere</span>
          </div>

          <label class="ff-sr" for="proofCaption">Share caption</label>
          <textarea class="ff-input ff-input--tight ff-captionbox ff-mt-2"
                    id="proofCaption"
                    readonly
                    rows="5"
                    aria-label="Share caption">Supporting {{ _org_name }} this season 🎯

Help us cover travel, gym time, fees, and gear. Secure checkout + instant receipt:
{{ _share_url }}

#FutureFunded #YouthSports #Community</textarea>

          <div class="ff-proofactions ff-mt-2" role="group" aria-label="Caption actions">
            <button class="ff-btn ff-btn--primary ff-btn--sm" type="button" data-ff-copy-caption>Copy caption</button>
            <button class="ff-btn ff-btn--secondary ff-btn--sm" type="button" data-ff-native-share>Share…</button>
            <button class="ff-btn ff-btn--ghost ff-btn--sm" type="button" data-ff-share-open aria-controls="ffShareModal" aria-expanded="false">Share tools</button>
          </div>

          <p class="ff-help ff-muted ff-mt-2">Pro tip: send to sponsors with the QR + “Sponsor $1,000” ask.</p>
        </section>

        <section class="ff-mini ff-mini--tight" aria-label="Recognition preview">
          <div class="ff-row ff-row--between ff-ais">
            <div class="ff-minw-0">
              <div class="ff-kicker">Recognition preview</div>
              <div class="ff-card__title">What supporters will see</div>
            </div>
            <a class="ff-btn ff-btn--ghost ff-btn--sm" href="#sponsors">Leaderboard →</a>
          </div>

          <div class="ff-kpi ff-mt-2" aria-label="Recognition preview details">
            <div class="ff-kpi__row">
              <div class="ff-kpi__name">Donor</div>
              <div class="ff-kpi__val" id="proofDonorName">Your name</div>
            </div>
            <div class="ff-kpi__row">
              <div class="ff-kpi__name">Amount</div>
              <div class="ff-kpi__val ff-num" id="proofAmount">$75</div>
            </div>
            <div class="ff-kpi__row">
              <div class="ff-kpi__name">Campaign</div>
              <div class="ff-kpi__val" id="proofCampaign">{{ campaign_name|default('Season Fund') }}</div>
            </div>
          </div>

          <div class="ff-divider ff-my-2" aria-hidden="true"></div>

          <div class="ff-row ff-wrap" role="group" aria-label="Proof actions">
            <button class="ff-btn ff-btn--ghost ff-btn--sm" type="button" data-ff-policy-open aria-controls="ffPolicyModal" aria-expanded="false">Trust center</button>
            <button class="ff-btn ff-btn--ghost ff-btn--sm" type="button" data-ff-copy-link>Copy link</button>
            <a class="ff-btn ff-btn--primary ff-btn--sm" href="#donate" data-ff-donate-cta>Donate</a>
          </div>

          <p class="ff-help ff-muted ff-mt-2">Premium flex: sponsor spotlight auto-posts + export-ready assets.</p>
        </section>

      </div>
    </div>
  </div>
</div>

<!-- TRUST CENTER MODAL -->
<div class="ff-modal ff-modal--v14 ff-modal--compact"
     hidden
     aria-hidden="true"
     id="ffPolicyModal"
     data-ff-modal="policy"
     data-ff-policy-modal>
  <button class="ff-modal__backdrop" type="button" data-ff-modal-close aria-label="Close trust center"></button>

  <div class="ff-modal__panel ff-modal__panel--policy"
       role="dialog"
       aria-modal="true"
       aria-labelledby="policyTitle"
       aria-describedby="policyDesc"
       tabindex="-1"
       data-ff-modal-panel>
    <header class="ff-modal__head ff-modal__head--tight">
      <div class="ff-minw-0">
        <div class="ff-kicker">Trust center</div>
        <div id="policyTitle" class="ff-card__title">Security, privacy, and policies</div>
        <p id="policyDesc" class="ff-help ff-muted ff-m-0">Secure checkout, donor control, and clear support paths.</p>
      </div>
      <button class="ff-icon ff-btn--ghost" type="button" data-ff-modal-close aria-label="Close trust center">
        <span aria-hidden="true">✕</span>
      </button>
    </header>

    <div class="ff-modal__body ff-modal__body--tight">
      <div class="ff-policygrid" role="document" aria-label="Trust center content">

        <section class="ff-mini ff-mini--tight" aria-label="Security">
          <div class="ff-row ff-row--between ff-ais">
            <div class="ff-minw-0">
              <div class="ff-kicker">Security</div>
              <div class="ff-card__title">Protected checkout</div>
            </div>
            <span class="ff-pill ff-pill--sm" aria-hidden="true">Stripe</span>
          </div>
          <ul class="ff-bul ff-mt-2">
            <li>Payments are processed by Stripe (encrypted in transit).</li>
            <li>No password required for donors.</li>
            <li>Receipts delivered instantly by email.</li>
          </ul>
        </section>

        <section class="ff-mini ff-mini--tight" aria-label="Privacy">
          <div class="ff-row ff-row--between ff-ais">
            <div class="ff-minw-0">
              <div class="ff-kicker">Privacy</div>
              <div class="ff-card__title">Donor control</div>
            </div>
            <span class="ff-pill ff-pill--sm" aria-hidden="true">Optional</span>
          </div>
          <ul class="ff-bul ff-mt-2">
            <li>Recognition is optional; anonymous donations supported.</li>
            <li>Only essential details are collected for receipts.</li>
            <li>You can change recognition before submitting.</li>
          </ul>
        </section>

        <section class="ff-mini ff-mini--tight" aria-label="Support">
          <div class="ff-kicker">Support</div>
          <div class="ff-card__title">Get help fast</div>

          <div class="ff-slimlist ff-mt-2" aria-label="Support contact">
            <div class="ff-slimitem">
              <span class="ff-help">Email</span>
              <strong class="ff-help ff-num"><a href="mailto:{{ _support_email|e }}">{{ _support_email|e }}</a></strong>
            </div>
            <div class="ff-slimitem">
              <span class="ff-help">Receipts</span>
              <strong class="ff-help">Instant email delivery</strong>
            </div>
          </div>

          <p class="ff-help ff-muted ff-mt-2">
            If your receipt doesn’t show up, check Promotions/Spam, then email support.
          </p>

          <div class="ff-row ff-wrap ff-mt-2" role="group" aria-label="Support actions">
            <button class="ff-btn ff-btn--ghost ff-btn--sm" type="button" data-ff-copy-link>Copy fundraiser link</button>
            <button class="ff-btn ff-btn--secondary ff-btn--sm" type="button" data-ff-share-open aria-controls="ffShareModal" aria-expanded="false">Share tools</button>
          </div>
        </section>

        <section class="ff-mini ff-mini--tight" aria-label="Policies">
          <div class="ff-kicker">Policies</div>
          <div class="ff-card__title">Campaign guidelines</div>
          <p class="ff-help ff-mt-2">Refund policies can vary by campaign. Contact support for issues or corrections.</p>
          <p class="ff-help ff-muted ff-mt-2">Premium flex: publish a full privacy policy + terms page per organization (white-label).</p>
        </section>

      </div>

      <div class="ff-modal__footerbar" role="group" aria-label="Trust center actions">
        <button class="ff-btn ff-btn--ghost ff-btn--sm" type="button" data-ff-copy-link>Copy link</button>
        <button class="ff-btn ff-btn--secondary ff-btn--sm" type="button" data-ff-share-open aria-controls="ffShareModal" aria-expanded="false">Share</button>
        <a class="ff-btn ff-btn--primary ff-btn--sm" href="#donate" data-ff-donate-cta>Donate</a>
      </div>
    </div>
  </div>
</div>

<!-- ACTION SHEET (optional UI surface; safe if unused) -->
<div class="ff-sheet ff-sheet--sv ff-sheet--v14"
     hidden
     aria-hidden="true"
     data-ff-sheet
     id="ffActionSheet">
  <button class="ff-sheet__backdrop" type="button" data-ff-sheet-close aria-label="Close quick actions"></button>

  <div class="ff-sheet__panel ff-sheet__panel--tight"
       role="dialog"
       aria-modal="true"
       aria-labelledby="ffSheetTitle"
       tabindex="-1">
    <div class="ff-sheet__grab" aria-hidden="true"><i></i></div>

    <header class="ff-sheet__head ff-sheet__head--tight">
      <div class="ff-row ff-row--between ff-ais">
        <div class="ff-minw-0">
          <div class="ff-sheet__title" id="ffSheetTitle">Quick actions</div>
          <div class="ff-sheet__sub" id="ffSheetSub">Donate, share, or sponsor — in one tap.</div>
        </div>
        <button class="ff-icon ff-btn--ghost" type="button" data-ff-sheet-close aria-label="Close quick actions">
          <span aria-hidden="true">✕</span>
        </button>
      </div>
    </header>

    <div class="ff-sheet__body ff-sheet__body--tight">
      <div class="ff-row ff-wrap" role="group" aria-label="Quick actions">
        <a class="ff-btn ff-btn--primary" href="#donate" data-ff-donate-cta>Donate</a>
        <button class="ff-btn ff-btn--secondary" type="button" data-ff-share-open aria-controls="ffShareModal" aria-expanded="false">Share</button>
        <button class="ff-btn ff-btn--ghost" type="button" data-ff-proof-open aria-controls="ffProofModal" aria-expanded="false">Proof kit</button>
        <button class="ff-btn ff-btn--ghost" type="button" data-ff-policy-open aria-controls="ffPolicyModal" aria-expanded="false">Trust center</button>
        <button class="ff-btn ff-btn--ghost" type="button" data-ff-sheet-close>Close</button>
      </div>
    </div>
  </div>
</div>
<!-- [FF PATCH] Removed inline Stripe boot script (ff-app.js owns Stripe lifecycle). -->

</body>
</html>



